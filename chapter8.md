## 第八章 操作系统：资源管理的结构
操作系统是硬件与应用之间的总调度师。它将物理资源虚拟化，提供统一的编程接口，并管理着资源访问的复杂并发。现代操作系统非常复杂，一个成熟的系统往往需要几千个人月的精雕细磨才能使用。从结构的角度看，操作系统提供了十几层不同的抽象来，它用精妙的结构设计，将有限、混乱、易错的物理资源，组织为无限、整齐、可靠的计算环境，为软件的运行提供了基础。

操作系统的核心是资源管理， 具体体现在资源的的虚拟化，资源的并发访问化，以及可编程接口。本章我们深入了解一下操作系统用什么样的结构来支持这些能力。

### 8.1 进程与线程
从CPU的角度看，世界简单的令人厌倦，读指令，执行指令，不断循环往复直到停机。这是单一的执行结构。分时系统是操作系统的进化的一个关键里程碑，它把CPU时间分为短暂的片段，每个片段执行不同的指令序列，时间到了就切换到下一个。尽管CPU依然只有一个，但是从指令的角度看，每个指令序列的执行就好像独占了CPU，这是进程和线程最初的来源。

> 每个指令序列不执行的时候，处于暂停状态。从人类的角度看，因为CPU实在是太快了，这个暂停完全没有感知，就如同你在暗夜甩动一个光点，光点会连成线。
> 然而，这个暂停，引入了时间依赖。别的指令序列可能在你暂停的时候修改了某个值。这种情况在独占CPU的情况下从来不可能发生。这个问题在后面我们会重点说明。

#### 8.1.1 进程
进程是对正在运行程序的封装。它提供了一个封闭的“虚拟计算机”幻觉，独立的地址空间、打开的文件、信号处理器等资源。

进程仅仅在允许它执行的时间片内执行。操作系统使用进程控制块（PCB，Process Control Block）记录关于一个进程的所有关键信息。这样当需要执行一个进程的时候，操作系统就可以依据这些信息重建进程依赖的环境，让进程完全感觉不到差异。

进程控制块是内核中表示进程的结构体，关键信息有：
- 进程状态（就绪、运行、阻塞）
- 程序计数器（PC）和寄存器上下文
- 内存管理信息（页表指针）
- 打开文件表
- 统计信息和权限

切换进程不仅仅单纯依赖时间片。现代操作系统支持优先级的概念，高优先级的进程有更多的机会被调度，所以有更高的执行概率。

还有一个需要调度进程的机会就是中断响应。

**中断**
中断本来是CPU的概念，但现代操作系统广泛借用这个词来表示任何需要内核立即关注的事件。CPU直接触发的称为硬中断（Hardware Interrupt），如磁盘完成I/O、网卡收到数据包、键盘输入等；内核自身或软件触发的称为**软中断**（SoftIRQ），如定时器、任务队列处理等。

当中断发生时：
1. CPU暂停当前正在执行的指令，保存现场（寄存器、PC等）到内核栈。
2. 跳转到预先注册的中断处理程序（Interrupt Handler）。
3. 内核处理事件（例如把收到的网络数据包放入对应socket的接收队列）。
4. 检查哪些进程正在等待这个事件（例如正在read()磁盘或网络的进程）。
5. 将这些进程的状态从阻塞变为就绪，并可能立即触发调度。

这种机制确保了系统对外部事件的及时响应。例如，当用户按下键盘时，硬中断几乎瞬间唤醒对应的用户进程，让用户感觉输入毫无延迟。

**进程状态**
一个进程在其生命周期中会在几种主要状态之间转换：
- 新建。进程刚被创建，PCB已分配，但尚未获得资源。
- 就绪。已获得除CPU外所有资源，等待CPU调度。
- 运行。正在CPU上执行。
- 阻塞。因等待I/O、信号、锁等资源而暂停。
- 终止。执行完毕或被杀死，等待回收资源。

当一个进程处于阻塞状态，它几乎不占用CPU时间。   

**上下文切换（Context Switch）**
当操作系统决定从当前进程切换到另一个进程的过程叫做上下文切换。这里的上下文是针对CPU等来说的，指的执行进程的环境。这是一个相对昂贵的操作，因为它需要：
1. 保存当前进程的所有寄存器、程序计数器、栈指针等状态到PCB
2. 切换页表（CR3寄存器），指向对应进程的虚拟地址空间，这个后面会提到
3. 恢复目标进程的寄存器和执行状态
4. 可能还需要刷新TLB和部分缓存

一次完整的进程上下文切换通常消耗几微秒到十几微秒。在高负载系统中，频繁的上下文切换是性能杀手之一，这也是为什么线程被广泛采用的重要原因。

#### 8.1.2 线程
线程是进程内的轻量级执行流。既然是执行流，必然拥有独立的栈和寄存器上下文；既然是轻量级的，必然和进程共享大部分的资源。线程和进程的核心区别就是，进程的地址空间是独立的，但是同进程内的线程共享进程的地址空间。这意味着，线程之间可以直接传递信息不需要跨越进程边界，这非常高效。

#### 8.1.3 调度算法
调度算法选择下一个运行谁，是操作系统内核的核心秘技。在无限的计算需求和有限的CPU核心的情况下，需要决定下一个执行哪个线程，使得进程得到公平的执行时间，且资源利用最大化。

经典调度算法有：

| 算法 | 描述 | 优劣 |
|------|------|------|
| FIFO | 先来先服务 | 简单，但护航效应（长进程堵塞短进程）|
| Round Robin | 时间片轮转 | 公平，但时间片大小难调 |
| STCF（最短完成时间优先）| 抢占式SJF | 最优平均周转时间，但可能导致饥饿 |
| 多级反馈队列 | 多级优先级，优先执行短任务，老化防止饥饿 | 兼顾响应和周转，现代系统的基础 |

> 老化（Aging）指的是随着等待时间增加提升优先级，以确保低优先级任务最终能被执行。

**Linux CFS（完全公平调度器）**：
CFS的核心思想不是固定的时间片，而是考虑了执行优先级的虚拟运行时间（vruntime），调度器始终选择vruntime最小的任务运行。

```
vruntime = 实际运行时间 / 权重（优先级）
```

CFS用红黑树（自平衡BST）组织就绪队列，键为vruntime。
- 调度选择：取树的最左节点 → `O(log n)`
- 插入：`O(log n)`
- 优势：能处理数千个线程，扩展性好，公平性精确

```c
// 简化的CFS决策逻辑
task = rb_leftmost(&cfs_rq->tasks_timeline);
schedule(task);
```

### 8.2 内存管理
内存是仅次于CPU的稀缺资源，程序必须加载到内存才能运行，因为内存比外部存储大约快一万倍！时间片和调度让程序似乎独占CPU，虚拟内存和页面置换让程序似乎独占了内存。

内存管理是核心是虚拟内存，让每个进程都以为独占整个内存。这不仅仅提升了安全性，还大大简化了内存管理的复杂度。最早的现代虚拟内存思想通常追溯到 Atlas 系统，而 IBM System/360则推动了其商业化与普及。

#### 8.2.1 虚拟内存（Virtual Memory）
在操作系统看来，物理内存有限，但是多个进程竞争，且进程数并不固定。更麻烦的是，进程的地址空间可能超过物理内存。

虚拟内存是物理内存和进程之间的一个中间层，隔绝了程序对物理内存的直接访问。其核心是为每个进程提供连续的虚拟地址空间，以32位x86为例，每个进程都可以独享4G（`2^32`）的虚拟内存空间 。在需要的时候，通过页表映射到碎片化的物理内存。内存的管理单元是页，通常是4K大小。

地址转换流程值得说一下，其流程如下 ：
```
虚拟地址 (VA) = [虚拟页号(VPN)] + [页内偏移]
                    ↓
              查询页表（由VPN索引）
                    ↓
              物理页框号 (PPN)
                    ↓
物理地址 (PA) = [PPN] + [页内偏移]
```

以x86-64四级页表为例，其页表结构是：
```
虚拟地址 (48位): [9位L4][9位L3][9位L2][9位L1][12位偏移]

CR3寄存器 → L4页目录 → L3页目录 → L2页目录 → L1页表 → 物理页框
(根指针)     (512项)    (512项)    (512项)    (512项)
```

常见的x86-64 CPU都使用了48bit的虚拟地址（256T总空间大小）且是多级页表。多级页表的优势是无需为整个虚拟地址空间分配连续页表内存，未使用的虚拟地址空间不分配中间页表。

页表转换是现在操作系统最关键的核心操作之一，绝大多数CPU都内建了支持，通过一种叫做TLB（Translation Lookaside Buffer）的机制：
- 硬件缓存，存储最近使用的虚拟页表到物理页表的映射
- 命中则地址转换只需极少CPU周期；未命中需遍历页表（数百周期）
- 局部性原理使其命中率极高（>99%）

#### 8.2.2 虚拟内存的使用 
这种机制导致了几个非常有意思的结果。这里简单说一下。

**内存映射（MMAP）**
比如你要访问一个文件，并不需要手工加载它，而是打开之后就直接访问它。你访问的内容因为尚未加载，会触发一个缺页中断，操作系统会分配物理页面，自动加载文件的相关部分到物理页面，映射到虚拟页面，然后返给你。这个是完全透明的，就好像那个页面已经在哪里。这个叫做内存映射（MMAP）。

MMAP的核心是**按需分页**。不预先加载，只在访问缺页时才从磁盘加载页。程序启动快，物理内存利用率高，因为仅仅真正被使用的才加载。具体流程为： CPU触发缺页异常 → OS从磁盘读页 → 建立映射 → 恢复执行。

**几乎“永远“不会失败的malloc**
malloc是C环境下申请内存的函数，它会在申请失败的时候返回0地址，表示失败。但是这个函数的实际结果仅仅是分配一段虚拟内存的地址范围，而不会直接映射到物理内存。对于64位的CPU来说，一个程序可用的虚拟内存有256T（其中内核部分大约使用一半）。这意味着，如果仅仅是分配，而不直接使用，malloc几乎永远不会失败，因为你总是有足够的虚拟内存地址空间可用。

但是malloc仍然可能返回0，常见的情况有：
1. 系统禁用了内存过量使用（overcommit），这时会提前检查物理内存是否足够
2. 操作系统通常对单个进程的最大虚拟地址空间有软限制，实际大小远远小于CPU支持的范围 
3. 虚拟地址空间碎片化，放不下连续的虚拟内存区段。malloc要求返回的内存区间必须是连续的

尽管malloc本身没有失败，但是写入的时候，会触发物理内存的实际映射。如果物理内存和交换分区（把物理内存临时存储到磁盘上的分区）都耗尽了，会触发操作系统的最后干预。OOM Killer会按照某种启发式策略选择一个进程杀死以保证整个系统不崩溃！

内存不够时，把物理页面替换的过程叫做页面置换。

#### 8.2.3 页面置换
如果不断加载新的页面，总有一个时候，物理内存不够了，我们需要用页面置换算法，把那些暂时不需要的页面卸载掉。基础的算法有LRU（最近最少使用），核心思想是最近用的最少，最近的将来再次被用到的机会也不大，就可以换出去。但是跟踪所有的页面的需要特别支持，一个近似的算法是时钟算法，也叫二次机会算法，在现代OS中使用较多。

它把所有的页面的物理页面组织成一个逻辑上的环形链表，当发生缺页中断时，开始扫描：
1. 如果使用位是1，清零并继续，直到找到第一个使用位是0的页面，换出它
2. 需要时继续扫描。因为是环形的，这个可以一直不中断

#### 8.2.4 进程与线程的内存布局对比
我们已经知道，进程是独享虚拟地址空间的，但是线程不是。但是线程拥有自己的栈（一段固定但是不同的地址空间），所有的线程操作都是在自己的栈上面。下面列出了它们的内存布局简要示意，可以让你对这两个最常见的操作系统对象有一个直观的理解。

```
进程A (独立地址空间)      进程B (独立地址空间)
+----------------+      +----------------+
| 代码 .text     |      | 代码 .text     |
| 数据 .data/.bss|      | 数据 .data/.bss|
| 堆             |      | 堆             |
| 栈             |      | 栈             |
+----------------+      +----------------+
        ↑                       ↑
    页表A                   页表B
        ↓                       ↓
+----------------+----------------+ ... (物理内存)

线程组（共享地址空间）
+----------------+
| 代码 .text     | ← 线程1、线程2、... 共享
| 数据 .data/.bss|
| 堆            |
+----------------+
| 线程1栈 | 线程2栈 | 线程3栈 | ... (独立)
+----------------+
```

### 8.3 文件系统
内存是易失的，掉点后，所有的信息立即清空。想要把信息持久化地保存下来，我们需要外部存储。文件系统是持久化设备的支持结构，也是数据在持久存储上的表达形式。

#### 8.3.1 文件系统的核心问题
操作系统把设备分为字符设备（如键盘，可以一个字符一个字符地输入，较慢）和块设备（每次只能一个块，一个批次，一般很快）。磁盘就是典型的块设备，读写单位为扇区/块。文件系统的任务是将块的线性空间组织为文件和目录的层次树结构，这样就可以在成千上万个文件中快速找到需要的那个 。

文件系统的核心概念是目录和文件。后者存储实际的数据，而前者是文件的容器。所有的文件和目录都有名字，我们用路径表示一个文件的绝对位置。如`/home/bob/log.txt`表示`/`目录下，`home/`目录下，`bob/`目录下一个叫做`log.txt`的文件，其实这是一个索引结构。

这个结构允许我们快速找到需要的文件的。这引入文件系统的核心结构

#### 8.3.2 i-node与FAT
i-node是类Unix操作系统下文件系统的经典结构，表征文件组织。以经典的ext文件系统系列为例：
- 每个文件有一个i-node（索引节点），存储：元数据（大小、权限、时间戳）+ 数据块指针
- 直接指针（12个）→ 指向数据块
- 间接指针（1个）→ 指向指针块（128个指针）
- 双重间接指针 → 指向间接块
- 三重间接指针 → ...

```
i-node结构:
[ direct[0]  ] → 数据块
[ direct[1]  ] → 数据块
...
[ direct[11] ] → 数据块
[ indirect   ] → 指针块[128个指针] → 数据块
[ double-ind ] → 指针块 → 指针块 → 数据块
[ triple-ind ] → ...
```
通过多级的间接指针，可以支持非常大的文件。

FAT（File Allocation Table）也是流行的文件结构，MS-DOS/早期Windows系统使用，移动存储也常使用这个格式，特点是很简单直观：
- 文件分配表是一个数组，索引为块号，值为下一个块号（类似链表）
- 目录项存储文件的第一个块号

FAT最大的优点是实现容易，简单直接。缺点是随机访问需遍历链表，也无内置扩展性，非常适合容量较小的块设备。

它们的特性比较如下。

| 特性    | i-node         | FAT          |
| ----- | -------------- | ------------ |
| 随机访问  | O(1) 通过间接块     | O(n) 需遍历     |
| 大文件支持 | 极佳（间接块）        | 差（链表遍历）      |
| 目录组织  | B树（现代fs）       | 线性列表         |
| 破碎容忍  | 高              | 低            |
| 典型系统  | ext4, XFS, ZFS | FAT32, exFAT |

#### 8.3.3 日志文件系统
高级的文件系统提供了更重要的功能，其中首要的是崩溃安全。这意味着，如果系统崩溃了，如完全不可控的掉电，文件系统需要确保状态一致，有效。这对于持久存储至关重要。

比如，在写入多个块时，i-node已指向新块但块未写入时，此时系统突然断电，状态就不对了。即便恢复供电了，我们也不知道如何恢复数据，因为之前的上下文信息丢失了。

解决方案是引入日志（Journaling）能力，它记录了所有的信息，确保可以恢复而保证了一致性。
1. 将事务（一组块写操作）先写入日志区，日志不影响文件系统的一致性  
2. 标记提交，这是一致性的分割点 
3. 再将事务应用到主文件系统，这是一致性的后续操作
4. 崩溃后，扫描日志并重放未完成的事务

EXT4文件系统是典型的日志文件系统，它提供了三种日志模式：
- Journal：数据+元数据都先写日志（最安全，最慢）
- Ordered：只记录元数据到日志，数据直接写（默认）
- Writeback：元数据记录日志，数据不保证（最快，风险高）

#### 8.3.4 写时拷贝（CoW，Copy-on-Write）
设备块的覆写，传统的更新模式是原地修改。如果写了一半崩溃，数据不完整，而且也没法恢复到之前（快照）的样子。

写时拷贝在覆写的时候总是写到新的块，然后更新父节点的连接，指向新的块。旧的块依然保留，可以随时用作恢复。  
```
修改前：                修改后：
[Block A] ← [Parent]    [Block A] (旧，只读)
[Block B]               [Block B']
                          ↑
                        [Parent'] (新指针指向B')
```

写时拷贝的优势是显然的：
- 崩溃一致性：指针更新是原子的，系统永远处于一致状态
- 快照：保留旧版本，瞬间创建
- 去重：多文件可以共享相同块

ZFS与Btrfs使用CoW实现了很多高级的文件系统能力，如快照、克隆、压缩、校验和等。

#### 8.3.5 零拷贝（Zero-Copy）
现代操作系统中，内核态和用户态是隔离的。这种隔离带来的一个副作用是，所有的信息，如果需要处理，就必须在内核态和用户态之间复制。比如一个web服务器，如果用户请求一个文件，它需要通过内核把这个文件读出来，然后发送给用户。数据的流程是：
```
#  传统I/O路径上的数据流向
磁盘 -> 内核页面缓存 -> 用户内存 -> 内核Socket缓存 -> 网卡驱动
     |              |          |                |
   DMA复制     内-用CPU复制  用-内CPU复制       DMA复制
```

对于高速的I/O来说，这个CPU复制完全可以省略。

零拷贝技术相当于在隔离之间开了一个后门。数据不需要复制到用户态再复制回去，操作系统直接把数据送到期望的位置：
```
#  零拷贝激活之后I/O路径上的数据流向
磁盘 -> 内核页面缓存 -> 内核Socket缓存 -> 网卡驱动
     |              |                |
   DMA复制      内-内CPU复制        DMA复制
```

耗时的内核态/用户态复制被消除了。只剩下一次简单的内核态内复制（协调不同设备间的数据管理模式）。如果想要彻底去除CPU复制，我们需要硬件的支持，如RDMA（Remote Direct Memory Access）或者 DPDK（Data Plane Development Kit）。这些技术打通了从设备到设备的数据通路，可以不经过CPU直接传送数据。

系统级的零拷贝一般是通过额外的系统调用支持。

#### 8.3.6 虚拟文件系统（VFS）
不同的文件系统有不同的适应场合。FAT系列适合移动存储设备，EXT系列适合普通家用，在Btrfs等在商业存储上独占鳌头。他们都必须提供完全一致的接口才能让不同的应用程序访问。这催生了现代操作系统的一个伟大设计VFS。VFS是文件系统的接口层，通过标准POSIX协议对外提供接口服务。

VFS下面，是各种具体的文件系统，直到底层硬件：
```
应用  
↓  
POSIX API  
↓  
VFS  
↓  
ext4 / xfs / nfs / tmpfs  
↓  
block layer  
↓  
device driver
```

这种接口和实现分离的结构随处可见，是语义和实现隔离的常规设计。 

### 8.4 并发控制
操作系统的核心就是协调CPU的使用。确保多线程同时访问共享数据时，执行流的“交织”不会导致数据结构不一致（竞态条件）。一个典型的错误：

```
线程1: load  counter   (得到 5)
线程2: load  counter   (得到 5)
线程1: add   #1, counter (写回 6)
线程2: add   #1, counter (写回 6)
期望: counter == 7  实际: 6  损失了一次加法
```

#### 8.4.1 互斥与锁
顾名思义，互斥确保对数据的访问是互斥的，就好像用锁锁上。因为一个锁只能被一个执行体持有，这强制了对数据的访问为串行。

互斥锁（Mutex）是最基础的并发控制语义。互斥访问的区段叫做临界区。 
```c
pthread_mutex_lock(&mutex);
// 临界区 (只能一个线程进入)
pthread_mutex_unlock(&mutex);
```

> 对于临界区极短的场景，自旋锁（Spinlock）比mutex更高效。因为Mutex会导致上下文切换，而自旋就是不断循环重试，直到成功。

如果使用多个锁，一种致命的情况是死锁（deadlock）。比如有两个锁A和B，两个执行体执行体分别持有一个等待另一个。两个都无法前进，也永远不会退出。这是操作系统需要极力避免的情况。    

死锁的四个必要条件（Coffman条件）：
1. 互斥
2. 持有并等待
3. 不可抢占
4. 循环等待

避免死锁的常见办法是资源排序，所有执行体按相同顺序请求锁，这就打破了上面死锁的第四个条件。但是操作系统无法强制应用程序的锁顺序，所以应用程序在使用互斥锁的时候需要格外注意。

读写锁是锁的进一步优化，它把锁的意图进一步细化为读和写。这样多个读都可以不受影响（因为读不改变被锁资源）的并发进行，但是但凡有一个写锁，其他所有的都必须等待。

#### 8.4.2 信号量
信号量是用于同步计数的计数器。可以把互斥锁看作一种特殊的信号量，计数最高为1的信号量。

信号量支持两种原子操作：
- `P(sem)`（等待）：若计数器>0，减1；否则阻塞
- `V(sem)`（信号）：计数器加1，唤醒等待线程

```c
semaphore empty = N;   // 空闲缓冲区数量
semaphore full = 0;    // 已填充缓冲区数量

// 生产者
P(empty);
// 放入数据
V(full);

// 消费者
P(full);
// 取出数据
V(empty);
```

#### 8.4.3 无锁结构
仔细想一下锁和信号量表的语义，可以看到这是表达一种原子操作。这个操作不可以被打断，要么成功（拿到锁，完成更新，释放锁），要么等待，永远不会出现更新了一半，被打断，暴露了中间状态的情况。

对于简单的单值更新，CPU提供了直接的指令支持，常见的有CAS（Compare-And-Swap）。其语义类似这样
```c
int compare_and_swap(int* addr, int expected, int new_val) {
    int old = *addr;
    if (old == expected) *addr = new_val;
    return old;  // 返回旧值，调用者可判断是否成功
}
```

但是因为是CPU直接支持，这个相当快。使用示例： 
```c
Node* push(Node* head, Node* new_node) {
    Node* old_head;
    do {
        old_head = head;                       // 保存原有的head
        new_node->next = old_head;             // 新节点指向链表头
    } while (!CAS(&head, old_head, new_node)); // 当链表头和保存的原有的链表头一致（没有其他线程接入）则替换
}
```

无锁结构的优势是无死锁（不涉及锁），无上下文切换开销一般更快，同时高并发下缩放性好。它的一个挑战是ABA问题（指针被A→B→A改回，CAS误认为未变），这个需用双字CAS或标记指针解决。

还有一种原子更新是对整数加一。你可能会疑问，整数加一这种操作怎么会有中间状态，毕竟要么加一完成， 要么还是原值。为什么需要原子语义保证？

问题在于CPU复杂的优化特性。现在CPU往往有三级甚至更多的指令和数据缓存、乱序执行和store buffer，为的就是尽可能让CPU的计算发挥出最高的能力。因此即使是简单的加一操作也可能被拆解为多个阶段（比如更新到了L1缓存，但是还没有同步到L2），使得不同的线程可能看到不同的结果。原子指令通过缓存一致性协议与内存序约束，确保不会出现竞争线程观察到部分更新状态。这个复杂性的研究甚至是一个专门的分支。

#### 8.4.4 RCU（读取-复制-更新）
RCU使用了类似COW的思想，达到了极致读优化的同步。它的核心思想是让读者几乎无开销，写者通过“复制-更新-延迟回收”间接同步。

```c
// RCU模式
ptr = global_ptr;
rcu_read_lock();          // 读者开始临界区（极轻量，通常禁抢占）
local = rcu_dereference(ptr);  // 获取指针
// 使用 local
rcu_read_unlock();

// 写者
new_ptr = kmalloc(sizeof(*new_ptr));  // 分配新的空间
*new_ptr = *global_ptr;               // 复制旧值到新空间 
update(new_ptr);                      // 更新  
rcu_assign_pointer(global_ptr, new_ptr);  // 发布新版本
synchronize_rcu();                    // 等待所有现有读者完成
kfree(old_ptr);                       // 回收旧版本
```

RCU的关键机制：
1. 读几乎无开销
2. 写创建新版本（复制）
3. 发布新指针（原子操作）
4. 等待宽限期（所有读者完成），回收旧版本

非常适用于读多写极少（路由表、文件系统查找）场景。

### 8.5 操作系统的统一视角
我们从资源和结构两种统一视角回顾一下。

#### 8.5.1 资源管理
资源管理需要把资源原子化，根据情况进一步虚拟化，灵活调度使用，使用冲突管理以及灾难恢复。操作系统管理资源时，需要不断在多种策略间根据实际情况做权衡。

| 资源  | 原子化 | 虚拟化    | 调度       | 冲突管理    | 灾难恢复       |
| --- | --- | ------ | -------- | ------- | ---------- |
| CPU | 时间片 | N/A    | CFS、多级队列 | 调度      | N/A        |
| 内存  | 内存页 | 虚拟地址   | LRU、时钟   | 锁（分配器内） | ECC内存、后备电池 |
| 磁盘  | 扇区  | 文件     | I/O调度    | VFS锁    | 日志、CoW     |
| 网络  | N/A | socket | 包调度      | 协议栈锁    | N/A        |

在关键业务场景下，典型的如商业环境中，内存的错误需要专门的处理。ECC（Error Correcting Code）内存的核心工作就是检测并自动修正内存中的比特翻转错误（错误来源有环境因素，硬件磨损等），这也是它成为服务器标配的关键原因。但是整体掉电，ECC就没有办法了。商业存储系统一般提供后备电池，确保掉电发生时，所有的内存信息可以被转存到持久存储设备上，恢复供电后再加载回来，确保数据的安全性。

#### 8.5.2 结构模式
操作系统是最复杂的软件系统之一，使用了多种结构来应对复杂性。以下是常见的几种：
1. 虚拟逻辑层（虚拟内存，VFS）。使用虚拟层隔离接口的简单和实现的复杂
2. 分层（IO栈，网络栈等）。通过协议分层不断抽象资源使用，每一次提供不同的抽象能力
3. 缓存（TLB、页缓存）。利用小而快的结构加速大而慢结构，局部性良好的情况下，可以达到10倍甚至更高的加速比
4. 事务（日志）。使用顺序日志 + 异步应用提供事务和回滚的能力
5. 副本（RCU、CoW）。优化存储，读时共享，写时复制

这些模式，不断在不同层次，不同子系统中复现。这些模式不仅仅在操作系统中，在更广泛的场景都有大量的运用。

业内有句行话说，所有的问题都可以引入一个中间层解决。这其实是强调了抽象巨大能力，虚拟内存就是典型代表。

与此还有另一句话， 所有的性能问题都可以通过去掉几个中间层解决。零拷贝就是这句话的完美注解。内核态和用户态的抽象隔离带来的性能问题，通过零拷贝技术的打洞消除了。

这是一个重要的结构设计提示。抽象赋予强大的能力，而合适地打破抽象则是性能的关键。

### 8.6 资源隔离谱系
资源的共享的背面就是资源的隔离。从隔离的角度看，我们可以更全面的看待操作系统的资源管理能力。

#### 8.6.1 谱系总览
以下隔离谱系展示了操作系统如何通过隔离不同的资源达到各种不同程度的抽象。每一种抽象都对应了一个现实的使用场景。这是安全和效能的平衡。 
```
隔离程度低 ───────────────────────────────────────────────→ 隔离程度高

线程隔离      进程隔离      chroot/      容器         虚拟机
(Thread)      (Process)     (jail)      (Docker)      (KVM)
    │             │            │            │            │
    │             │            │            │            │
    ▼             ▼            ▼            ▼            ▼
  同一进程空间   同一OS       同一OS        同一OS内核    独立内核
  共享几乎所有   共享内核      共享内核      共享内核      独立内核
                 共享文件系统  共享文件系统  共享文件系统  独立虚拟硬件
                              (受限视图)    (独立视图)
```

#### 8.6.2 详细对比

| 维度        | 线程     | 进程        | chroot/jail | Docker容器    | KVM虚拟机 |
| --------- | ------ | --------- | ----------- | ----------- | ------ |
| **虚拟化层**  | 无      | 无（内核隔离）   | 文件系统视图      | 内核命名空间      | 完整硬件   |
| **内核**    | 共享     | 共享        | 共享          | 共享          | **独立** |
| **地址空间**  | **共享** | **独立**    | 独立          | 独立          | 独立     |
| **文件系统**  | 共享     | 共享        | **受限视图**    | **独立视图**    | 独立镜像   |
| **进程列表**  | 可见     | 可见        | 可见          | **隔离**      | 独立     |
| **网络栈**   | 共享     | 共享        | 共享          | **独立**      | **独立** |
| **用户/权限** | 共享     | 共享        | 共享（视图受限）    | 独立（user ns） | 独立     |
| **资源限制**  | 无原生支持  | 部分（nice等） | 无原生支持       | cgroups     | 硬件辅助   |
| **隔离开销**  | 极低     | 低         | 低           | 低-中         | **高**  |
| **启动时间**  | 微秒级    | 毫秒级       | 毫秒级         | 毫秒级         | 秒级     |
| **安全级别**  | 极低     | 低         | 中           | 中-高         | **极高** |
| **典型场景**  | 并行计算   | 多任务       | 简单沙箱        | 微服务部署       | 多租户、云  |

##### 线程隔离
这是最弱，共享最多的隔离，也是最高效，安全性最差的隔离。
* 共享部分：地址空间（代码、堆、全局变量）、文件描述符、信号处理器、当前工作目录、用户ID、进程ID
* 隔离部分：栈、寄存器上下文、程序计数器、线程局部存储（TLS）、信号掩码

线程是“进程内的执行流”，是共享一切的独立执行体。一个线程的崩溃（如段错误）会杀死整个进程。

```text
进程地址空间
+----------------+
| 代码段         |
| 数据段         |
| 堆             |
+----------------+
| 线程1栈 | 线程2栈 | 线程3栈 |
+----------------+
```

##### 进程隔离
这是操作系统的最基础隔离模型，做到了执行体的安全，一个进程崩溃不会影响其他进程。
* 共享部分：内核、文件系统（全局挂载空间）、进程间通信（IPC）机制、网络栈（同一主机）
* 隔离部分：独立的地址空间、独立的进程ID、独立的寄存器上下文

进程是资源分配的基本单位。关键机制是虚拟内存 + 页表基础上的地址空间隔离。

##### chroot/jail
chroot/jail是进程隔离的基础上增加了文件系统隔离。 
* 共享部分：内核、进程列表（同一主机上可见其他进程）、网络栈、IPC、设备文件（如果挂载）
* 隔离部分：**文件系统根目录视图**（限制进程只能看到子目录树）
```bash
chroot /new/root /bin/bash
# 进程认为 /new/root 就是系统的根 /
# 无法看到 /new/root 之外的任何文件
```

这是最早的“轻量隔离”，只隔离了文件系统的命名空间，限制了文件系统的访问权限。不过root用户可以轻松逃逸，进程仍可见其他进程（可发送信号、ptrace调试）等。

##### Docker容器
Docker隔离是命名空间隔离 + 资源限制，基于内核的命名空间实现隔离，加上cgroups机制实现资源限制。

* 共享部分：内核（同一OS内核，不能运行不同内核版本）
* 隔离部分：

| 命名空间       | 隔离内容                    | 命令标志              |
| ---------- | ----------------------- | ----------------- |
| PID命名空间    | 进程ID（容器内进程PID从1开始）      | `CLONE_NEWPID`    |
| NET命名空间    | 网络栈（独立网卡、路由表、iptables）  | `CLONE_NEWNET`    |
| MNT命名空间    | 挂载点（独立文件系统视图）           | `CLONE_NEWNS`     |
| UTS命名空间    | 主机名和域名                  | `CLONE_NEWUTS`    |
| IPC命名空间    | 共享内存、消息队列               | `CLONE_NEWIPC`    |
| USER命名空间   | 用户ID（容器内root可映射为主机普通用户） | `CLONE_NEWUSER`   |
| CGROUP命名空间 | cgroups视图               | `CLONE_NEWCGROUP` |

* 资源限制（cgroups）：
```bash
# 限制单个容器最多使用1个CPU核心、512MB内存
docker run --cpus=1 --memory=512m ...
```

Docker提供了足够多的隔离和资源限制，但是又非常轻量级，提供了更高的安全保证，目前使用非常流行。

##### 虚拟机
虚拟机，如KVM，是最彻底，最严格的隔离，也是仅次于完全物理隔离的最安全的隔离机制。

* 共享部分：
	- 物理硬件（通过VMM调度）
	- 宿主机内核的KVM模块（用于虚拟化支持，但Guest不直接调用）
* 隔离部分：
	- 独立内核。Guest可以运行不同版本甚至不同类型的OS（Linux、Windows、FreeBSD）
	- 虚拟硬件。CPU（vCPU）、内存（Ept/NPT）、磁盘（qcow2）、网卡（virtio）
	- 硬件辅助的嵌套页表（EPT/NPT）完全地址空间隔离

涉及到的核心技术有：
- CPU虚拟化（Intel VT-x / AMD SVM）：敏感指令自动陷入VMM
- 内存虚拟化（EPT/NPT）：Guest物理地址 → 主机物理地址的两层转换
- I/O虚拟化：virtio半虚拟化驱动减少VM Exit开销

典型的KVM分层架构如下：
```
+------------------+  +------------------+
|  Guest OS (Linux) |  | Guest OS (Win)   |
|  Kernel 5.10      |  | Kernel 10.0      |
+------------------+  +------------------+
|  QEMU (设备模型)    |  | QEMU |
+------------------+  +------------------+
|         KVM内核模块 ( /dev/kvm )        |
+-----------------------------------------+
|            Linux宿主机内核              |
+-----------------------------------------+
|               硬件                      |
+-----------------------------------------+
```

#### 8.6.3 安全边界与逃逸风险
隔离的核心是安全。这里列出的安全对比。 

| 技术     | 攻击者突破后能做什么                   | 逃逸风险                             |
| ------ | ---------------------------- | -------------------------------- |
| 线程     | 塌缩整个进程                       | -                                |
| 进程     | 访问同一内核/文件系统                  | 内核对进程隔离依赖强，历史bug多（脏管道、Dirty Cow） |
| chroot | 普遍可逃逸（`chdir("..")` + 挂载点发现） | 极高，不视为安全隔离                       |
| Docker | 可访问宿主机内核（需内核exploit），可影响其他容器 | 中-高（依赖seccomp+capabilities裁剪）    |
| KVM    | 需突破硬件虚拟化（极难，历史bug极稀缺）        | 极低（云厂商依赖）                        |

观察这个谱系，看以看到隔离程度与资源开销、灵活性成反比。隔离越彻底，共享越少，抽象层越厚，性能损失越大。 不同的安全需求决定了隔离的程度。

### 8.7 执行隔离 
资源隔离是效能和安全的平衡，那么执行隔离则完全为安全而生。它控制的是一段代码到底能在多大程度上被安全地限制在它应该活动的范围内。

执行隔离的根本目标是最小权限原则（Principle of Least Privilege）。代码只能访问它真正需要的东西，即使出现漏洞，也难以造成严重破坏。这是一条从硬件到语言的完整纵深防御谱系。

| 隔离层次       | 隔离载体        | 核心控制点           | 安全强度  | 性能开销 | 典型技术与场景                         |
| ---------- | ----------- | --------------- | ----- | ---- | ------------------------------- |
| **硬件级**    | CPU 保护环     | 指令集操作权限（特权指令）   | ★★★★★ | 极低   | x86 Rings、ARM EL                |
| **固件/微码级** | TEE（可信执行环境） | 内存加密 + 远程证明     | ★★★★★ | 中    | Intel SGX、ARM TrustZone、AMD SEV |
| **内核逻辑级**  | eBPF 虚拟机    | 内核验证器静态检查       | ★★★★☆ | 低    | Linux eBPF（网络、观测、安全）            |
| **应用逻辑级**  | Wasm 虚拟机    | 沙箱虚拟机 + 能力控制    | ★★★★  | 低-中  | WebAssembly、Wasmtime、WasmEdge   |
| **应用构造级**  | 类型系统与所有权    | 编译期静态检查 + 运行时边界 | ★★★☆  | 极低   | Rust、Java（字节码验证）、Go、TypeScript  |

#### 8.7.1 硬件级执行隔离
CPU Rings保护是最基础的执行隔离。现代 CPU 从诞生之初就内置了执行隔离能力。以 x86 架构为例，CPU 设计了4 个保护环（Ring 0~3）：
- Ring 0，特权模式。可执行所有指令（包括 I/O、中断屏蔽、页表修改等特权指令），操作系统内核就运行在这个模式
- Ring 3：普通模式。禁止直接访问硬件资源，用户程序就运行在这个模式 。

> Ring 1 和 Ring 2历史上曾计划用于设备驱动或 Guest OS，但在当代主流系统中几乎不被使用。现代虚拟机控制程序Hypervisor（KVM、Hyper-V、Xen 等）运行在VMX Root Mode（Ring -1）模式下，是由 Intel VT-x 和 AMD-V 引入的超特权模式，它比 Ring 0 权限更高，负责对多个 Guest 操作系统的进行强隔离。

这种结构设计通过硬件强制实现。当 Ring 3 代码尝试执行特权指令时，CPU 直接产生通用保护异常（#GP），“陷入”到内核，交由操作系统处理。

这一层是所有上层隔离的物理底座，性能损失几乎为零，但一旦内核（Ring 0）被攻破，整个系统就失去了信任根。

#### 8.7.2 固件可信防护
当操作系统不受管控时，我们就需要TEE（Trusted Execution Environment）。这是硬件可以向远程服务器的证明，证明运行的代码是期望的，签名正确，未被篡改。这意味着你可以通过这个确保硬件执行的是你期望的操作系统，而不是被恶意修改的版本。

恰当地配置TEE，你的代码甚至可以安全地运行在默认不安全的环境中，只要这个环境的硬件提供TEE支持，如典型的公有云。云服务上，管理员即便可以直接访问硬件，也很难拿到你的代码的机密信息。你还可以通过TEE的机制向你的客户证明你的代码是可靠的，客户也就可以提交机密信息到云上。       

TEE的核心是硬件加密保护。这个硬件的密钥是在芯片制造时烧录，即使是CPU厂商也无法读取明文密钥。

#### 8.7.3 内核安全保护
eBPF 是 Linux 内核近十年最重要的一项创新。它允许用户态程序向内核注入经过严格验证的代码，在内核关键路径上安全执行。

其安全核心是 eBPF Verifier（验证器）：
- 静态检查所有可达路径，通过有界循环验证确保程序终止
- 确保不会访问非法内存、不形成死循环、不越权访问内核数据结构
- 限制指令数量和程序复杂度

eBPF 让用户可以在不修改内核、不加载内核模块的情况下，实现高性能网络过滤、系统调用追踪、容器安全策略、观测（Observability）等功能，被誉为“内核里的安全沙箱”。

#### 8.7.4 应用沙箱
Wasm 是为 Web 而生的沙箱虚拟机，现在已走出浏览器，成为通用的高安全执行环境。其隔离特性包括：
- 线性内存模型（只能通过偏移访问固定大小的内存）
- 基于栈的虚拟机架构
- 能力导向的安全模型（Capability-based Security）
- 与宿主环境通过明确导入/导出函数交互

Wasm 在进程内提供了细胞级隔离。即使 Wasm 模块被完全攻破，也极难逃逸到宿主进程。这使得它特别适合插件系统、边缘计算、Serverless 函数、多租户 SaaS等场景。

#### 8.7.5 构造保护
最高层的隔离发生在代码被编写，应用被构造之时：
- 语言级。通过强类型系统，所有权要求，生命周期要求等，语言层面强化安全
- 编译时。静态分析，安全分析等识别潜在安全问题，如缓冲区溢出 
- 运行时。字节码验证器，GC，受限指针等，显著降低传统 C 常见漏洞

这一层隔离的成本最低，强度也最弱。它依赖开发者正确使用语言特性，无法阻止运行时被绕过的逻辑漏洞。

安全在现代社会越来越重要，然而真正的安全从来不是单一层次的，强调的是纵深防御（Defense in Depth）。优秀的系统会在每一层都施加适当的隔离，即使某一层被突破，其他层仍能提供保护。

资源隔离回答的是“不同任务之间如何互不干扰”，执行隔离回答的是“同一任务内的不同代码段，如何被限制在其应有的权限内”。前者是横向隔离，后者是纵向隔离。二者共同构成了操作系统的完整安全基座。

### 8.8 本章小结
我们从进程与线程（执行的容器）开始，看到操作系统如何让多个程序共享有限CPU，通过调度算法公平分配资源。虚拟内存构建了从虚拟到物理的映射层，让每个进程拥有独立的地址空间，通过页表+TLB实现高效转换。文件系统在磁盘上组织持久数据，i-node提供了高效的随机访问，日志和写时拷贝保证了崩溃一致性。并发控制机制（锁、信号量、无锁结构、RCU）管理着多线程访问共享数据的风险。

操作系统的一个核心启示是根据待管理的资源 选择合适的结构设计，核心是资源的充分，可靠，高效的使用，其中隔离是关键。页表是多级索引结构，调度队列是红黑树，文件系统是B树+i-node，并发控制是锁协议图。所有结构都服务于同一个目标，如何在有限的物理资源上，创造无限的、可靠的、高效的抽象。

执行隔离则完全是安全性的要求。我们总是容易低估安全的必要性，除了安全事故才匆忙的亡羊补牢。安全的结构设计应该是系统设计的必修课。
