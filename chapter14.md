## 第十四章 计算机体系结构：转换的物理基础
所有软件最终都必须在物理硬件上执行。体系结构是物理结构与逻辑结构之间的接口，它向上承载着操作系统和应用程序，向下控制着晶体管和电信号。体系结构为转换提供了物理基础，为效能提供了现实对应。

体系结构是宏观结构，是看得见，摸得着的硬结构。我们一直强调，转换的性能来自于结构。从体系结构的历史可以清晰地看到，不断演进的结构是如何支持转换性能的持续提升，这个方向没有终点。

计算机体系结构是在物理约束（功耗、面积、延迟）下，寻求更高性能、更低功耗的结构的工程艺术。

### 14.1 冯·诺依曼结构
在计算机发展的早期，程序都是“硬连线”的，继电器、开关、布线，一个程序就是一种硬件的连接配置。重新编程意味着重新完成硬件布线，不仅繁杂，更重要的是完全不通用。

1945年，冯·诺依曼在EDVAC的技术报告中描绘了现代计算机的蓝图，其中提出了存储程序的概念。一般认为，埃克特和莫奇利作为EDVAC的设计者，也做出了核心贡献。

#### 14.1.1 存储程序概念
储存程序的核心思想是把程序和数据存储在相同的读写内存中。初看起来，程序和数据截然不同。然而图灵早在1936年就提出通用图灵机的设想，把图灵机编码为符号，然后由另一台图灵机模拟。 冯·诺依曼更进一步，给出通用计算的实现方法，彻底使得计算通用化。程序从此可以作为数据加载。

冯·诺依曼结构有五个组件：
```
+------------------+
|   控制单元(CU)    | ← 取指、译码、生成控制信号
+------------------+
         ↓
+------------------+
|   算术逻辑单元    | ← 执行运算
|      (ALU)       |
+------------------+
         ↑ ↓
+------------------+     +------------------+
|     寄存器组      | ←→ |     内存          |
|  (Register File) |     |   (Memory)       |
+------------------+     +------------------+
         ↑
+------------------+
|     输入/输出     |
+------------------+
```

其核心是“取指-译码-执行-写回”循环：
```
while (true) {
    instruction = memory[PC++];    // 取指 (Fetch)
    decoded = decode(instruction); // 译码 (Decode)
    operands = read_regs(decoded); // 读操作数 (Read)
    result = ALU.execute(decoded, operands); // 执行 (Execute)
    write_back(result);            // 写回 (Write Back)
    update_PC();                   // 更新PC
}
```
多半个世纪过去了，现代计算机基本都是这个模型，从未改变。

#### 14.1.2 冯·诺依曼瓶颈
仔细观察，CPU要执行程序，指令和数据都需要从内存中读取，执行结果也需要写回内存。如果读写速度跟不上CPU的速度，数据传输通道就成为系统性能的核心限制，现在叫做“内存墙”问题。

```
        CPU                           内存
    +--------+                    +--------+
    | 高速   | ←—— 系统总线 ——→    | 相对慢 |
    | 运算   |   (带宽有限)        | 存储   |
    +--------+                    +--------+

CPU以GHz频率运行，内存访问延迟~50-100ns → CPU可能因等待内存而空闲数百个周期。
```

人们设计了多种缓解措施来降低这个问题的影响：
- 缓存（Cache）。将近期访问数据保存在CPU附近。Cache能够工作的根本原因，是绝大多数程序天然具有局部性。如果程序随机访问整个内存，缓存几乎不会带来任何收益。缓存友好的程序执行效能能有数量级提升，这是现代程序优化的核心手段之一
- 分支预测。猜测并执行下一条指令，减少取指停顿。如果预测失败，流水线中已经执行的大量指令都会被丢弃并重新开始
- 多线程执行。调度多个执行队列，这个等待数据返回的过程中立即切换另一个执行

硬件层面，不断提升制造技术。如先进封装技术（如 TSMC CoWoS、3D IC Stack）以及高带宽内存等。

这些手段并不能消除冯·诺依曼瓶颈。还有一些替代性的探索：如指令和数据分离的哈佛架构、近内存计算（Processing In Memory）、非冯结构（数据流、归约机）等，实际用的还很少。

### 14.2 指令集架构（ISA）
CPU执行的是指令，ISA定义了CPU可以支持的指令。不仅如此，它还定义了所有支持运行程序的一系列IO和管理接口，是软件（编译器/操作系统）和硬件（处理器）之间的核心基础定义，是一个计算平台的基础规范。

#### 14.2.1 ISA的核心要素
指令集的核心要素有：

| 要素    | 描述          | 例子                              |
| ----- | ----------- | ------------------------------- |
| 指令集   | 支持的指令类型     | ADD, LOAD, STORE, BRANCH        |
| 寄存器   | 程序员可见的寄存器   | x86: RAX, RBX, ...; ARM: R0-R15 |
| 寻址模式  | 如何计算操作数地址   | 立即、直接、间接、基址偏移                   |
| 数据类型  | 支持的数值类型     | 字节、半字、字、浮点                      |
| 异常/中断 | 处理器如何响应异常事件 | 缺页、除零、外部中断                      |
| 内存模型  | 多核内存访问顺序    | x86-TSO, ARM/POWER弱内存模型         |
ISA是每个CPU独有的。流行的有x86/x64指令集，ARM多个版本的指令集等。

#### 14.2.2 复杂指令集（CISC）和精简指令集（RISC）
传统的x86一般被视为CISC的代表，特点是指令丰富，语义复杂，灵活多变。当CPU主频越来越快的时候，针对CISC的优化就非常困难了。这时候，RISC作为一个替代异军突起。它们的主要差异体现如下：

| 维度   | RISC (精简指令集)      | CISC (复杂指令集)        |
| ---- | ----------------- | ------------------- |
| 指令数量 | 少 (~50-100条)      | 多 (~200-500条)       |
| 指令长度 | 固定 (32位)          | 变长 (1-15字节)         |
| 寻址模式 | 少 (通常2-3种)        | 多 (10+种)            |
| 内存访问 | 只有LOAD/STORE      | 许多指令可访问内存           |
| 译码   | 简单 (直接控制信号)       | 复杂 (微码)             |
| 典型代表 | ARM, RISC-V, MIPS | x86, z/Architecture |

CISC的思想是提供高层次指令，减少指令数量，节省内存。例如：x86的`ADD`指令可以直接将内存中的数据与寄存器相加，并将结果写回内存，一条指令隐式完成了Load、Add、Store三步。

RISC的思想是简单指令快速执行，复杂操作编译器合成。例如：`x = y + z` 在RISC中编译为：
```
LOAD  R1, addr_y   // 内存→寄存器
LOAD  R2, addr_z
ADD   R3, R1, R2
STORE R3, addr_x
```

现代CPU往往不再局限于特定的设计哲学，取长补短，兼容并蓄：
- CISC内部将复杂指令翻译为RISC风格的微操作（微码）
- RISC引入更丰富的指令（如ARM的`LDP/STP`多数据加载）

CPU的架构往往真正的区别在微架构如何实现。

#### 14.2.3 微码
x86自1978年开始，指令集不断扩展（从16位到32位到64位，从MMX到SSE到AVX）。其向后兼容性强制保留了几十年前的指令。背后的秘密就是复杂的译码器以及微码机制。
```
x86指令 (变长，复杂)
    ↓ (微码ROM/解码器)
微操作 (固定长度的RISC类操作)
    ↓ (执行单元)
完成
```

微码是处理器内部的“解释器”，负责将CISC翻译为RISC。这层抽象让Intel能够保持x86软件兼容性，同时内部微架构不断更新。

#### 14.2.4 开放ISA
RISC-V始于2010年UC Berkeley，目标是提供一个简单、开放、模块化的ISA。其设计哲学体现了纯粹的RISC风味：
- 基础整数指令集非常保守，仅40余条。RV32I, RV64I
- 标准扩展相对开放。M（乘除）、F/D（浮点）、A（原子操作）、C（压缩指令）、V（向量）
- 用户特权模式，使得轻易裁剪（嵌入式到超算）成为可能

RISC-V的更大意义在于：
- 无专利/授权费（可自由实现）
- 模块化（按需选择扩展）
- 简单（利于教学和研究）
- 正在挑战ARM（嵌入式）和x86（服务器）的垄断。

现代移动计算使用的 CPU 几乎被 ARM 垄断。而 RISC-V 正在物联网（IoT）、嵌入式领域异军突起，并开始向移动和服务器领域发起挑战。

### 14.3 并行与加速
单核性能增长在2005年左右趋于平缓（功耗墙、暗硅）。此后性能提升主要来自并行执行。

#### 14.3.1 指令级并行（ILP）
指令级并行的目的是让CPU在单线程内同时执行多条指令。

指令级并行是所有并行形式中最隐蔽、也最基础的一种。它不需要程序员做任何特殊处理，完全由硬件在后台自动完成。你在写 `a = b + c; d = e + f;` 时，这两条加法在硬件上很可能就同时执行。

怎么做到的？三个层面的技术叠加
- 流水线让指令重叠执行
- 超标量让多条指令同时发射
- 乱序执行让指令不受书写顺序束缚。

#####  14.3.1.1 流水线
一条指令的执行可以分为多个阶段。经典的五阶段划分是：
```
取指(IF) → 译码(ID) → 执行(EX) → 访存(MEM) → 写回(WB)
```

如果不做任何优化，CPU一条一条地处理指令：第一条走完5个周期，第二条才开始，第三条再等……平均每条指令5个周期。

流水线的想法很朴素。既然每个阶段对应的硬件单元是独立的，为什么不让它们同时工作？

就像工厂流水线：第一辆车的底盘正在焊接时，第二辆车已经开始组装发动机了。理想情况下，流水线填满后，每个周期都能完成一条指令，吞吐量提升5倍。

然而指令之间不是独立的，它们会互相干扰。这就导致流水线的冲突（Hazard）问题。分为以下几类：
- 结构冲突。两个阶段争抢同一个硬件资源。比如指令和数据放在同一块缓存里，取指和访存同时发生时就冲突了。解决方案通常是分开的指令缓存和数据缓存（哈佛架构的体现）。
- 数据冲突。后一条指令需要前一条指令的结果。`add` 的结果还没算出来，下一条 `mul` 就要用。解决方式分两种：
  - 转发（Forwarding）。结果一算出来就直接传给需要的指令，不等写回寄存器。这是硬件级的短路，大多数数据依赖都能这样解决。
  - 流水线停顿（Stall）。实在等不到的就暂停发射，插入空泡（bubble）。这是最后的手段，会损失吞吐量。
- 控制冲突。分支指令会改变PC（程序计数器）。CPU在取指时不知道分支往哪走，猜错了就要丢弃已经进入流水线的指令。分支预测就是为了解决这个问题，后面会讲到。

流水线是有极限的。把5级拆成10级，理论上吞吐量更高，但冒险的开销也随之增加，而且物理上不可能无限细分。现代CPU的流水线深度通常在14-20级。

#####  14.3.1.2 超标量
流水线让指令在时间上重叠。超标量（Superscalar）做的更激进。在一个周期内，同时发射多条指令。
```
        周期1    周期2    周期3
发射宽度=1:  [A]     [B]     [C]      ← 标量流水线
发射宽度=3:  [A][B][C] [D][E][F]     ← 超标量
```

现代CPU的发射宽度通常是4-8条。这意味着CPU需要：
- 每个周期从指令缓存中抓取多条指令
- 配备多个译码器、多个ALU、多个访存单元
- 判断哪些指令可以同时发射（没有数据依赖）

超标量的限制在于，程序中的指令级并行度是有限的。依赖链（dependency chain）上的指令必须顺序执行，发射宽度再宽也无济于事。

#####  14.3.1.3 乱序执行
乱序执行（Out-of-Order Execution, OoO）是ILP的集大成者。它解决的核心问题是：程序是按顺序写的，但硬件可以不按顺序执行，只要保持程序可观察行为一致。

考虑这段代码：
```
LOAD  R1, [addr_a]   // 从内存读a，可能缓存缺失，需要200个周期
ADD   R2, R1, 1      // 依赖R1，必须等
MUL   R3, R4, R5     // 不依赖任何前面的指令，现在就能算
```

如果严格按顺序执行，第三条乘法要等200个周期。乱序执行的做法是：先执行能执行的，不阻塞在后面等待的指令上。

硬件实现的关键结构有三个：

**寄存器重命名（Register Renaming）** 解决的是“假依赖”问题。看这段代码：
```
ADD  R1, R2, R3    // 结果写入R1
SUB  R4, R1, R5    // 依赖上面真正的R1
MUL  R1, R6, R7    // 又写入R1
ADD  R8, R1, R9    // 依赖上面真正的R1
```

这里的R1被反复使用，MUL和ADD之间有真依赖，但第二条SUB和第四条ADD之间没有任何关系，它们只是恰好用了同一个寄存器名。寄存器重命名会把第三条MUL的结果写到一个新的物理寄存器上（比如P35），第四条ADD读取这个P35。这样两条原本被“假依赖”隔开的指令就可以并行执行了。“假依赖”的推导依赖于对程序的数据流做分析。

保留站（Reservation Station） 是乱序发射的调度器。所有被译码的指令进入保留站，等待操作数就绪。操作数一准备好，指令就被发射到对应的执行单元。操作数还没准备好的（比如正在等内存数据），就留在保留站里等。这个过程叫乱序发射（Out-of-Order Issue） 。

重排序缓冲区（ROB, Reorder Buffer） 解决的是乱序执行如何保证结果正确的问题。指令乱序执行、乱序完成后，ROB按程序顺序将它们提交（Commit）写回寄存器或内存。这样做有两个原因：
- 精确异常：如果第10条指令触发了缺页异常，CPU需要保证第11条及之后的指令没有产生任何可见效果（尽管它们可能已经执行完了）。ROB会丢弃这些未提交的结果，CPU的状态回退到第10条指令之前。
- 按顺序退出。程序的行为在外部看来必须和顺序执行完全一致。
```
       顺序取指
            ↓
       寄存器重命名  ← 消除假依赖
            ↓
       保留站（发射） ← 操作数就绪即发射（乱序）
            ↓
       执行单元并行执行
            ↓
       重排序缓冲区  ← 按序提交，保证精确异常
            ↓
       顺序提交
```

##### 14.3.1.4 ILP的局限
指令级并行看起来很美，但它有三道不可逾越的天花板：
1. 程序本身的并行度有限。 大量指令之间存在真依赖链，必须串行执行。学术界用IPC（Instructions Per Cycle，每周期指令数）来衡量ILP的实际效果。理想情况下超标量+乱序可以做到IPC>1，但实际上绝大多数程序的IPC在1.0-2.5之间，偶尔超过3.0。花巨大硬件代价换来的提升，边际收益递减。
2. 功耗与复杂度不划算。 乱序执行需要庞大的重命名逻辑、保留站、ROB，以及复杂的依赖检测电路。这些硬件本身消耗大量功耗和芯片面积，而收益却在递减。这是为什么苹果的M系列和ARM的Cortex-X系列在功耗受限的移动端开始限制乱序窗口大小的原因之一。
3. 内存延迟是硬约束。 流水线、超标量、乱序都解决不了一个问题，当缓存缺失、需要从DRAM读数据时，CPU要等待数百个周期。 在这个时间尺度上，再大的保留站和ROB也填不满。这就是为什么ILP的进一步突破必须依赖存储层次的优化，以及多线程和异构计算的转向。

现代高性能CPU（Intel Core、AMD Zen、Apple M系列）都是“超标量+乱序+深度流水线”的复合体。但从2005年左右开始，ILP的收益已经微薄。体系结构的重心转向了多核和异构。

#### 14.3.2 SIMD（单指令多数据流）
基础思想是让一条指令同时对多个数据元素执行相同操作。这在现代图像/音频处理、矩阵乘法、科学计算、AI推理方面是常态。
```
传统标量：
ADD R1, R2   // 1个加法

SIMD (128位向量)：
ADD V1, V2   // 4个32位浮点加法
```

各平台的支持如下：

| ISA    | 向量扩展                 | 向量宽度        |
| ------ | -------------------- | ----------- |
| x86    | SSE (1999) → AVX-512 | 128 → 512 位 |
| ARM    | NEON → SVE           | 128 → 可变    |
| RISC-V | V扩展                  | 可变          |

#### 14.3.3 多线程（SMT / 超线程）
多线程的基础思想是多硬件并行。一个物理核，用两个（或更多）硬件线程上下文共享执行单元。当一个线程等待（如cache miss），另一个线程利用执行单元。

SMT的主要限制是共享资源（L1/L2、执行单元）的干扰，做不到线性加速。

硬件并行的架构对比如下：
- 单核单线程：1个上下文
- SMT：2个上下文，共享执行单元（+5-30%性能）
- 多核：独立核心，线性加速（理想情况下）

#### 14.3.4 多核与缓存一致性
每一个核都有自己的缓存，而缓存又是用户不可见的。为了维持用户可见行为的一致，硬件必须保证缓存一致。

一个典型的多核结构示例如下
```
+------------+   +------------+
|  Core 0    |   |  Core 1    |
| L1I L1D    |   | L1I L1D    |
|   L2       |   |   L2       |
+------------+   +------------+
      ↓               ↓
+--------------------------+
|    共享 L3 缓存           |
+--------------------------+
      ↓
+--------------------------+
|        内存控制器         |
+--------------------------+
```

当跨核访问同一个变量的时候，问题就来了
```
Core0: cache[x] = 5
Core1: cache[x] = ?   (如何知道自己的缓存已经失效)
```
硬件必须明确地说明，core1缓存的x是否还有效。这里，因为core0更新了x的值，所以我们期望core1需要知道这一点，并且在需要的时候可以从内存中读到更新的值。如何做到这一点？我们需要MESI协议来同步缓存状态。

MESI协议支持4种状态。通过这些状态，一个core就可以确保清楚自己维护的缓存是否失效，并且可以请求已经更新的core写入内容，然后从内存读取而拿到最新的值。

> 在多执行线程的情况下，仅仅MESI是不够的。还需要锁和内存屏障等应用级的控制维持多线程下的语义正确，具体可参见第十一章。

MESI通过总线嗅探（cores监听总线）或目录协议（多核/大型系统）在硬件级别维护一致性。

### 14.4 异构计算
通用CPU为通用设计，在特定任务（矩阵乘、密码、图像处理）上效率往往达不到要求。专用的硬件加速器是自然的解决方案。这就是异构计算。

#### 14.4.1 用GPU做大规模并行计算
GPU是最常见的，用于图形处理加速的专用硬件，现在广泛应用于AI训练和推理。GPU和CPU的的架构区别有：

|       | CPU核心           | GPU核心      |
| ----- | --------------- | ---------- |
| 数量    | 4-64            | 数千         |
| 每核心能力 | 强（乱序、分支预测）      | 弱（顺序、简单）   |
| 缓存    | 大（复杂 hierarchy） | 小（软件管理）    |
| 寄存器   | 少               | 极多（上下文切换）  |
| 适合    | 串行、复杂控制流        | 数据并行、简单控制流 |

英伟达公司的CUDA编程模型是使用最广泛的GPU线程模型：
```
Grid (整个任务)
  └── Block (共享内存组)
        └── Warp (32线程，硬件调度)
              └── Thread (单个SIMT线程)

硬件执行：Warp是调度单位（32线程）
同一Warp执行相同指令（若分支，掩码）
```

GPU有自己的共享内存，这个和系统的全局内存构成了内存层次：
- 全局内存（DRAM，带宽高但延迟高）
- 共享内存（片上，手动管理）
- 寄存器（每条线程私有）

#### 14.4.2 FPGA
无论CPU还是GPU，一旦投产，其功能就不再变化。FPGA通过可编程的计算架构提供了动态的计算能力。FPGA的结构如下：
```
+-----------------------------------+
| 可配置逻辑块 (CLB) × 大量          |
|   ↓                               |
| 可编程互联 (连接CLB)               |
|   ↓                               |
| 块RAM (BRAM)                      |
|   ↓                               |
| DSP切片 (乘法器等)                 |
+-----------------------------------+

CLB内部: LUT (查找表) + 触发器
```
通过硬件描述语言（Verilog/VHDL）就可以控制配置FPGA的功能，编程出适应特定领域和需求的高效计算单元。

FPGA的优势有：
- 完全并行（无冯·诺依曼瓶颈）
- 可重配置（更新硬件功能无需更换芯片）
- 延迟确定（硬件管道）
- 极高能效（比GPU高10-100x）

相对劣势有：
- 编程难（硬件思维+时序约束）
- 峰值频率低（相比ASIC）
- 成本（单位逻辑成本高于ASIC）

在网络加速（智能网卡）、信号处理、原型验证、高频交易等特定领域，FPGA大行其道。

#### 14.4.3 专用集成电路ASIC
如果FPGA追求的是灵活多变，ASIC追求的则是极致。仅仅为单一算法/应用设计的固定硬件。
```
算法 → RTL → 验证 → 物理设计 → 流片 → 制造
```

其优势是显然的：
- 极高能效（每瓦性能最优）
- 极高密度（无冗余逻辑）
- 适合大规模生产

劣势也是显然的：
- 初始成本极高（数百万美元掩膜费）
- 无法修改（错误无法修复）
- 上市时间长（6-12个月）

实践中，挖矿，AI推理加速，网络包处理等专门的领域，都有专门的ASIC支持。

#### 14.4.4 领域专用架构（DSA）
把通用的CPU和专用的计算单元有机结合，就进入领域专用架构的范围。

> DSA =  通用 CPU + 加速器（异构）

典型的DSA异构系统结构如下：
```
+---------------------------------------+
|         主机CPU (x86/ARM)             |
|          (控制、串行部分)              |
+---------------------------------------+
       ↓ (PCIe / CXL / 芯片间互联)
+---------------------------------------+
|  加速器 (GPU / FPGA / TPU / NPU)       |
|        (数据并行、高吞吐部分)           |
+---------------------------------------+
```
DSA的设计考量一般有：
- 加速器覆盖足够大的工作负载（Amdahl告诉我们，可加速比例限制总加速）
- 数据传输开销（DMA、复制）需被计算收益抵消
- 编程模型需要和现有生态尽可能兼容（CUDA、OpenCL、OneAPI、SYCL）

#### 14.4.5 能效优先计算
在移动计算领域，性能不再是最重要的指标，每瓦性能（Performance per Watt）才是。这不仅仅是推动了移动计算的发展，更直接重构了传统计算体系结构。

2005年左右，CPU频率增长停滞在3-4GHz。原因不是做不出更快的晶体管，而是功耗不允许。动态功耗公式是 $P_{dyn} = α · C · V^2 · f$。这意味着：
- 频率提升一倍 → 功耗提升一倍
- 电压提升一倍 → 功耗提升四倍

为了提升频率，电压不得不跟着涨，功耗瞬间失控。一个100W的芯片已经需要风扇+散热片，200W需要水冷，再高就不可工程化了。

手机和平板的崛起彻底改变了游戏规则。电池容量有限，散热靠被动（无风扇），芯片的功耗预算通常只有 3-10W（桌面CPU是65-150W）。在这个功耗预算下，每毫瓦能完成多少计算成了核心指标。

全球数据中心的耗电量已占全球发电量的1-2%。AWS、Google、Azure每年支付数十亿美元的电费，降低服务器功耗直接关系到运营成本。这就是为什么云厂商大规模采用ARM服务器（如AWS Graviton）的原因。

ARM在2011年率先提出了 big.LITTLE 架构，随后Intel、AMD纷纷跟进。核心思想很简单，不是所有任务都需要大核。大小核协同作战。

大小核的硬件差异：

| 维度 | 大核（Performance Core） | 小核（Efficiency Core） |
|------|-------------------------|------------------------|
| 设计目标 | 峰值性能 | 能效最优 |
| 流水线深度 | 深（14-20级） | 浅（10-12级） |
| 乱序执行 | ✅ 有（窗口大） | ❌ 通常无/窗口小 |
| 缓存容量 | 大 | 小 |
| 芯片面积 | 大 | 小（1个大核≈4个小核面积） |
| 峰值功耗 | 5-10W | 0.5-1W |
| 同性能功耗比 | 1x | 约1/3功耗完成相同算力 |

小核执行相同任务时功耗只有大核的1/3-1/2，代价是速度上限较低。但这恰好符合现实工作负载波动的特点，小马就配个小鞍。以智能手机一天的典型使用为例：

```
任务类型          占比    性能需求    适合核心
─────────────────────────────────────────
后台同步          40%     极低        E核
消息推送          20%     低          E核
浏览/滑动         20%     中等        E核 + 偶尔D核
视频播放          10%     中等        E核（硬件解码）
游戏/拍照         5%      高          P核全开
应用启动          5%      高突发      P核爆发
```

80-90%的时间内，小核完全够用。 大核只在高强度突发任务（应用启动、游戏加载）时短暂激活，然后迅速休眠，这正是大小核架构的黄金场景。小核处理大部分时间的中低负载，大核应对短时间的高性能突发 。

历史上，体系结构的评价指标是每秒多少指令（性能）。今天，每瓦每秒多少指令才是核心KPI：
```
能效 = 性能 / 功耗
```

这个公式的转化，标志着体系结构设计哲学的彻底转变：

| 时代        | 第一性原则       | 设计驱动力      |
| --------- | ----------- | ---------- |
| 1960-2000 | 性能最大化       | 时钟频率、指令吞吐量 |
| 2000-2010 | 性能+功耗平衡     | 桌面CPU功耗墙出现 |
| 2010-2020 | 能效优先        | 移动计算、数据中心  |
| 2020+     | 每瓦性能 + 领域专用 | AI、边缘计算    |

大小核架构只是这个趋势的冰山一角。未来的CPU设计将是能效原生（Energy-First）的，芯片上不会有任何一块晶体管是浪费电的。


### 14.5 数据传输
冯·诺依曼结构中，CPU与内存、I/O设备之间的数据传输通道是系统的核心瓶颈。半个世纪以来，CPU性能提升了百万倍，但数据传输的架构演进却经历了多次根本性重构。每一次重构都在让数据更快地在计算单元和存储/设备之间更快地流动。

#### 14.5.1 传统总线
最早的计算机采用简单的总线结构，所有组件（CPU、内存、I/O设备）挂在同一条共享总线上。
```
     CPU
      │
      ├─────── 内存
      │
      ├─────── 磁盘控制器
      │
      ├─────── 网卡
      │
      ├─────── 键盘/鼠标
      │
    总线（共享、半双工）
```

这个结构的核心特征是共享。所有设备共享同一条总线，同一时刻只能有一个设备发送数据。其优点是结构简单，缺点是显而易见的。键盘敲一下也会占用总线带宽，磁盘和网卡同时传输数据时会相互干扰。

当CPU频率进入MHz时代后，这种共享总线的带宽（ISA总线约8MB/s，EISA约33MB/s）迅速成为瓶颈，CPU不得不等待总线空闲才能访问内存。

#### 14.5.2 南北桥分离
为了解决CPU和内存之间的高速通信需求，Intel在1990年代引入了南北桥架构。这个架构的核心思想是快慢分层，南桥负责慢速设备，北桥负责高速通道。CPU只和北桥通信，北桥再和南桥通信。

```
CPU
 │
 ├─────────────── 北桥 ──────────────── 内存
 │               │
 │               ├──────── 显卡 (AGP/PCIe)
 │               │
 │               └──────── 南桥 ────── IDE/硬盘
 │                           │
 │                           ├────── USB
 │                           ├────── 网卡
 │                           ├────── 声卡
 │                           └────── PCI插槽
```

南北桥的引入解决了两个根本问题：
- CPU到内存的路径不再被I/O设备干扰（前端总线，FSB）
- 慢速设备挂在南桥，不影响北桥的高速通道

北桥承担的最关键的职责内存控制器（管理DRAM访问）和高速I/O桥接（连接显卡等高速设备），是数据传输的核心枢纽。南桥则负责处理慢速I/O（USB、SATA、音频、PCI等）。

南北桥架构统治了PC十余年。然而，随着CPU频率提升到GHz级别，南北桥之间的连接（通常称为南北桥总线或Hub Link，如Intel的Hub Interface、VIA的V-Link）本身也成了瓶颈。CPU与内存之间隔着北桥，即使频率再高，数据也必须经过北桥中转。

#### 14.5.3 集成内存控制器
AMD在2003年率先打破了这条路径，把内存控制器从北桥移到了CPU内部。Intel则采用了一种不同的方式，通过专有的QPI（QuickPath Interconnect）实现CPU与CPU之间、CPU与芯片组之间的高速点对点连接。

这一变化颠覆了持续十多年的架构格局：
- 内存访问不再经过北桥，延迟从几十纳秒降至个位数纳秒
- 多CPU系统中，每个CPU都有自己的本地内存（NUMA架构）
- 北桥的职责大幅缩减，逐渐被集成到CPU中
- 南北桥架构事实上瓦解，北桥消失，南桥变成了“芯片组”（PCH，Platform Controller Hub），通过DMI（Direct Media Interface）与CPU连接

```
CPU + 内存控制器
         │
         ├────────── 内存插槽（直接连接）
         │
         └────────── PCH（南桥功能集合）── 硬盘
                       │  
                       ├──────── USB    
                       ├──────── 网卡 
                       ├──────── 音频 
                       └──────── PCIe 
```

今天，内存控制器已经100%集成在CPU内部，北桥已经彻底消失。而南桥演化为了PCH，主要处理慢速I/O，不再参与内存路径。

#### 14.5.4 通用高速互联PCIe
南北桥消失后，CPU与外部设备之间的通用互联通道变成了PCIe（Peripheral Component Interconnect Express）。PCIe取代了PCI、AGP、PCI-X等互不兼容的总线，用单一标准覆盖显卡、网卡、SSD、FPGA加速卡等几乎所有高速设备。

PCIe的核心改进是从并行共享总线转为串行点对点。传统PCI是并行共享总线，多个设备共享带宽；PCIe则是串行点对点，每个设备独享通道（lane），通过交换机实现多设备连接。

| 特性 | 传统PCI | PCIe |
|------|---------|------|
| 拓扑 | 共享总线 | 点对点交换 |
| 带宽分配 | 所有设备共享 | 每个设备独享 |
| 扩展 | 受限于总线负载 | 通过交换机扩展 |
| 热插拔 | 不支持 | 支持 |

PCIe目前已经发展到了第七版。市场主流设备都是PCIe 6.0，一般支持16个独立的数据传输通道，单向传输总的速率约为128GB/s。

PCIe每升级一代，通道速率就会翻倍。这种演进是因为它定义了良好的分层结构（物理层、数据链路层、事务层），每一层可以独立演进而不影响上层协议。

#### 14.5.5 专用高速内存HBM
PCIe解决了设备间的互联带宽。但CPU/GPU与内存之间的带宽墙依然存在，而且越来越严重。传统DDR内存的带宽受限于引脚数和信号完整性，每通道带宽提升有限。

HBM（High Bandwidth Memory）采用了完全不同的思路。它用复杂的封装工艺如用硅通孔（TSV，Through-Silicon Via）将多个DRAM芯片堆叠在一起，然后通过宽接口（1024位）直接连接到CPU/GPU。带来的效果是几十倍的带宽提升：
```
传统DDR:
  64位总线 × 3200 MT/s ≈ 25.6 GB/s

HBM3:
  1024位总线 × 6400 MT/s ≈ 819 GB/s
```

HBM的带宽密度极高，功耗远低于DDR（每比特传输功耗约1/3），缺点是成本高、容量受限，不适合作为主内存，主要用于高性能计算、AI训练、高端显卡。

CPU也可以配置HBM。对于CPU来说，HBM可以作为超大容量的L4缓存使用（缓存模式），也可以作为类似NUMA中的本地内存使用（内存模式）。

HBM是3D堆叠的典型应用。计算芯片与存储芯片在垂直方向上集成，和芯片集成封装在一起。其物理距离可以缩短到微米级，带宽密度也达到前所未有的水平。这是典型的把存储搬到计算旁边。

CXL则走了完全不同的方向，把计算搬到存储旁边。

#### 14.5.6 计算快速连接（CXL）
CXL（Compute Express Link）基于PCIe构建，解决的是设备间共享内存的问题。

PCIe的设备与CPU之间是显式数据传输，设备有自己的内存空间，CPU通过DMA搬运数据。这种模式的问题是数据传输开销大，且无法做到细粒度的共享。

CXL在PCIe物理层之上提供了三种子协议，其中最核心的是CXL.cache和CXL.mem，允许设备（加速器、智能网卡、内存池）和CPU以缓存一致的方式共享同一地址空间。

这意味着GPU可以直接访问CPU内存，而不需要显式的DMA操作；多个CPU socket可以挂载同一个内存池；更重要的是，CXL.cache允许加速器缓存CPU内存，并参与缓存一致性协议（MESI的扩展版本）。当加速器修改了一个缓存行，CPU会通过一致性协议感知到这个变化，就像这是另一个CPU核心在修改一样。

CXL 3.0更进一步，引入了交换能力，允许构建由数十个设备组成的内存池，数据中心正在从每台机器独立内存走向统一内存池。

#### 14.5.7 数据传输架构的演进逻辑
回顾整个数据传输架构的演化，可以看出一条清晰的路径：

| 时代          | 架构          | 核心瓶颈             | 解决方向                   |
| ----------- | ----------- | ---------------- | ---------------------- |
| 1980s-1990s | 共享总线        | 所有设备争抢同一条总线      | 分离慢速/快速设备              |
| 1990s-2000s | 南北桥         | 内存控制器在北桥，延迟高     | 内存控制器集成到CPU            |
| 2000s-2010s | CPU集成内存控制器  | 设备间互联带宽不足        | PCIe统一设备互联             |
| 2010s-2020s | PCIe + HBM  | 内存带宽墙、设备与CPU数据搬运 | 3D堆叠（HBM）、缓存一致性扩展（CXL） |
| 2020s+      | CXL + 统一内存池 | 数据在计算节点间移动的成本    | 内存池化、计算与存储融合           |
瓶颈在哪里，架构就重构到哪里。当总线成为瓶颈，南北桥分离；当北桥成为瓶颈，内存控制器集成；当设备互联成为瓶颈，PCIe统一；当内存带宽成为瓶颈，HBM堆叠进入；当数据搬运成为瓶颈，CXL提供一致性扩展能力。

每一次重构都是用更复杂的互联结构，换取更低的数据访问延迟和更高的带宽。最新的趋势表明，数据传输的未来大趋势不是更快地移动数据，而是让数据不动，计算向数据靠拢。



### 14.6 冷却基础设施
我们在异构计算中提到了能耗挑战导致的计算结构的变革。然而耗能的设备必然散热，一个无法及时排走热量的系统，无论设计多精妙，最终都会因过热而崩溃。冷却系统因此成为现代体系结构的物理基础，它决定了计算单元的部署密度、运行频率和长期可靠性。

#### 14.6.1 能耗
2005年左右，CPU频率增长停滞在3-4GHz，能耗成为关键阻碍。后摩尔定律时代，芯片制程技术发展趋缓，计算芯片转向通过增加核心数量来提高算力。

AI时代的到来让问题更加严峻。以GPU为例，用于AI训练的GPU热设计功耗（TDP）从早期V100的300W提升至B200的1000W。单颗AI芯片功耗已突破传统风冷单芯片散热800W的上限。

这种功耗攀升直接传导到数据中心层面。一台8卡H20服务器的整机功耗约3.5kW，如果使用B200等高功率GPU，整机功耗更高。传统风冷机柜通常只能支持5kW左右功耗，面对AI服务器的密集部署，风冷技术已全面捉襟见肘。

#### 14.6.2 从风冷到液冷
数据中心的冷却技术经历了从自然对流到精密风冷再到液冷的完整演进。

##### 14.6.2.1 风冷
早期数据中心依靠简单的通风设备散热。2005年中美同步出台数据中心能效标准后，精密空调系统成为主流。风冷机柜的电能利用效率（PUE）通常在1.3-1.4之间，且单机柜功耗难以超过5-8kW。

PUE（Power Usage Effectiveness）是衡量数据中心能效的核心指标：
```
PUE = 数据中心总耗电 ÷ IT设备耗电
```

PUE越接近1，说明越多的电能被用于计算本身，而非制冷和配电等辅助设施。2023年，全球数据中心平均PUE约为1.58，相当于每用100度电，就有约58度用于制冷和配电，其中制冷系统约占27%的能耗。

##### 14.6.2.2 液冷
液体的比热容和换热能力是空气的3000倍以上。液冷由此成为打破风冷瓶颈的关键技术。

液冷主要分为两个层次：
* 冷板式液冷。这是是目前主流方案。它通过金属导热板将GPU或CPU热量传导至循环冷却液中，实现高效散热。英伟达Rubin平台的全液冷方案即属此类。冷板式机柜PUE可降至1.15-1.2，且改造成本相对可控，成为当下解决高热密度问题的最现实路径
- 浸没式液冷。将服务器主板、CPU、GPU等发热核心组件完全浸入特制冷却液中。这种方案的优势是单机柜功率密度可提升5倍以上；取消风扇后噪音降至45分贝；服务器故障率大幅下降，数据中心整体能耗可降低约40%

国内企业中科曙光已在浸没式液冷领域取得全球领先地位，其技术将芯片、主板浸泡在特殊液体中，利用液体相变（沸腾）带走热量，实现了高效散热。从风冷到液冷，散热方式的升级带来的不仅是省电，更是高密部署能力、长期稳定运行和更低运维成本。

冷却液的研发也有多条技术路线并行推进，包括氟化液、合成酯、有机硅、碳氢等方案[](https://paper.cnstock.com/html/2026-05/29/content_2223765.htm#1)。2025年12月，工信部已将液冷散热技术纳入重点推广的节能降碳技术目录[](https://paper.cnstock.com/html/2026-05/29/content_2223765.htm#1)，政策正在加速液冷的规模化应用。

#### 14.6.3 从设备到系统
冷却技术的演进不仅是换个散热器。这是完整的系统结构设计，从设备级优化向系统级重构的转变。

冬季及过渡季节，室外温度低于室内，自然界蕴含丰富的冷源。新风直接自然冷却技术可直接将室外冷风引入数据中心。雅虎在纽约的数据中心采用全新风自然冷却，PUE可达1.08。腾讯贵安七星数据中心利用贵州溶洞中的天然冷风，极限PUE达到1.1左右。

更激进的是直接水侧自然冷却技术。抽取温度恒定的海水或湖水作为冷却源，可最大化利用自然冷源。这正是海底数据中心的冷却基础。

海底数据中心是一个集成的方案。把几百上千台服务器集成到一个集装箱大小的数据舱中，光纤连接后然后直接沉入海底，用海水实现无动力循环制冷。这个技术节省土地资源超过90%，且几乎不消耗淡水。微软2018年首先验证了这个方案的可行性。

这种方案往往还与海上风电项目深度绑定。绿电直供、天然冷源、节省土地。零淡水消耗，几乎是绿色计算提供的绝好样板。目前我国在这方面的技术领先全球。

废热回收则是另一个方向。数据中心产生的热量可以被回收用于区域供暖、农业温室等场景。这在北欧等寒冷地区已有多例实践。未来PUE提升的潜力，不仅仅是更好的冷却设备，还在于系统级的能源整合。在气候问题成为挑战的今天，绿色计算的重要性只会与日俱增。

### 14.7 体系结构的统一视角
我们从不同的角度回顾一下体系结构的谱系。
#### 14.7.1 计算结构的演化
曾几何时，我们可以用摩尔定律直接预测CPU的性能，只要等一年半，计算能力就会翻倍。当制造能力逐渐达到物理极限，单核的性能增长放缓，多核开始出现，然后能效异构核开始出现，直到领域异构：

| 阶段         | 代表芯片                     | 核心配置            | 意义       |
| ---------- | ------------------------ | --------------- | -------- |
| 单核         | Intel Pentium 4          | 1×大核            | 性能=频率    |
| 同构多核       | Intel Core 2 Quad        | 4×同质大核          | 横向扩展     |
| 异构大小核（移动端） | ARM Cortex-A7/A15        | 4×小核+4×大核       | 能效进入设计中心 |
| 异构大小核（桌面）  | Intel Alder Lake         | 8×P核+8×E核       | 能效回归桌面   |
| 异构大小核（服务器） | Ampere Altra             | 同构小核            | 大规模能效    |
| 领域异构       | Apple M系列 / NVIDIA Grace | CPU+GPU+NPU+ISP | 专用加速器集成  |

这条轨迹背后的驱动力只有一个。在功耗预算内，榨取每瓦性能的极限。

未来的芯片会是一个异构计算综合体：CPU处理控制流和不可并行部分，GPU处理大规模数据并行，NPU处理AI推理，ISP处理图像信号，各个单元通过高速片上互联（如Apple的Fabric、Intel的EMIB）协同工作，形成一个完整的、能效最优的计算系统。这正是当代计算机体系结构的终极形态。

#### 14.7.2 存储结构的层次
存储是图灵机的纸带。为了性能，我们把存储做的越来越靠近计算，但是越靠近，成本越高，容量也越小，这就得到的存储层次。

CPU的典型存储层次如下：
```
+--------------------------------------------------+
| 寄存器 (~64-256B)  1-cycle   [CPU核内]          |
+--------------------------------------------------+
         ↓
+--------------------------------------------------+
| L1 缓存 (~32KB)    3-5 cycles [每核私有]         |
+--------------------------------------------------+
         ↓
+--------------------------------------------------+
| L2 缓存 (~256KB)   10 cycles [每核私有/部分共享]  |
+--------------------------------------------------+
         ↓
+--------------------------------------------------+
| L3 缓存 (~2-32MB)  30-50 cycles [多核共享]       |
+--------------------------------------------------+
         ↓
+--------------------------------------------------+
| HBM    ~1-64G 80-100ns (~300 cycles) [封装内专用]  |
+--------------------------------------------------+
         ↓
+--------------------------------------------------+
| 内存 (DRAM) ~8-128GB 80-100ns (~300 cycles)        |
+--------------------------------------------------+
         ↓
+--------------------------------------------------+
| SSD (NVMe) 1-8TB     10µs-50µs                   |
| SSD (SATA) 1-8TB     50µs-200µs                   |
+--------------------------------------------------+
         ↓
+--------------------------------------------------+
| HDD 或 网络存储    5-10ms级                          |
+--------------------------------------------------+
```


GPU的存储层次则大不相同
```
+--------------------------------------------------+
| 寄存器 (每个线程私有)    0周期     [海量，数万个]   |
| 每个线程拥有自己的寄存器文件                        |
+--------------------------------------------------+
         ↓
+--------------------------------------------------+
| 共享内存 (L1/共享)        ~1-3 cycles  [每SM]    |
| 同一线程块内共享，软件显式管理                     |
+--------------------------------------------------+
         ↓
+--------------------------------------------------+
| L2 缓存                  ~20-40 cycles [全芯片共享] |
| 硬件自动管理，统一缓存纹理、常量、全局数据          |
+--------------------------------------------------+
         ↓
+--------------------------------------------------+
| HBM 显存                 ~80-100ns   [板载]      |
| 主显存，带宽极高（>1TB/s），容量有限               |
+--------------------------------------------------+
         ↓
+--------------------------------------------------+
| 统一内存（CPU DRAM）     ~80-100ns + PCIe/NVLink  |
| 通过PCIe或NVLink访问CPU内存                        |
+--------------------------------------------------+
         ↓
+--------------------------------------------------+
| NVLink/NVSwitch 远端GPU显存      ~300-500ns                |
| 访问其他GPU的HBM，通过NVLink互联                   |
+--------------------------------------------------+
         ↓
+--------------------------------------------------+
| 远端存储 (RDMA)          µs-ms级                  |
| 通过网络访问远端存储或服务器                       |
+--------------------------------------------------+
```

GPU的L1缓存需要程序显式管理，这提升了复杂性，也为有效的优化提供了可能。

存储层次是现代体系结构最重要的设计原则之一。用小的快存储掩盖慢宽的访问延迟，以很小的成本达到很高的效率提升，达到效率和成本极佳平衡。

#### 14.7.3 能效结构的权衡
我们也可以从通用性和能效角度做出计算单元的谱系：
```
通用性高 ──────────────────────────────────────→ 能效高

通用CPU (x86/ARM)      GPU        FPGA      ASIC
   ↑                    ↑          ↑          ↑
   |                    |          |          |
灵活、可编程         数据并行     可重构    固定功能
中等能效             高能效      很高能效   极致能效
```

### 14.8 本章小结
我们从冯·诺依曼结构开始，看到了存储程序概念的深远影响以及其与生俱来的瓶颈，这是程序就是数据这个基础结构的胜利，也是阻碍性能的诅咒。指令集架构（ISA）是硬件与软件的契约，RISC vs CISC的争论最终走向了融合，而RISC-V正以开放姿态挑战既有格局。

为了突破顺序执行的性能天花板，体系结构引入了多层次的并行。指令级并行（流水线、超标量、乱序）、数据级并行（SIMD）、线程级并行（SMT、多核）。缓存一致性协议（MESI）是多核正确运行的基石。

异构计算的兴起反映了领域专用性的价值。GPU、FPGA、ASIC在特定任务上远超通用CPU。这不仅是技术的选择，更是经济的选择。当工作量足够大，专用化就很合理。

计算的能耗不断提升，冷却成为基础设施越来越关键的一部分，如何有效冷却成为计算机系统结构设计的关键。

体系结构是IT领域最宏大的结构，是计算结构，存储结构，网络结构，能源和冷却结构之集大成者。小到芯片设计制造，大到高性能计算机（HPC）的设计研发，体系结构技术可以看做是人类科学技术和工业制造能力的最高标杆，也是结构决定转换效能的根本体现。
