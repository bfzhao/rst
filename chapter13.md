## 第十三章 可计算性与计算复杂性：转换的边界与代价
计算是一类转换，能够由有限规则机械执行的转换。如果我们考虑深一点，就会遇到一个问题：计算有限制么，什么是可计算的？可计算的东西，计算的代价又有多大？

这个问题甚至在现代计算的影子都看不见的时代就被问起。随机计算技术的不断更新，关于这个问题的理解也不断深化。

首先可以明确地说，计算确实有边界（有些无法计算），计算的代价也有很大的不同（有些不可能快速计算）。我们之前学习过的算法以及评估算法的大O复杂性度量都部分展示了这一点。然而有一类问题，我们后面会看到，无论你的算法设计多么精巧，理论上都不可能有一个简单的解。

本章中，我们探讨计算的不可能性边界，那些算法永远无法跨越的高墙。

### 13.1 计算模型
当我们的祖先用手指计数，用算筹做基础运算，用算盘做复杂计算，直到现代计算机在方寸之间完成海量运算，我们很难看出计算到底是什么东西。要理解什么是计算，我们需要首先定义什么能被计算。这就是计算模型。

#### 13.1.1 形式化定义
计算模型尝试用最小的规则描述什么是计算。不同于现实计算机，计算模型并不是为了制造机器，而是为了回答什么叫计算这个数学问题。它们故意被设计得极其简单，只保留计算最本质的属性，但是又足够的通用，能描述问题的本质。

自20世纪30年代，多位学者独立提出了不同的形式化定义：

| 计算模型  | 提出者               | 年份    | 核心思想        |
| ----- | ----------------- | ----- | ----------- |
| 图灵机   | Alan Turing       | 1936  | 读写头+纸带+状态转移 |
| λ演算   | Alonzo Church     | 1936  | 函数抽象与应用     |
| 递归函数  | Gödel, Kleene     | 1936  | 基本函数+递归操作   |
| 元胞自动机 | von Neumann, Ulam | 1940s | 简单规则+局部交互   |

所有这些模型在可计算能力上是等价的。

这里我们介绍下图灵机和λ演算。

#### 13.1.2 图灵机
图灵机是现代计算理论最重要的基础模型。是图灵在论文《On Computable Numbers, with an Application to the Entscheidungsproblem》中提出。这里面的数指的是实数，如 $\pi,\sqrt{2}$ 等。图灵首先问：

> 有没有一种机械的方法，可以把这个数算出来？

如果存在一种固定规则，无论你要求第100位还是第100000位，都能有限步算出来，那么这个数就是 Computable Number（可计算数）。

我们知道 $\pi,\sqrt{2}$ 这些都有算法可以算出来，自然是可计算的。

> 有不可计算的数么？事实上，几乎所有实数都是不可计算的。
>
> 原因其实非常简单。假设程序可以编号，我们从1开始，给所有的程序编号，每个程序计算一个数，程序是无穷的，可计算数也是无穷的。但是康托已经用他的天才证明了实数是不可数无穷的，所以绝大部分数都是不可计算的。

当然，图灵根本不是想研究数学。他真正想弄明白的是我们上面提到的问题，什么叫机械计算。这里的机械指的是按照规则，有限步骤。图灵证明了，格子以及其中的符号，有限的记忆能力再加上按规则不断执行，就能完成所有的计算。这就是可计算！这就是图灵机！这才是图灵论文的本质！

但是可计算数就可以表达全部的算法么？这才是图灵最伟大的地方。他用符号统一了计算：数字也是符号，文字也是符号，公式也是符号，程序也是符号。一个程序，其实就是不断产生符号。证明一个数学命题其实也是不断进行符号变换。所有这一切，都是符号转换而已。而图灵机，处理的就是符号转化，至此大功已成。

如果站在更高的角度看，整件事情建立在三个基础之上：
1. 编码（Encoding）。任何数学对象，如数字、公式、程序、证明，都可以表示成有限的符号串
2. 机械规则（Mechanical Procedure）。计算过程只能依赖当前有限的信息和固定规则，不允许“灵光一现”或无限预知。
3. 可枚举性（Enumerability）。所有算法都可以用有限描述写出来，因此可以编号、枚举。这正是可计算数只有可数多个，而实数却不可数多个的根本原因。

结构上讲，图灵机是一个带有读写头的控制器，可以从纸带上读取和写入内容，同时有预定义的规则决定如何读写。很多人都被图灵机构造的简单性和其揭示问题本质的复杂性而迷惑倾倒。图灵被誉为计算科学之父绝非浪得虚名。

图灵机有很多变体。多带图灵机有多纸带作为输入，计算速度更快，但不扩大可计算范围；非确定性图灵机（NTM）允许每个状态有多种选择，只要有一条路径接受即接受；而通用图灵机是一种可模拟任意其他图灵机的图灵机。这其实是存储程序计算机的理论原型。

#### 13.1.3 丘奇的λ演算
有人认为λ演算比图灵机更易于理解，可以看作是计算的代数模型。λ演算由数学家邱奇提出，基于三个概念：
- 变量。`x`, `y`, `z`, ...
- 函数抽象。`λx.M`（接受参数x，返回M，相当于定义一个函数）
- 函数应用。`M N`（将M应用于N，相当于调用一个函数）

而β-归约则是计算的核心。简单理解就是函数调用的结果求值。
```
(λx.M) N → M[x := N]   (将N代入M中的x)

示例：
(λx.x²) 5 → 5² → 25
```

基于不断的函数应用，邱奇定义了邱奇数（Church numerals）：
```
0 = λf.λx.x
1 = λf.λx.f x
2 = λf.λx.f (f x)
3 = λf.λx.f (f (f x))
```

如果不知道结论，很难想象这个和图灵机没有一点相似性的构造竟然和图灵机是等价的。粗略地说，变量相当于图灵机的纸带，函数相当于规则，函数应用则是读写头。

这些模型形式迥异，却拥有完全相同的可计算能力。这表明，计算能力并不依赖于机器、语言或数学表示，而源于一种更抽象的本质，有限规则驱动的状态转换。正因为这种本质如此简单而普适，不同的形式最终都收敛到了同一个计算世界。

可计算性的根源，在于算法必须能够由有限规则描述。任何有限描述都可以编码为一个有限字符串，因此所有算法只能构成一个可数集合。正因为如此，所有计算模型最终都只能覆盖同一个可数的可计算世界，而无法穷尽不可数的数学对象。

这说明计算的普适性（Universality）。丘奇说：“任何直觉上可计算的函数，都可以用图灵机（或λ演算等等价模型）计算。”。它不是数学定理，而是计算理论最重要的经验性论题。

这直接导出一个概念，图灵完备。

#### 13.1.4 图灵完备
图灵完备指的是，如果一个系统能模拟通用图灵机，那它就是图灵完备的。所有的图灵完备的系统，他们的计算能力是等价的。

什么样的系统是图灵完备的？
- 编程语言（C、Python、Haskell...）不用说了
- Conway生命游戏
- Powerpoint + 超链接（是的，图灵完备）
- 某些卡牌游戏（Magic: The Gathering）
- Minecraft红石电路

图灵完备性只回答能否计算，不回答计算多快。我们在复杂性理论一节讨论这个问题。

### 13.2 判定和计算能力
判定问题就是输出只有Yes/No的问题。如果存在某个算法，对所有输入都能正确回答“是”或“否”，且保证终止，那么它是可判定的问题；如果存在算法，对“是”输入回答“是”（可能对“否”输入不停机），那么是半可判定问题；如果没有这样的算法，这个问题就是不可判定的。

最著名的不可判定问题是停机问题，是图灵在上面的论文中给出的。

#### 13.2.1 停机问题
停机问题很简单。一个程序`P`，以及它的输入`I`，请判断`P`在输入`I`上是否会终止（停机），还是永远运行下去。

初看起来，似乎这不是个问题，程序的行为是明确的，只要输入确定，是否终止不是很简单的事吗？我们把所有可能的执行情况都枚举出来（可能非常多，但是理论上总有极限），自然可以得出结论不是么？

图灵用一个精巧的反证，证明这个问题是不可判定的。

图灵的思想核心是反证。不去考察P本身是否可判定，直接假设存在这样的程序`Halt(P, I)`，能判定任意程序`P`在输入`I`上是否停机
```
  Halt(P, I) = true  →  P(I) 停机
  Halt(P, I) = false →  P(I) 不停机
```

然后在构造另一个程序`T`，其逻辑为：
```
  if Halt(P, P) == true:
      while(true)  // 死循环，不停机
  else:
      halt          // 立即停机
```
现在问题是：`T(T)` 停机吗？

> 注意，这里`Halt`函数的输入`I`是`P`自身，因为既然任何程序都可以作为输入，自己也可以，因为程序本身是数据。

情况1：假设` T(T)` 停机，则 `Halt(T, T) = true`，然后 `T(T)` 进入死循环，推出不停机（矛盾）
情况2：假设` T(T)` 不停机，则 `Halt(T, T) = false`，然后 `T(T)` 立即停机（矛盾）

因此，`Halt(T, T)` 无法给出一致答案，所以 `Halt` 程序不存在。结论是停机问题不可判定。

熟悉罗素的集合矛盾和哥德尔的不完备定理的读者一眼就可以看出，他们的类似的自指结构是核心困难，而一个自洽的系统是不可能自己证明自己的。

除了停机问题，还有那些不可判定的呢？似乎我们日常也不会用停机检查啊。事实上，很多基础的日常问题也是不可判定的。

#### 13.2.2 不可判定问题
常见的不可判定问题有

| 问题             | 描述         | 可判定性 |
| -------------- | ---------- | ---- |
| 判断两个程序是否等价     | 对所有输入有相同输出 | 不可判定 |
| 判断程序是否有死循环     |            | 不可判定 |
| 判断程序是否会访问未定义内存 |            | 不可判定 |
| 判断程序是否输出特定字符串  |            | 不可判定 |

莱斯定理更是明确说，关于程序行为的任何非平凡性质都是不可判定的。非平凡性质指的是取决于程序的实际行为的属性，程序的语义性质，如：
- 程序是否会终止？
- 程序是否正确计算平方？
- 程序是否会输出‘hello’？

这意味着，绝大多数我们关心的程序语义属性都是不可判定的。这解释了为什么程序验证如此困难。完全自动化是理论上是不可能的。但是这并不等于，在适当放弃部分要求的情况下，我们不能做出工程上实用的方案和工具。

一个问题是可计算的，但是却可能不可判定。我们自然要问，计算能力到底是什么？

#### 13.2.3 计算能力
图灵机的计算能力可以归纳为如下几个基础属性
1. 可修改，可访问状态
2. 状态的访问能力
3. 状态转移的规则
4. 驱动结构（驱动结构仅仅是驱动计算的装置，决定计算如何一步一步推进，其具体形式不影响能力本身，可暂不考虑）

细化这些基础属性，我们可以分列如下：

| 属性     | 取值                                        |
| ------ | ----------------------------------------- |
| 状态数量   | 有限 / 无限                                   |
| 状态属性   | 只读 / 可读写（可变）                              |
| 状态访问方式 | 无损性/回顾性                                   |
| 状态转移规则 | 无转移（停机/终止）/ 确定性（只有一个目标状态）/ 非确定性（可有多个目标状态） |

访问方式值得解释一下。

**无损性**
指的是是否可以访问一个状态而不破坏其他状态。一个只能向前的迭代器是有损的，访问后就不能再次访问；而栈是无损的。

**回顾性**
指的是是否可以访问过去的状态而不破坏无损性。栈是不可回顾的，因为必须把其上的数据全部弹出才可以访问，这破坏了无损性。两个栈的组合是可回顾的，因为我们总是可以在访问一个数据的时候，使用另一个栈记录历史，并且访问之后恢复状态。

组合这些基础能力，我们可以得到非常有趣的计算能力图谱。

##### 13.2.3.1 计算图谱
| 模型               | 状态数量 | 状态属性 | 访问方式    | 转移规则 | 能力等级      | 可解决问题                        |
| ---------------- | ---- | ---- | ------- | ---- | --------- | ---------------------------- |
| **组合逻辑电路**       | 无状态  | —    | —       | -    | 单纯映射，纯函数  | 固定位数的布尔运算                    |
| **有限自动机/DFA**    | 有限   | 只读   | 有损/不可回顾 | 确定性  | 正则语言      | 词法分析，文本模式匹配，状态机              |
| **有限自动机/NFA**    | 有限   | 只读   | 有损/不可回顾 | 非确定性 | 正则语言      | 与DFA相同，但更便于描述某些模式，如正则中的“或”   |
| **下推自动机/DPDA**   | 无限   | 可读写  | 无损/不可回顾 | 确定性  | 确定上下文无关语言 | 对称结构，带分隔符的结构                 |
| **下推自动机/NPDA**   | 无限   | 可读写  | 无损/不可回顾 | 非确定性 | 上下文无关语言   | 嵌套结构，无分隔符回文                  |
| **线性有界自动机/DLBA** | 有限   | 可读写  | 无损/可回顾  | 确定性  | 确定上下文有关语言 | `aⁿbⁿcⁿ`类匹配，类型推断（部分），上下文依赖结构 |
| **线性有界自动机/NLBA** | 有限   | 可读写  | 无损/可回顾  | 非确定性 | 上下文有关语言   | 至少不弱与DLBA，开放问题               |
| **图灵机/DTM**      | 无限   | 可读写  | 无损/可回顾  | 确定性  | 通用计算      | 所有可计算问题                      |
| **图灵机/NTM**      | 无限   | 可读写  | 无损/可回顾  | 非确定性 | 通用计算      | 与DTM相同，更易于描述NP类问题            |
|                  |      |      |         |      |           |                              |

这里有两点需要注意：
1. 下推自动机的确定性和非确定版本有着不同的能力。非确定性可以处理递归结构，而确定性版本只能处理对称结构
2. 线性有界自动机的确定性和非确定版本能力是否等价是一个开放问题。我们知道确定性版本必然是非确定性版本的子集。但是是否是真子集还未知

仔细分析这个限制，我们会对结构对计算能力的限制有一个更深入的理解。

##### 13.2.3.2 能力的结构限制
状态提供了对问题的建模能力，而可读写的状态则意味着对可以维持历史。这就是下推自动机可以支持对称结构，递归结构的原因。

而状态的转移规则，也就是说历史状态的访问能力，则决定计算能力的上限。图灵机有最强大的历史状态访问能力，这使得他是可计算问题的最高能力代表。

然而，这种能力带来了一个根本性的副作用，通用判定问题上的无解。这是图灵机结构的必然结果。

##### 13.2.3.3 图灵机在通用判定问题上无解的原因
图灵机在判定问题上无解，根本原因在于它无损的可回顾的状态访问能力，这使得“自指”结构可以构造出来。图灵的反证中有一个关键的结构`Halt(P, P)`，这意味着需要两次访问`P`，而需要保持`P`不变。这至少需要时无损的。而`P`本身包含多个状态，这又要求访问是可回顾的。无损的可回顾的状态访问是通用判定问题上无解的充分必要条件。

其实这是哥德尔不完备定理是同一结构的不同表现。哥德尔证明了任何足够强大的形式系统，都能构造一个自指命题 G = “G 不可证明”，导致系统不完备。而图灵的证明不过是符号计算领域的一般特化版本。任何足够强大的计算系统（图灵机），都能构造一个自指程序 D = “D 不会停机”，导致判定问题不可解。

其中的核心细节，是自指的构造。图灵机的无损可回顾的状态访问使得系统能够“看见完整的自己”，从而触发自指结构，暴露出系统在完备性或可判定性上的根本局限。

限制了这个模式，如PDA是无损不可回顾的，我们就打破了这个基础。如你所预料，PDA在判定问题上是可解的。恰当地限制计算能力，破坏构建自指的结构性能力，我们就得到了确定得可判定性结果。

### 13.3 计算复杂性
计算能力考察的时候计算的可能性，复杂性理则论讨论的是计算的复杂性和实用性，转换的时间和空间的要求，也就是转换需要的资源要求。

#### 13.3.1 复杂度分类
我们把常见的复杂度类列如下表

| 复杂度类       | 定义                   | 资源限制            |
| ---------- | -------------------- | --------------- |
| **P**      | 确定性多项式时间可解           | 时间 = O(n^k)     |
| **NP**     | 非确定性多项式时间可解（解可多项式验证） | 验证时间 = O(n^k)   |
| **EXP**    | 确定性指数时间可解            | 时间 = O(2^{n^k}) |
| **L**      | 确定性对数空间可解            | 空间 = O(log n)   |
| **PSPACE** | 确定性多项式空间可解           | 空间 = O(n^k)     |
其中P和NP是核心。

#### 13.3.2 P与NP
P类问题是存在多项式时间算法的问题。如排序、最短路径、二分查找、矩阵乘法等，这些通常被认为具有高效算法。

NP类问题是解可以多项式时间验证的问题，即非确定性图灵机（NDTM）在多项式时间内可解的问题集合。它有如下特性：
- 如果某人给你一个解，你可以快速验证是否正确
- 但找解可能很难（或可能容易，未知）

NP的代表问题有：

| 问题   | 问                  | 验证           |
| ---- | ------------------ | ------------ |
| SAT  | 是否存在满足布尔公式的赋值？     | 代入赋值，验证      |
| 旅行商  | 是否存在总距离<k的路线？      | 计算路线总长，检查<k  |
| 图着色  | 是否能用K种颜色着色？        | 检查所有边端点颜色不同  |
| 背包   | 是否存在价值至少V且重量≤W的子集？ | 计算子集价值、重量，检查 |
| 整数分解 | 是否存在N的非平凡因子？       | 验证乘积=N       |

#### 13.3.3 NP完全性
NP完全性定理说的是，如果问题Q是NP完全的，那么所有NP问题可以在多项式时间归约到Q。这意味如果任何一个NP完全问题有多项式时间算法，则所有NP问题都有多项式时间算法（P=NP）。

那么P=NP么？

> 有一个笑话说的是，当N为1时，P=NP。

几乎所有专家都相信P ≠ NP，但是没有证明。

### 13.4 可计算性与复杂性的统一视角
我们继续从统一视角看一下可计算性和复杂性和谱系。
```
未知领域
    ↑ 突破不可判定？
不可判定问题 ← 停机问题、等价性...
    ↑ 无算法，原则上
指数时间（EXP、EXPSPACE）← 难但可解（玩围棋?→EXPTIME）
    ↑
NP完全（SAT、TSP、背包）← 难解但易验证
    ↑
P（排序、最短路径、矩阵乘）← 高效可解
    ↑
Log空间（L）← 极节约空间
```
仅仅利用下三层的能力，我们就构建了庞大的数字世界！

可计算性理论把函数做了分类：
- 可判定问题（总能终止），包含能在有限时间给出答案的问题
- 可计算函数（可数无限），包含所有图灵机可计算的函数
- 绝大多数函数（不可计算），包含停机问题、不终止函数等待
- 所有的函数 (不可数无限)

请铭记以下事实：
- 可计算函数是可数无限的，图灵机可编码为整数
- 所有函数的集合是不可数无限的

 绝大多数数学函数不是可计算的。我们日常遇到的是那极少的可计算者，正如我们看到的数字都是极少数的可数数。

### 13.5 本章小结
我们从计算模型开始，确立了可计算的定义。丘奇-图灵论题断言所有合理的计算模型在能力上等价，刻画了“算法”的理论边界。然而，停机问题的不可判定性证明了这个边界的存在，存在原则上没有任何算法能解决的问题。

从图灵机的机构，我们抽提了基础原子要素并且重构了计算能力的谱系。基于这个结构，我们分析了图灵机为什么在通用判定问题上无解。通过限制结构的灵活性，从而限制了计算的能力，反而可以在判定问题上有确定的结果。这是结构在能力和确定性上的权衡的结果。

但当问题可判定时，复杂度理论追问代价：P与NP？NP完全的发现揭示了成千上万问题在计算上等价的惊人结构。我们推测 P ≠ NP，但是需要证明，是当代理论计算机科学最重要的未解问题之一。
