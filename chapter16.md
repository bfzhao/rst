## 第十六章 软件危机2.0：AI时代构建可信软件的挑战
1960年代的时候，爆发了软件危机。我们从传统工程领域学习迁移和很多知识到软件工程领域，并且取得了光辉的成就。从传统强调流程保证质量的瀑布式开发到近十年来流行的强调更小反馈循环的敏捷开发模式，软件开发的流程始终是从需求到分析，从架构到设计，从实现到验证，从交付到维护一整套完整的生命周期。

进入到AI时代，当AI仅仅可以凭借一句提示词就可以直接输出一个看起来还可用的应用的时候，情况似乎发生了变化。如果AI可以直接生成软件，传统的软件开发范式还能用么，还有用么？特别对应到我们的主题，当需求到实现的差距只是一句提示词，表达的目的何在，结构的意义是什么，转换又是否需要？

让我们把AI带来的软件开发的挑战叫做软件危机2.0。深入讨论AI给软件开发带来的巨大影响，深刻挑战以及可能的应对策略。

### 16.1 软件开发的本质
如果看软件开发最终交付的产品是代码，很容易把软件开发的本质错误地理解为输出代码。这就好像认为建筑工程的本质是混凝土一样。

软件是用来处理工作，解决问题的。我们使用软件，是因为我们认为它能按照预期的方式方法提供帮助，说到底，这是一种信任。

我们站在玻璃栈道上，俯瞰脚下几百米深的峡谷。尽管感觉战战兢兢，但是大脑还是能理智地告诉我们，这是可以信任的。这种信任源自几个方面，运营方的资质和风控管控，管理部门的监管，生产厂家的质量保证，特种玻璃的材料力学特征，以及物理定律。

我们对软件的信任也是类似。用户口碑，开发厂商的可信度，软件工程师的专业程度，工具链的完整性，编程语言和操作系统的稳定可靠，物理硬件的精准计算。

尽管一个人不大可能理解这个链条涉及到的所有领域，但是如果他想这样做，他是可以做的，也是做得到的，尽管可能需要相当的资源才行。因为每个链条每个环节都有人非常清楚地理解他的工作的信任基础，需要的只是知识的转移。

每一个环节的信任都不是完全的。但是因为每一个环节都是可以复核的，甚至可审计的。经过测试运行，我们可以保证大面上不会有严重的问题。实际使用中，边界场景不断被发现，矫正，我们最终得到一个相当可靠的数字系统。这就是软件的信任来源。

当我们开始使用Vibe Coding时，这个链条被打破了。

### 16.2 Vibe Coding打破了软件开发信任链
Vibe Coding 的典型模式是，一句话就能生成一个应用。这意味着，从需求直接跳到了实现。这看起来是美好的祝福，实际带来的却是无法验证的梦魇。

传统软件工程中，每一阶段的交付和验证准确配对，确保实施的准确。这是一个精准V模型，左边是构建，右边是对应的验证：
```
需求分析 —————————————————————> 验收测试
    \                          /
  概要设计 ———————————————> 系统测试
       \                    /
    详细设计  ——————————> 集成测试
         \               /
       编码实现 —————> 单元测试
```

Vibe Coding模式下，从需求直接到编码，中间所有的构建环节，以及输入输出都消失了。AI可能会部分的模拟这个过程，但是我们不知道发生了什么，我们甚至都不知道该验证什么。在传统的软件开发中，这个过程也是对问题的深入理解的过程。真正的风险，突破都在这个过程中涌现并被恰当处理。Vibe Coding模式下，我们彻底失去了对系统的理解。

> 你不能理解它，你就无法信任它；你不信任它，你就无法将信任传递给你的客户。

更重要的是，非常多的特性并不是直接需求特性，如安全性，可靠性，可观察性，可维护性，这些都几乎不会直接体现在原始需求中。

最不幸的是，AI也不能从这个过程中积累。它可能生成一些过程性的文档，这些文档只是作为上下文填充到后面的问题中，谈不上理解，更谈不上积累。人类可以从构建一个复杂系统中学习到大量的知识，培养大量的新手，但是AI无论做多少项目，它的积累永远为零！

> 即便把这些输出作为训练材料重新训练AI，已有的研究表明，这种模型自举训练会导致最终会丢失分布尾部的信息，输出多样性骤降，陷入"模型痴呆"或"自噬"状态。

在航空航天等关键领域，我们可以直接说对比现有最强大的Coding AI更强大十倍的AI说，“帮我实现一套可以着陆火星的软件系统”，然后祈祷它不出问题么？

有人说，要么快，要么可靠，你只能选择一个。 

我们需要重建Vibe Coding时代的开发信任关系。但是首先我们需要知道，信任是从哪里丢失的，然后再看如何找回来。

### 16.3 软件传统工程如何构建信任
20世纪60年代，软件危机第一次出现的时候，人们也对软件的可靠性表示出极大的怀疑。最后的见解方案可以归结为这两条基础原则：
1. 工程化管理。承认软件开发是复杂的工程，引入传统工程领域的方式方法管理软件开发
2. 阶段性确认。把软件开发划分为不同的阶段，每一个阶段都需要和关键相关人确认，确保不走样    

这两条基础规则是瀑布式软件软件开发模式的基础，而瀑布式开发流程是交付大型软件的标准流程。与此同时，技术的革新和工具链的进步也没停下。从限制随意跳转的结构化编程到面向对象成为几乎所有的新型语言的基础范式，从修改-编译-运行循环到集成开发环境，从手工编写交互布局到“所见即所得”再到样式和布局的完全独立，从编译器到公共语言运行时到JIT，从编辑提示到自动补全到语法遵从的跨文件重构支持，我们可以看到几个明显的特性：
* 复杂性的局部化。使用抽象等手段把复杂性隔离出来，降低偶然复杂性对整体的影响，同时消除无关复杂性相互干涉的可能；
* 让反馈循环尽可能快。问题在发生的时候就暴露成本最小；   
* 确定性。所有的流程工具提供了确定的保证。软件有bug不可怕，流程有缺陷没问题，我们总有机会完善之，并且确认类似的情况不再发生；  

整个软件工程过程中，最薄弱的环节是人本身。但是我们通过有效的手段，把人的风险降低到了最低。我们知道人容易犯错，所有我们设计了完善工具和流程支持，把偶尔的犯错降到最低，把主动的作恶控制在风险之内。

这里确定性是关键。编译器是确定性的、类型系统是确定性的、测试用例是确定性的，这是传统工程能够建立信任的底层逻辑。但是当工具本身不确定的，一切都变换了。  

### 16.4 AI工具的信任缺失
毫无疑问，AI非常强大。我们训练它使用了几乎所有可用的人类知识。在绝大多数的问题上，AI表现出超过人类平均水平的能力。不要说普通人很容易被AI展现出来的非凡能力惊艳，甚至专家都相信AI可以做出创新性的成果。一些AI乐观主义者甚至宣称超级人工智能就在下一个路口等着我们，我们需要的不过是更多的芯片和电力供应而已！

然而，仔细想来，我们可以得出结论，AI本身是不可靠的。

AI是基于统计规律从海量的信息中发现结构，然后从最可能相关的信息中拼凑问题的答案。这是完全机械的过程。为了故意表现出多样性，AI设计时往往不选择最高概率的回答，而从高概率的回答中选择一个作为输出。这在大部分的情况下表现优良，但是在少数的情况下可能不尽如人意，甚至出现幻觉。

如果仅仅是回答差强人意还不是大事，毕竟人类个体能力也有区别，我们不是靠天才完成绝大部分工作的。AI的问题在于，它无法确认事实和臆想，这是绝大多数的幻觉的来源。

对于AI来说，如果不借助动态检索（现在绝大部分的AI都支持在回答的时候，通过搜索引擎做事实核查），它所认知的“事实”，就是他的海量参数中关于某个信号的激活程度，也就是它的训练材料中关于该事实的出现的概率。

AI训练的材料体量太大了，完全无法做到人工审查。这意味着恶意的训练材料投毒完全无法避免。从信任的观点讲，这就是死穴。社会工程攻击会变成更加隐蔽，甚至无法避免。

这种不确定性或者信任怀疑，导致一个可怕的后果。有商业利益冲突的供应商提供的AI模型，彼此是不可信任的。这和你的AI模型的能力无关，和你的用户基数无关，甚至和你的模型是否开源无关。

> 这里所说的开源，指的是开源权重，也是AI模型开源的一般含义。完全的开源应包含训练材料，训练手段等全部开源。目前几乎没有一个有影响力的开源模型做到完全开源。

即便是训练材料，训练算法以及权重都完全开源，相同训练材料的不同训练顺序也会导致完全不同的模型，其结果也不是可完全预测的，更不用说随机的初始化参数，dropout等各种引入随机性的手段。完全开源也不等于可复现。

完全有可能存在这样的情况：仅仅通过精心调整训练材料的顺序就可以植入一个隐藏的安全漏洞。这展示了这类统计学习对某些边界条件的极端敏感性。所以对于AI这种基于大规模数据的统计学习的结果做信任审计是不现实的。

传统的安全工具也失效了，完全测试一个AI是不可能的，黑盒分析也几乎做不到。对于一个云托管的AI模型，因为它的不确定性，除非它告诉你，你甚至无法区别它是否更新了版本。

信任或者不信任，只有两端，没有中间光谱。

对于软件开发来说，不被信任的软件是没有市场的。软件开发厂商也不能简单地宣布，他们不使用AI开发软件。这意味着他们的交付速度自然会比竞争对手更低。这是一个囚徒困境，你不能冒着被淘汰的风险坚持可信的软件开发，因为AI导致的信任灾难可能是明天后天触发甚至不触发，但是被淘汰就是当下的风险。没有人会为了未来的风险忽略现在的风险！最终的结果就是大家都飞蛾扑火，加速死亡，把不可信任的产品交付给客户。直到有一天， BANG！

这里面还有一个悖论。如果AI编码可以解决一切商业开发问题，客户为什么不直接用大模型生成软件，而要通过昂贵的软件开发中间商？毕竟客户更了解自己的需求。答案是显然的，客户购买的从来不是“代码本身”，而是确定性交付、技术背书与风险兜底。

### 16.5 逃出困局的无效尝试
看来，我们必须应对Vibe Coding带来的问题。这里是几个常见的想法，以及它们为什么不可行。    

#### 16.5.1 建立一种“意图形式化语言”，让用户精确描述需求
软件开发最大的困难就是理解用户的需求，如果这样的意图形式化语言可行，它早就应该存在了， 不用等到21世纪出现AI之后。说到底，意图无法被精确描述，只能不断地通过反馈细化。

意图本身是模糊的、冲突的、演进的。任何试图“预先形式化意图”的努力都违背了软件工程的本质：需求不是被发现的，而是被共创的。如果这种语言是图灵完备的，它本质上就是编程语言；如果不是，它无法表达任意计算。此外，普通用户无法描述他们不理解的东西，特别是安全、隐私、可靠性等“负向属性”，这使得用户驱动的意图迭代必然收敛到不安全的局部最优。

#### 16.5.2 用形式化验证来确认AI生成的代码是否正确
同样的道理，如果这样的通用工具可以存在，它必然早就存在很长时间了。软件测试的成本和软件开发的成本不相上下，甚至更高。如果有这样的工具，软件测试的成本会大大降低。事实上，形式化验证仅仅在最核心最关键的模块中可使用，因为其成本高昂，实施困难。

除了形式化定义本身很复杂，还有一个核心原因是形式化验证只能验证代码是否满足规约，但无法验证规约是否匹配真实世界的意图，比如你写错了规范定义，这是典型的语义鸿沟（Semantic Gap）。任何形式系统都无法自证其与外部现实的一致性。

形式化验证把错误从运行时推向了设计时，这意味着在程序运行的期望数学空间里进行状态的穷举搜索。组合爆炸使得任何非平凡系统的行为不可穷举。形式化验证只能覆盖关键路径，无法覆盖所有路径。

#### 16.5.3 人类审查AI代码，形成“人机协作”模式
这里存在双重问题。
1. 人成为瓶颈。人类开发者每分钟能阅读200-400行代码，而AI几秒生成数千行，审查速度永远跟不上产出速度。
2. 人的疏忽无法拦截AI的错误。这是自动化偏见（Automation Bias），使人类倾向于信任AI的输出，即使在看到明显错误证据时也倾向于忽略，审查逐渐退化为“仪式”。

更根本的是，AI系统的错误不可归因、不可精准修复、不可验证改善，因此没有学习闭环。我们无法从错误中系统性地学习并提升可靠性。

#### 16.5.4 通过尽可能多的测试确保正确性 
测试只能证明问题的存在，不能证明问题的不存在。传统的软件开发的测试行为，测试开发是专门的工作，测试case需要评审，测试执行更是依赖相关测试人员的直觉，常识来覆盖大量测试用例没有覆盖的场景。自动化测试仅仅是减轻了测试人员的重复劳动，从来就不可能代替测试人员。

我们依赖测试，因为我们信任测试的组织方式，测试团队，相关的人员，更依赖测试团队和开发团队的有效协助。如果测试由AI完成，这不比AI开发更令人信服；如果测试由人类完成，这是另一种不可持续的”人机协作“。 

#### 16.5.5 通过监控、行为分析来理解AI的行为模式，建立统计信心
这是最硬的障碍。
* 技术层面，模型是黑盒，千亿参数的神经网络无法被人类读懂；
* 统计层面，分布外永远存在，任何关于AI行为概率的陈述在未知场景下都失效；
* 认识论层面，如果场景本身无法被预先定义和枚举，那么“X%的概率”在逻辑上就是空的。我们无法确认AI生成的代码在哪些场景下、以多大概率出现非预期行为。AI的行为空间是不可穷举的

正是因为AI的不确定性导致了信任危机，不可能通过不确定性建立信任。

#### 16.5.6人类提供安全预制件，AI仅负责组合这些预制件。
这个想法看起来很美好。但是在复杂系统中，“局部”从来不是真正局部的。每一个看似局部的决策，都隐含着对全局结构的假设。一个AI生成的日志函数可能看似“非关键”，但如果它有安全漏洞，就可能被攻击者利用以击穿整个系统。关键性不是组件的固有属性，而是在特定上下文和失败模式中涌现的。因此，我们无法划定一条清晰的“AI可安全使用”的边界。

简言之，结构隔离无法消除局部与全局的耦合。

#### 16.5.7 既然AI无法保证可信任，转向问责
AI本身不是法律主体，不能被追责。一个选项是追责模型的使用者。这可以确保有人可以被追责，追责可能产生改善。这可能是最现实的选项，但是同样存在着上面说的博弈困境问题。

更进一步的是追责模型的开发者，这个效果更有限，因为他们自己也无法完全理解和控制模型行为。追责使用AI的组织虽然可能，但效果被效率诉求抵消。如果审查成本抵消了AI的效率优势，AI的价值就消失了。

传统问责的前提是因果链可追溯，AI生成代码的因果链从"需求→代码"直接跳过了"决策→设计→实现"的每个中间节点，所以归因在根上就断了。问责如果只有惩戒而无法改善，甚至无法验证改善是否真的改善了系统，这个问责其实是无效的。

问责是系统外的循环。无法对AI问责，这个循环就无法建立。

至此，我们不得不得给出一个结论：

> AI的不可知性是根本性的，结构性的、不可消除的，不可问责的。现有的生态和工具链上，不存在“让AI变得可信任”的技术或者组织捷径。

让我们回到开始，看看一个需求从提出到被交付，表现是什么，结构是什么，转换又在哪里。

### 16.6 软件开发中的表达、结构和转换
如果把软件开发阶段交付物看作是左端（需求）和右端（产品）的中间表现，我们可以清晰地画出如下完整的交付示例图：
```
需求 → 需求分析说明 → 概要设计 → 详细设计 → 代码 → 可执行程序 → 运行中程序 → 使用
    -------------非确定但可信-------------    ---------确定可信----------
```

其中每一个几点都是上一个阶段的转换结果，也是本阶段的完整表达。两个表达之间是转换，代码之前的表达都是非规范化的，所以这个转换是不确定的，但是软件工程保证了可靠；代码之后的表达都是确定性的，转换也是确定的，可信基于确定性派生。

AI介入的是“非确定但可信”部分。如果未来AI在软件开发中继续扮演角色（这是必然的），我们必然需要减少这段非确定的过程。

直观的想法的，人类完成到概要设计，甚至详细设计，然后让AI基于此生成。目前的AI开发趋向于这个办法，在提示词中尽可能包含详细的需求描述，在环境设置中尽可能喜欢技术和架构要求，确实有效地提升的产出代码的质量。然而因为从设计到代码的转换依然是不确定的，AI尽管表现提升了，但是信任度问题却并没有解决。

如果从需要到实现中间有一个有效的表达，既可以准确表达需求，又可以可靠地转化为代码，那无疑是最好的结果。

#### 16.6.1 需求的表达
从左端看，需求从分析到详细设计，完成了几个关键的工作：
1. 需求的澄清，分解和确认（分析阶段）
2. 映射为可靠的架构，定义系统边界（概要设计）
3. 分解为相互关联但是彼此隔离的模块（详细设计）

其中2和3都是技术密切相关的，体现为软件的架构模式和设计模式。但就需求而论，其实就是第一点。

需求一般分为几类：
- 结构性需求。不需要人类干预，判断的客观要求，包含流程，策略，约束等
- 非结构性需求。依赖和需要人类干预的主观性要求，包含交互，风格，视觉，可访问性等
- 伴生需求。由最终实现的结构和有效性保证，如性能，可靠性，可观察性等

非结构性需求是最适合AI生成的领域，典型的如web前端。它有一个核心的特征的，即便出错，也不会有灾难性的后果，是和人类的软适配层，人类的有足够的灵活度容忍简单错配。

伴生需求是实现结果的属性，这些需求反过来限制了实现的结构选择。我们可以把它们视为结构性需求的实现约束，是语义的一部分。这些需求实现有偏差，也基本不会影响结构性需求的正确性。

结构性需求是关键。任何结构性需求的失败直接导致整个需求基础的崩溃。这类需求往往是软件的内部状态，用户即便发现的差错，也毫无办法。这是我们关注的核心，AI的任何幻觉都可能导致流程失效，数据不一致甚至被破坏。

传统的需求规格说明中，结构性需求往往表现是流程图、交互图、状态机以及约束性质，这部分的精确表达是可能的，往往也对应着实现的基础框架。

#### 16.6.2 实现的表达
传统流程中，代码是可信转换的起点，我们总是可以从代码构建出完全一致的产品。代码也是开发阶段，一个不确定但是可信的流程，的终点。

代码的物理表现是多个源码文件。每个代码文件都是用某种编程语言书写的满足要求的结构化文本。在编译过程中，一般用一个等价的抽象语法树（AST）来表达其逻辑结构。

代码是一个多维信息的综合体。从图灵机的角度看，状态的转换规则是基础，外部输入驱动的代码的执行。以流行的web服务器为例，一个web服务的核心逻辑不外乎：
```
接收请求
处理处理
返回处理结果
```

然而即便是一个实现精巧的现实web服务器，如nginx，最新的版本至少有20万行之多。这么多的代码，除了核心逻辑的扩展以支持更多特性，其余大多都是辅助性的：
- 并行管理
- 协议支持
- 资源管理
- 错误处理
- 事务，会话，认证等跨界要求
- 日志、调试和可观察性支持
- 等等

其中没有一个是不重要的。任意一部分专门提取出来，都可以是一个专业的方向。然而在nginx中，它们围绕着web服务器的核心逻辑构造，整体成为一个高效，安全，轻巧的实现。

如此多的维度合并为一维的代码，组合状态是一个天文数字，这是软件难以编写，难以测试，难以验证的核心原因。在AI之前，人类需要根据知识、经验、直觉，从这些组合中找到一条实现之路。这个过程也反向塑造了我们的知识，经验和直觉。这被证明是最有效的。

对比需求的表达结果，可以看出，语义是需求和实现都需要关注的重点。我们把语义部分从代码中抽出来，单独作为需求的映射，然后把所有非语义的其他部分按需合并到语义中，就得到如下公式
```
程序 = 语义 + 附属构造
```

“程序 = 算法 + 数据结构”是Pascal语言之父维尔特提出的，它精准地指出了程序的两个核心构成：算法（处理问题的逻辑与步骤）和数据结构（数据的组织方式），这是代码的视角，是实现的视角，这是工程师的视角。

从需求确定性和工程信任的角度，我们自然得到了新的公式。语义代表了需求的核心，是人类绝对不会放弃决策的基线，而附属构造是满足语义要求的构造约束。

语义或者语义表达，可以是新的可信转换起点，这引出了我们的阶段性结论。
 
#### 16.6.3 针对AI优化的软件交付流程
```
需求 → 语义表达 → 代码 → 可执行程序 → 运行中程序 → 使用
    ---         -------------确定可信------------
     |
 不确定但可靠
```

这个结构中，代码不再是可信转换的起点，更左边的语义表达形式才是。我们把确定且可信转换的起点左移。

需求的澄清细化不再是需求分析说明书。需求的结构化和伴生部分，转变为语义表达；非结构化部分则继续按照原有的流程处理，这里AI可以发挥最大的作用。

语义表达是一种精确的中间表达，可以描述需求的语义部分，更可以直接对应到代码实现。相对于实现代码，语义表达的规模是多个数量级的减少。人类只需要审查语义表达的准确性，其余的都交给可靠的转换。

这就是我们预期的，应对软件危机2.0的范式。

定义语义表达的结构是困难的，然而这是值得做，也是必须做的工作。如果我们认为AI在未来的软件开发中占据主要位置，通过语义结构的表达交付可信是唯一的出路。

#### 16.6.4 语义表达作为交付的其他问题
语义表达仅仅表达了需求中最核心的语义部分，大量的程序构造部分并没有体现。这些如何和语义表达兼容并且支持合成呢？

核心语义部分，这个是序列的，如web服务器的核心逻辑。这个逻辑的每一步可以不断地无限细化，如处理请求可以细分为针对不同的协议，实现不用的处理，这都是核心逻辑。核心逻辑的细化和完善，最后是一棵树。对这棵树的深度优先遍历，就是核心逻辑的执行。

基于此，有两个基础的思路。
1. 把非语义构造作为预定义的属性附加到语义节点，强化语义要求，合成器负责语义合成
2. 关键的外部依赖作为语义原子，只能出现在叶子几点，其可靠性有外部保证，如系统调用之类

这种模式下，软件开发就转变为从需求到语义表达的转换，以及根据需求配置不同的语义属性，复用或者新开发新的语义原子。

#### 16.6.5 核心语义作为开发交付物的优点
使用核心语义（表现一棵树）作为开发交付物的好处有：
1. 核心语义是相对简单且稳定的。和最终的代码相比，比例可能只有极小部分。这种规模上的极大压缩为有效的审查提供了可能。
2. 核心语义是可控的。人类对软件的控制体现在核心语义的控制上，只要核心语义正确，最终的合成实现就必然正确，这个由软件合成器保证。
3. 核心语义的可扩展的。核心语义的细化本身就是对人类思维模式的模拟，非常适合不断深入细化。核心语义结构不涉及任何的技术，可以是客户和开发人员共同认可的标准产品文档
4. 核心语义是可模块化的。单个节点相对都是独立的，仅仅依赖于上一个节点的输入作为其输出，这完全消除了潜在的全局依赖导致的复杂困难。部分附加属性，如事务属性，可能把几个相邻的步骤框在一起，划定事务边界。这种要求的显式的，也是局部的，非常便于处理。
5. 核心语义的树状结构把所有的依赖显式化了。一个节点的唯一输入是上一个节点的输出，没有任何其他依赖。这使得最终的代码看起来可能繁复，但这不是问题。谁会抱怨编译器生成的二进制代码难以阅读呢？
6. 核心语义使得有效的优化成为可能。核心语义保留了满足需求的所有结构。默认是串行的。 但是如果一个节点使用的原子语义都是可并行的，该节点的执行自然可以并行化。同时如果想要为了效率并行化一个操作，则其子节点都必须要可以并行，不满足要求的自然被标识为合成失败，避免了问题。这种模式为优化提供了极致的可能。

### 16.7 从“信任-追责”到“生成-验证”模式的转变
人类协作本质上是“信任-追责”架构，然后通过纪律和规则来强化；对于AI来说，不存在信任，更谈不上追责，只有生成和验证。其中， 生成的确定性更胜过验证的有效性，因为验证的成本非常高。

通过分析软件开发各个阶段的交付产物，我们发现把代码作为交付物是关键。进一步的分析建议，一个更靠前的，抽象的语义表达可能更适合作为交付物。通过缩短“信任-追责”链，加长“生成-验证”链，我们既可以给人类的需求细化和确认留足空间，同时可以让确定性的转换满足交付信任。这是开发范式的转变。

未来的软件不再是开发出来，而是在人类和AI的协作下，像一颗树一样“生长”出来。感谢AI，我们的迭代速度从来不曾像现在这么快！

### 16.8 本章小结
AI时代的软件危机不同于第一次软件危机。第一次危机是因为需求太多，无法按时、在成本可控的前提下交付软件，但我们可以投入足够的人力去测试、验证和分解。AI带来的危机恰恰相反：我们可以近乎零成本地生成软件，却不敢信任它。软件太多了！

这直接对传统的软件开发模式和工具链提出了根本挑战。传统开发中，从需求到代码经过层层转换，可信度正是在这一过程中被逐步构建和确认的。如果这整段转换被AI完全接管，而AI的输出又存在结构性的信任缺陷，我们唯一的出路就是收缩这段转换的范围。让AI只覆盖一小部分，之后全部交由确定性转换负责，像编译器一样可靠、稳定。

这意味着，我们需要在从需求到代码的路径上找到一个分界点。这个点之前，AI协助人类处理；这个点之后，全部由确定性转换覆盖。这是转换范式的改变，也是以"代码作为交付物"这一传统开发结构的根本重构。

这是交付物形态（表现）的左移，是开发结构的革新，更是转换模式的升级。

新的、适合AI时代的可信软件构造方式，必然会从这个结构中生长出来。
