## 第九章 数据系统：规模化数据管理的结构
数据库是专门的数据管理和转换系统，用于处理持久化、结构化，规模化数据。持久化指的是一般存储与外部存储，如磁盘；结构化指的是数据有显然的一致结构，这也是大规模处理的前提；规模化则指的是数据的量级远远超过计算机内存容量，达到TB，甚至PB级。

如果说操作系统是硬件资源的虚拟化层，那么数据库就是数据的结构化层。它将磁盘上零散的字节组织为可查询、可事务、可并发的逻辑模型。

数据库管理系统是将声明式查询转换为高效执行计划的编译器，同时提供了隔离性与持久性的安全网。

### 9.1 数据的表达和逻辑结构
数据如果没有有效的表达，就是毫无意义的二进制流。数据的表达结构完全由我们需要表达的含义确定。一方面，我们想尽可能直观地表达数据的现实含义，另一方面，我们也希望这个表达可以能被高效地处理。这是两个完全不同的目标，也是在设计数据表达是必需要考虑的问题。

然而我们倾向于认为，有效地表达数据的现实含义往往比更高效地处理更重要。这并不是说更高效的处理不重要，而是把逻辑表达和数据实现分层来处理，这样在前者基本不变的情况下，后者可以随着技术和硬件的进步不断优化。这种分层的思想是在处理各类问题中不断出现，是最有效的隔离复杂性的手段之一。     

#### 9.1.1 数据记录
单个字段可以表达单一的信息，有预定义的类型（整数、字符串、日期等），数据记录则是一组字段的集合，是数据处理的基础单元。

数据记录可以是定长的，也可能是变长的。通过设定单个字段的最大长度，或者外置变长字段的存储，我们总是可以把变长记录转变为定长的记录。定长记录的最大好处是，我们可以通过计算直接确定任何一条记录的位置。

把数据记录直接存储在文件中，我们得到记录文件。

#### 9.1.2 记录文件
文件是操作系统视角的持久化单元，表示一个长度明确的，连续的，可以按字节寻址的字节流。文件自然可以作为数据记录的容器。记录文件是最早被普遍使用的数据库。

虽然记录之间可能有关系，但是记录文件一般不维护这个关系，它仅仅是一些列的记录的最原始的排列。要精确表达记录之间的关系，我们需要关系表格。

#### 9.1.3 关系表格
我们最常见的关系表格就是Excel中的电子表格。这是一个二维的结构，横向的表示一条具体的记录，纵向的则是记录的每个字段。这个结构看起来简单，理论上却可以表达所有的数据关系。这是1970年E.F. Codd的经典论文的结论。这里，关系被定义为实体之间的集合，及关系表。一个关系表包含：
- **元组（行）**：实体的一个实例 
- **属性（列）**：实体的某个特征 
- **域**：每个属性的取值范围

每个实体都有唯一的ID，叫做键；关系表之间，通过键相互关联。 

基于关系表，可以定义如下基础的关系操作：

| 操作 | 符号 | 描述 | SQL对应 |
|------|------|------|---------| 
| 选择 | σ | 筛选行 | WHERE | 
| 投影 | π | 筛选列 | SELECT |
| 连接 | ⨝ | 合并两个表 | JOIN | 
| 并 | ∪ | 合并两个结果集 | UNION | 
| 差 | − | 行差集 | EXCEPT | 
| 笛卡尔积 | × | 所有行组合 | CROSS JOIN |

当表述复杂的连接关系，如树或者图，关系表格有点力不从心，处理起来的复杂，直接表达这种数据结构的是图结构。 

#### 9.1.4 图结构
当数据之间具有多对多、多层级、递归的关系时，表格的外键连接往往需要昂贵的连接操作；而图结构将关系显式存储为边，允许在常数时间内沿边跳转。常见的图数据模型包括属性图（顶点+边+键值属性）和RDF三元组。连接图的逻辑结构使得图遍历算法（如最短路径、PageRank）更有效。然而图在大规模下的分片极其困难，切割一个高度互联的图必然破坏局部性，导致分布式图查询频繁跨越网络。

#### 9.1.5 数据流
数据流是记录的时间序列。流不同于静态数据集：它无界、持续到达，且通常以追加方式写入。流的逻辑结构强调顺序与时间戳，每个记录都携带隐式或显式的时序信息。流处理系统将流视为一等公民，支持窗口、水位线、乱序处理等操作。然而，流本身不提供随机访问能力，一旦记录流过窗口，若要重新查询，必须依赖外部持久化存储或状态后端。

所有这些数据的表达结构中，使用最多，最成熟的就是关系表。不论使用什么结构表达，持久化是数据库的核心能力，这就涉及到如何存储。  

### 9.2 存储的结构
数据的逻辑结构决定了用户视角的数据模型，而存储结构则决定了数据在磁盘或内存中的物理布局。存储结构的选择直接决定了读写性能、空间效率和可靠性。

有序存储使得我们可以使用二分查找在O(log N)时间内定位记录，同时范围扫描也只需按顺序读取即可。所以我们在组织数据的时候，如何能保持有序，就可以到后续使用数据的时候获得极大的好处。

#### 有序结构
有序结构中，数据按某个键全局排序后连续存储，常见的排序字符串表（SSTable）或有序平面文件。有序结构使得范围扫描极为高效，数据在磁盘上物理相邻，一次预读可以加载大量连续记录。

维护全局有序性需要付出巨大代价，随机插入必然触发数据重排。LSM树（Log-Structured Merge-Tree）通过延迟排序来解决这一矛盾。其机制是：
1. 新写入先进入内存中的有序结构（MemTable）
2. 达到阈值后刷入磁盘成为不可变的SSTable
3. 后台进程定期合并多个SSTable以消除重复键并维护总顺序

这种分代有序的结构大幅度提升了写入吞吐，代价是读取可能需要查找多个SSTable，需要使用类似布隆过滤器（Bloom Filter）来避免磁盘读。

#### 树结构
文件系统使用树结构（B树及其变体）来存储文件内容，事实上这也可以用于数据记录的存储。B+树将所有数据记录存储在叶子节点，内部节点仅存放键和子节点指针。每个节点通常对应一个磁盘页（4KB~16KB），树的高度控制在3~4层，使得任何点查询只需读取3~4个页。

树结构天然支持范围查询。从最小的键开始，沿叶子链表顺序扫描即可。

B+树的写入代价高。插入或更新可能导致节点分裂、页重组，随机写操作会触发多次磁盘I/O。为缓解此问题，现代数据库引入写缓冲（buffer pool）和预写日志（WAL），但本质并未改变树结构的随机写特性。

#### 日志结构
日志结构（log-structured）将所有写入操作追加到文件末尾，永不原地更新。这种结构将随机写转化为顺序写，在磁盘时代曾大幅提升写入性能（磁头无需寻道）。日志文件系统（如LogFS）和消息队列的底层存储均采用此结构。

日志结构的问题在于读取，由于旧版本数据未被删除，需要额外的清理机制（compaction）来回收空间。此外，日志不维护任何索引，若想快速定位某条记录，必须构建外部映射（如偏移量表）。

LSM就是将日志结构与有序结构结合。

#### 行结构和列结构
行存储将一条记录的所有字段连续存放。这种布局对于点查询（取整行）和更新操作非常友好，一次I/O即可获取完整记录。但一个实体的属性数较少时，这个非常有优势，这是在线事务数据库（OLTP）最常用的模式。

但是在线分析数据库（OLAP）的情况大大不同。一张表往往有上百列甚至更多，而典型的分析查询只需读取其中的少数几列。这种情况下，列式存储就非常有用。

列存储则将每列的数据连续存放在独立的文件或文件段中。对于查询 `SELECT AVG(salary) FROM employees`，列存储只需读取salary列的连续区域，完全忽略其他列。此外，同一列的数据类型一致，便于使用轻量级压缩（字典编码、游程编码、位打包），压缩率远高于行存储。在数据仓库场景中，列存储的压缩率通常可达5-10倍，显著减少I/O成本。

行存储和列存储并不是对立的，现在数据库系统为了同时支持事务和分析需求，试图组合这两种格式：
- 同时行存和列存。查询性能最好，但是存储膨胀和写放大明显
- 智能混合存储。主要是行存，副本使用列存。列存策略也非常丰富：部分列，最近数据等。这是主流的方式
- 行存+内存中维护列格式。

### 9.3 索引结构
如果说有序使得查找更加有效，索引则是专门为加速查找而设计。索引直接把键映射到记录位置，是数据的辅助的，也更高效的访问路径。B+树是常用的索引结构，此外还有：
- 哈希索引。适用于等值查询，时间复杂度O(1)。哈希索引无法支持范围查询，且在大量冲突时性能退化严重
- 位图索引。用于低基数（有效值范围小，如性别之类）的属性。为每个取值创建一个位图，位向量的长度等于数据行数，第i位为1表示该行取此值。位图索引支持快速的位运算组合（AND、OR、NOT），尤其适合复杂布尔查询
- 跳表索引，一种概率性的有序结构，加速链表的访问

#### 9.3.1 倒排索引结构
如果说索引是从ID到内容的映射，倒排索引就是从内容到ID的映射。这是搜索引擎最常用的索引结构，它将文档中的每个词项（term）映射到包含该词项的文档ID列表（posting list）。其构建过程为：
1. 词项提取。从文档中提取独立词，归一化处理等
2. 倒排列表构建

倒排索引能够支持短语查询、布尔查询（词项AND/OR）和模糊匹配（编辑距离）。

### 9.4 可靠性结构
可靠性是指系统在面对硬件故障、软件错误或环境异常时，仍能保证数据不丢失且可访问。可靠性结构从两个维度构建。

#### 9.4.1 硬件可靠性结构
硬件的可靠性在不同的层面都有体现，对于上层软件透明或者半透明。

**固件层**
磁盘固件在每个扇区末尾存储ECC校验码，确保扇区的数据可靠。用于检测磁头读取错误。

**设备层**
RAID是设备层的故障盘冗余技术。RAID卡通过校验盘（奇偶校验或Reed-Solomon码）容忍单个或多个磁盘故障。

硬件可靠性特性往往需要软件的配合，可以极大降低可靠性构建的负责性和成本。

#### 9.4.2 软件可靠性架构
软件，如数据库系统，也可以在硬件之上增加自己的校验层。常见手段包括：
- 存储校验和（checksum），写入时计算，读取时验证，若不一致则从副本恢复。
- 预写日志（WAL）中的每条记录包含CRC32校验，确保日志本身未被截断或篡改。
- 定期执行“数据清淤”（scrubbing）。后台线程遍历所有存储页，验证校验和并修复静默损坏。

**副本**
冗余是最直接的可靠性结构。数据被复制到多个节点（通常是3个或以上奇数个，便于可靠性投票），每个副本独立存储一份完整数据。副本提高了读吞吐（可并发读多个副本），同时容忍节点宕机（只要剩余副本数量足够）。

传统的大数据存储如HDFS，副本冗余的存储开销是3倍。纠删码（Erasure Coding）可以在相似的容错能力下将存储开销降低到约1.5倍，只需要一点需要额外的计算开销用于编解码。

副本的第一个问题是写放大，每次写入必须同步或异步地传播到所有副本；第二个问题是一致性，同步复制保证不丢失数据但增加延迟，异步复制降低延迟但可能丢失最新写入。分布式共识算法（Paxos、Raft）在支持异步复制的基础上，通过协商多数派来保证副本间的强一致性。

### 9.5 可扩展性结构
单个节点的存储或计算能力有限，但是数据累计无穷尽。为了支持更大的数据集，系统可以纵向发展（更强的CPU，更大的硬盘），也可以横向扩展（更多的机器）。可扩展性结构一般指横向扩展，将数据分散到多个节点，同时尽可能保持上层透明的单一系统映像。

**分区**
分区（sharding 或 partitioning）将完整数据集按某种规则划分为不相交的子集，每个子集（分区）存放在不同的节点上。常见的分区策略包括：
- 范围分区。按键值范围划分，如 `[A-M]` 到 `[N-Z]`。优点：支持高效的范围扫描；缺点：存在热点问题（如新数据集中在某个范围）。
- 哈希分区。对键计算哈希值，再按哈希值模节点数映射。优点：数据分布均匀，避免热点；缺点：范围查询退化为全分区扫描。
- 列表分区。按离散值列表划分，如将“北京、上海”放入一个分区。适用于地域或类别维度的天然隔离。

分区键的选择至关重要。若查询包含分区键的等值条件，路由器可以直接将请求转发到单个分区；若缺少分区键，则必须广播到所有分区，然后合并结果。这种“广播查询”是分区结构的主要代价。

**一致性散列**
一致性散列是一种特殊的哈希分区变体。传统的哈希分区在节点数量变化时（扩容或故障）会导致几乎所有键重新映射，引发大规模数据迁移。一致性散列将哈希值空间组织成一个环，每个节点负责环上的一段区间；节点加入或退出时，仅影响其相邻节点的映射范围，需要重新迁移的数据量约为1/N。

此外，一致性散列引入虚拟节点。每个物理节点对应多个虚拟节点散列在环上，从而平衡负载（避免节点性能差异导致的不均）。

可扩展性架构是自然的分布式，这带来一系列严重的挑战。

### 9.6 分布式结构的挑战
将单机结构扩展为分布式结构，虽然获得了可扩展性，但也引入了单机系统中不存在的根本性挑战。

#### 9.6.1 部分失败
在单机环境中，函数调用要么成功返回结果，要么抛出异常，系统状态是确定性的。在分布式系统中，网络是异步的不可靠介质：向远程节点发送一个请求后，可能发生多种情况：请求丢失、远程节点处理成功但响应丢失、远程节点宕机、远程节点过慢导致超时、时钟漂移导致混乱。这些“部分失败”意味着调用者无法区分“对方尚未处理”和“对方已处理但我不确定”。

分布式数据库必须通过超时、重试、幂等操作和共识协议来掩盖部分失败，但其根本不确定性无法消除，这就是著名的“分布式系统不可能区分故障与慢速”问题。

部分失败会导致一个令人尴尬的问题，网络隔断。比如一个分布式系统有6个节点，因为网络原因，6个节点分为两个隔断的网络 ，每边3个节点。3个节点内部彼此相通，但是两个网络不通。这种现象叫做脑裂。这种现象是不可避免地，必须想办法解决。

CAP定理说的是，对于一个分布式系统，一致性（Consistency）、可用性（Availability）和分区容错性（Partition Tolerance）三者不可兼得。由于网络分区（P）在实践中必然会发生，系统必须在C和A之间做出权衡：要么优先保证一致性（CP系统），要么优先保证可用性（AP系统）。

CP强调的是数据的一致性，数据安全，如账单，库存，余额等。所以那些不能容忍数据不一致的关键数据系统都是CP风格的，HBase，MongoDB，Google Spanner，TiDB，zookeeper等

AP强调的是服务的可用性。数据可以临时不一致，但是服务更重要，如日志存储，UGC内容，社交网络信息，推荐系统等。常见的有Amazon DynamoDB，CouchDB等。

很多系统是可配置的，可以恰当配置以满足不同的使用场景。

#### 9.6.2 乱序
分布式系统中，时钟飘逸或者消息经过不同的网络路径导致，都会导致接收顺序与发送顺序的不一致。对于依赖顺序的操作（如递增主键、写后读一致性），乱序会破坏逻辑正确性。

最常见的办法是引入逻辑时钟（Lamport时间戳、向量时钟）或物理时钟（TrueTime、HLC）来为事件建立偏序关系。然而，跨节点的全局排序成本极高，它要求所有节点就某个顺序达成共识，相当于在每次操作时运行一次共识算法。

大多数分布式数据库牺牲了全局顺序，只保证“因果一致性”（如果A发生在B之前，那么所有节点看到的顺序都是A先于B），而允许并发事件在节点间出现顺序分歧。

#### 9.6.3 原子性和事务
数据库领域的原子性强调是一个行为“全做或全不做”的能力，不暴露中间状态。单机数据库通过日志（undo/redo）和锁机制实现原子性：要么事务的所有修改持久化，要么全部回滚。

在分布式环境下，一个事务可能涉及多个节点上的多个分区。原子性要求提交协议（如两阶段提交，2PC）协调所有参与节点：
1. 第一阶段所有节点准备（持久化undo/redo日志并返回“准备好”）
2. 第二阶段协调者发出提交或中止决策。

2PC的致命缺陷是阻塞性。若协调者在发出提交决策后故障，参与者可能长时间阻塞。三阶段提交（3PC）和Paxos提交缓解了阻塞问题，但增加了复杂度和延迟。

分布式系统的全局事务能力需要很高的代价，所有不少分布式数据库为了性能放弃了跨分区事务，转而支持“最终一致性”或“单分片事务”，将跨分片一致性责任推给应用层。

### 9.7 查询的结构
查询是用户与数据库交互的主要方式。查询的结构决定了用户表达意图的便利性，也决定了系统能够接受的优化程度。

#### 9.7.1 查询的表达
不同的存储结构对应了不同的查询表达。

##### 9.7.1.1 表查询
SQL是表查询的标准语言。SQL的声明式特征允许用户描述“想要什么”而非“如何获取”，查询优化器负责生成执行计划。SQL的核心结构包括：SELECT子句（投影）、FROM子句（数据源）、WHERE子句（选择）、GROUP BY（分组）、HAVING（分组后选择）、ORDER BY（排序）和LIMIT（限制输出）。子查询和公共表表达式（CTE）提供了嵌套的组合能力。

SQL的表达力还有一个数学上的美，查询的输出仍然是一张表。这意味可以使用SQL组织非常复杂的查询，或者把复杂的分解为子查询来处理。

不过SQL在处理递归时显得笨拙，需要通过递归CTE模拟。

##### 9.7.1.2 图查询
图查询语言如Cypher、Gremlin、SPARQL，以图模式匹配为核心。例如Cypher查询 `MATCH (a:Person)-[:FRIENDS_WITH]->(b:Person) WHERE a.name = 'Alice' RETURN b.name` 直接表达了沿边遍历的意图。图查询更适合路径探索、社区发现、推荐系统等场景。

图查询优化器需要处理“无界遍历”问题。如果没有限制条件，一个查询可能遍历整个图！

##### 9.7.1.3 即席查询（Ad-hoc）
即席查询指用户临时提出的、未预定义的查询。在传统数据库中，即席查询依赖成熟的SQL引擎；在大数据系统中（如数据湖、日志分析平台），即席查询往往通过交互式工具（如Jupyter、Superset）或即席命令行（如`grep`、`awk`）完成。现代微服务模式下，Restful风格的API是查询的主要手段。

即席查询的挑战在于，没有预建索引，系统必须在执行时动态决定访问路径。一些系统采用“schema-on-read”策略，仅在查询时解析数据格式（如Parquet、Avro），代价是每次查询都包含解析开销。

#### 9.7.2 精确搜索
精确搜索要求返回的结果与查询条件完全匹配。其核心是快速定位存在性或等值。
- ID检索。根据主键或唯一标识符直接定位记录。在B+树或哈希索引支持下，ID检索的时间复杂度为O(log N)或O(1)。规模化挑战在于：当ID空间稀疏（如UUID）时，索引的填充因子低，空间膨胀严重
- 位置检索。基于空间坐标或地理范围的精确匹配，如“查询经度116.40、纬度39.90处的POI”。位置检索通常使用空间索引（R树、Geohash、四叉树），将二维空间降维到一维编码后，再借助B树或LSM树存储。位置检索的精度受限于编码粒度
- 内容检索。基于字段的内容匹配。内容较短时，全文匹配就可以；内容很长时，对内容做散列计算，然后匹配散列值，是空间换时间的典型操作

#### 9.7.3 模糊搜索
模糊搜索允许查询条件与数据之间存在一定差异，通常用于处理用户输入错误、拼写变体或未知精确值。

通配搜索（wildcard search）使用`*`、`?`等通配符来描述模糊的请求，如`LIKE 'a%'`。这种语法本身非常精确，依赖于用户对数据的理解。有些引擎更支持复杂的数据查询，如JSON结构的数据或者使用regex表达更强大的匹配能力。

模糊搜索的实现方式有：
* 扫描全表并应用模式匹配（O(N)）
* 使用Trie树或后缀树索引（内存密集）
* 将通配符模式转化为多个精确词项的并集（例如`a*`转换为所有以a开头的词项，然后并集倒排列表）
 
通配搜索的规模化挑战在于，前缀通配（`%abc`）需要反向索引（默认的索引都是正向的）。但是如果是类似`%abc%`，这几乎无法使用任何传统索引。优化这类问题需要在应用层更深入分析，识别更多的结构模式以有效利用索引。

#### 9.7.4 相似搜索
和模糊搜索不同，相似搜索指的是查询与输入在在某个度量（欧几里得距离、余弦相似度、点积）上接近的结果。向量检索是向量数据库（如Milvus、Qdrant、Pinecone）的核心能力，在AI时代尤其重要，是RAG的核心。

向量是一个固定维度的浮点数组。向量检索的核心问题是在高维空间中快速寻找最近邻。精确的最近邻搜索（如穷举计算）在大规模下不可行，因此近似最近邻（ANN）算法被广泛采用，包括几种不同的处理思路：
- 把高维信息投影到低维然后检索。这是基于哈希的LSH。使用局部敏感哈希将相似向量映射到相同桶中
- 把高维分区然后遍历。这是基于图的HNSW。构建层次化可导航小世界图，在图中贪婪搜索逼近最近邻
- 预计算然后匹配。基于乘积量化（PQ）。将向量分割为子向量，分别量化编码，压缩存储的同时支持距离近似计算

向量检索的规模化挑战在于，构建索引的时间复杂度超高（HNSW需要O(N log N)的图构建）、内存占用（亿级向量的图可能消耗数百GB）、以及混合查询（向量相似+标量过滤）的执行效率。

### 9.8 结构的转换
查询语言负责表达要什么，而查询执行器（Execution Engine）则负责真正完成怎么做。它负责将抽象的查询计划转化为真实的动作。

执行器是数据库中最容易被忽视，却最接近“计算本质”的部分。因为数据库并不仅仅是在存储数据，它本质上是在执行一种特殊的数据转换计算。其 关键流程如下

```text
输入记录流
    ↓
过滤（Filter）
    ↓
连接（Join）
    ↓
聚合（Aggregate）
    ↓
排序（Sort）
    ↓
输出结果
```

整个查询，本质上是一条由多个算子（Operator）组成的数据流图。

#### 9.8.1 算子模型
执行器将查询拆解为多个基础算子，每个算子完成一种局部的数据转换。最常见的算子包括：

|算子|功能|
|---|---|
|Scan|扫描表或索引|
|Filter|条件过滤|
|Project|列裁剪|
|Join|表连接|
|Aggregate|聚合|
|Sort|排序|
|Limit|限制输出|
|Shuffle|数据重分布|

例如：

```sql
SELECT name
FROM employee
WHERE age > 30
ORDER BY salary
LIMIT 10
```

可能被转换为：

```text
Limit
  ↓
Sort(salary)
  ↓
Project(name)
  ↓
Filter(age > 30)
  ↓
TableScan(employee)
```

每个算子只关注局部转换，而整个执行计划则是算子的组合。这种设计使数据库执行器具有类似Unix管道的结构化组合能力。

执行器有不同的执行模式。  

##### 9.8.1.1 拉取式执行（Volcano模型）
传统数据库广泛采用 Volcano 模型，也称 iterator 模型。每个算子都实现统一接口：

```text
open()
next()
close()
```

其核心思想是，父节点向子节点拉数据。

例如：

```text
Sort
  ↓
Filter
  ↓
Scan
```

执行流程：
1. Sort调用Filter.next()
2. Filter调用Scan.next()
3. Scan返回一条记录
4. Filter判断是否满足条件
5. 返回给Sort

整个系统像递归生成器。Volcano模型最大的优点是，结构统一， 算子高度可组合，且易于扩展。几乎所有传统关系数据库（PostgreSQL、MySQL早期版本、Oracle传统执行器）都使用这一模型。

但它也存在严重问题：
- 每条记录都需要函数调用
- 虚函数分发成本高
- CPU缓存局部性差
- 难以利用SIMD向量化

在数据量极大时，函数调用本身会成为性能瓶颈。

##### 9.8.1.2 向量化执行
向量化执行是使用批处理替代逐行处理，一次处理一批数据，而不是一条数据。

传统模型：
```text
for row in rows:
    if row.age > 30:
        output(row)
```

向量化模型：
```text
mask = age_column > 30
output = rows[mask]
```

这里，数据不再以“记录对象”的形式流动，而是以列向量（column vector）的形式批量处理。这种结构有几个巨大优势：
- CPU缓存命中率高
- SIMD可并行处理多个值
- 减少函数调用
- 更适合列存储

向量化执行本质上是将数据库执行器从解释器转变为数据并行计算引擎。现代分析型数据库（DuckDB、ClickHouse、VectorWise）广泛采用这一模式。

##### 9.8.1.3 流水线执行
如果每个算子都完整生成中间结果再传递，会导致大量内存占用，频繁磁盘落盘，CPU等待等影响效率的结果。因此现代执行器倾向于采用流水线（pipeline）执行，数据一边扫描，一边过滤，一边聚合，基本没有临时结果。

流水线的本质是将多个转换融合为一个连续的数据流。这与现代CPU流水线、流处理系统、GPU kernel fusion本质一致。

但并不是所有算子都支持流水线。Sort必须看到全部输入，Hash Join通常需要先构建哈希表，Global Aggregate需要全局状态等，这些都是阻塞算子（blocking operator）。阻塞算子会切断流水线，使系统产生阶段边界。

因此，一个查询计划实际上是多个流水线+ 少量阻塞边界组成的执行图。

#### 9.8.2 联合（Join）
关系数据库中最昂贵、最复杂的算子通常是联合，这是数据库真正的核心算子。联合本质上是在两个数据集之间寻找匹配关系。不同的联合算法适用于不同场景。
1. 嵌套循环联合（Nested Loop Join）最直观，两重循环匹配。 复杂度为$O(N \times M)$，适合小数据集
2. 排序归并联合（Sort-Merge Join）要求两边有序，这样像可以像归并排序一样同步推进
3. 哈希连接（Hash Join）则是现代数据库最常见的联合方式，复杂度接近$O(N + M)$，其本质上是用空间换连接查找时间

现代查询优化器的重要任务之一，就是选择最适合的联合算法。

#### 9.8.3 查询执行
优化器负责生成具体的物理计划，执行器负责真正执行。同一个SQL可能存在多种JOIN方案，需要优化器需要根据数据表的大小，数据的分布，索引情况，内存大小等特征，然后选择代价最小的执行路径。

在分布式数据库中，执行器进一步演化。一个查询计划会被拆分到多个节点。局部节点执行扫描，过来，局部聚合等操作，然后把结果汇总到一起做进一步的处理直到结果。

这种情况下，网络也成为执行器的一部分。网络拓扑，节点速度，数据分布密度等都会直接影响查询性能。

现代分布式数据库已经不再是传统数据库，更像是大规模分布式数据流执行引擎。

#### 9.8.4 执行器的本质
数据库的发展史，在某种意义上，就是执行器不断演化的历史。从逐行解释执行，向量化，JIT编译，GPU一直到分布式数据流，其核心目标始终不变：
1. 让数据尽可能少移动，  
2. 让CPU尽可能持续工作，  
3. 让转换尽可能接近硬件极限。

### 9.9 规模数据处理的模式
规模化数据管理不仅涉及存储和查询结构，还形成了一系列经过实践检验的处理模式。这些模式在不同系统间具有共性，构成了分布式数据处理的设计范式。

**分片-聚合（Scatter-Gather）**
查询被分发到所有数据分片，每个分片独立执行局部计算，结果返回给协调者进行聚合。该模式适用于没有分片键过滤的查询，也是大多数分布式数据库的兜底策略。其代价是“木桶效应“，整体响应时间受限于最慢的分片。

**MapReduce及其变体**
Map阶段将输入分割为独立的块，在数据本地执行用户定义的映射函数，产生中间键值对；Shuffle阶段按键对中间数据进行全局排序和分组；Reduce阶段对每个分组执行归约函数。MapReduce使得大规模并行编程成为可能。如果其中间结果需要反复落盘，整体延迟会比较大。

现代系统（Spark、Flink）采用有向无环图（DAG）和内存计算，效能大大提升，内部还是保留了相同的分治哲学。

**增量与流式处理**
海量数据执行全量计算是非常耗时的。对于持续到达的数据，批处理模式无法满足低延迟要求。流式处理模式将计算抽象为持续运行的数据流图，每个算子处理一条记录并立即向下游传递。窗口（滚动窗口、滑动窗口、会话窗口）用于将无限流切分为有限数据集进行聚合。

流式处理的核心挑战是状态管理，算子需要维护跨窗口的中间状态（如计数器、布隆过滤器），而状态的规模同样可能超越内存。

**分层存储与生命周期管理**
规模化数据往往具有明显的时效性，最新数据被频繁查询和修改，历史数据访问频率急剧下降。分层存储模式根据数据年龄或热度将其放置在不同的存储介质上。热数据放在NVMe SSD或内存中，温数据放在SATA SSD或普通磁盘，冷数据迁移到对象存储（如S3）甚至磁带库等。

透明分层要求系统在查询时自动合并跨层数据，并对用户隐藏物理位置。这一模式的核心结构是“生命周期策略表”，由用户定义规则（如“超过30天的数据迁移到冷存储”）或者使用启发式策略 。

**并发与冲突解决**
在高规模下，并发和冲突是常态。锁是保证并发访问正确的基础机制，但是对于分布式系统来说，所往往代价太高而不适用。常见的处理策略是
- 数据设计，尽可能缩小一致性域。
- 优先使用乐观并发控制。这样允许事务无阻塞执行，仅在提交时检查是否与其他事务产生冲突。绝大部分情况下，性能会优于悲观锁 
- 在一致性要求之间微调。如果因果一致性可以满足要求，就不需要序列一致性

### 9.10 本章小结
本章从数据的表达和逻辑结构出发，沿着存储结构、分布式挑战、查询结构、规模化处理模式的脉络，剖析了数据库系统作为数据结构化层的本质以及其上的转换能力。

核心结论可以归结为三点：
第一，结构即权衡。流式文件的自由、数据表的规整、分布式存储的可扩展性，每种结构都针对特定规模化问题而生，但无一例外地引入了新的代价，写放大、读放大、网络开销、一致性摩擦等。没有最好的结构，只有合适的匹配。

第二，查询转换的本质是结构的反射。数据库优化器将声明式SQL转化为执行计划的过程，本质上是在存储结构的物理特性（索引、分区、排序）上执行一种反射计算。优化器的能力极限，决定了结构化所能兑现的性能收益。在分布式规模下，这一反射必须包含网络拓扑和节点行为的不确定性，将其固化为分布式计划。一个查询的性能上限取决于数据的结构。

第三，规模化的终极敌人不是数据量，而是结构熵。随着系统演进，为了管理规模化而引入的各种结构（索引、日志、副本、分区、一致性协议）相互交织，形成复杂的依赖网络。局部变更（如增加一个索引）可能通过优化器、事务管理器、存储引擎的反馈环路放大为全局行为变化。控制结构熵，要求系统设计者始终保持一种元认知，清醒地看到每一层结构所使用的假设（数据分布均匀、节点同构、网络可靠），并在假设失效时提供降级或自适应机制。

数据库系统的发展史，就是人类与数据规模之间的博弈史。每一代结构化方案都曾是当时的巅峰，又在更大规模面前暴露出脆弱。本章所讨论的结构——从记录到图、从B树到LSM、从2PC到共识、从SQL到向量检索——都是这场博弈中的阶段性结晶。理解它们的原理、代价和适用范围，远比记住某个具体数据库的用法更为重要。在数据规模持续增长的情况下保持查询的效率和管理的可控性，答案总是以新的结构出现。
