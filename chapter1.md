## 第一章 表达之根：从比特到信息
一切计算皆始于“表达”。所谓表达，就是将现实世界的信息转换为符号系统，使其能够被存储、传输和计算。“表达”是计算机科学的起点，也是贯穿全书的一条主线。

比特是计算世界的原子，0和1两种可区分状态的最小表达单元。选择二进制并非偶然，双稳态物理器件最容易实现，真值逻辑也天然契合二值代数。比特本身没有意义，意义由解释框架赋予。

所有的表达从数字的表达开始。

### 1.1 数字的表达
数字，特别是整数，的二进制表达极为自然。一个比特表示一个二进制位，八个比特表示一个字节。二进制的表达往往对齐到字节的整数倍。流行的CPU都是64位的，这意味着该CPU可以直接计算8字节的数字，这个数字相当大。

字节可以表示无符号整数（0–255），或有符号整数（通常使用二进制补码，范围为 -128 到 127）。当多字节表示整数时，字节的排列顺序也有关系。假设内存是一个从左到右的有地址标号的格子，高位优先排列（Big-Endian，大端序）将最高有效字节存放在最低地址，低位优先排列（Little-Endian，小端序）则相反。

大端序符合人类阅读习惯，网络协议（TCP/IP）统一使用大端序，因此也被称为网络字节序；而x86架构的CPU使用小端序，这源于Intel早期处理器设计的历史延续。

两种字节序本身没有优劣之分，但当不同架构之间交换数据时，必须进行显式的字节序转换，否则会读出完全错误的值。例如，32位整数`0x12345678`在大端序机器中存储为`12 34 56 78`，在小端序机器中则为`78 56 34 12`。同一个内存内容在两个CPU上会被解释为截然不同的数。

需要计算的数字显然不能局限于范围受限的整数。加密算法需要计算非常大的整数的积，数值计算需要分数甚至无穷小数。人们很快发明出有效表达这些数字的方式。

#### 1.1.1 任意精度整数
固定位宽的整数受限于CPU字长：64位无符号整数的最大值约为$1.84 \times 10^{19}$，这在密码学应用中远远不够，RSA加密通常需要2048位（256字节）的模数，而椭圆曲线密码也常用256位及以上的素数。为了突破这一限制，人们设计了任意精度整数（Arbitrary-precision Integer，也常称为大数，Big Number）。

大数的核心思想是用多个机器字串联起来表达一个更大的整数。一个1024位的整数可以由16个64位字按顺序拼接而成。然而，实现高效的任意精度运算远比把字拼起来复杂。以加法为例，必须从最低字开始逐对相加，并且需要处理进位传播。这正是小学学过的竖式加法在二进制下的直接翻版。

乘法则复杂得多。朴素的大数乘法需要对所有字对做乘法和累加，时间复杂度为$O(n^2)$（$n$为字的个数）。对于RSA中2048位（$n=32$）的操作数，$32^2=1024$次字级乘法尚可接受；但当位数增长到数万甚至数十万位（如大素数搜索、高精度圆周率计算），$O(n^2)$将变得不可接受。为此，现代大数库采用更精妙甚至自适应的算法确保高效的乘法实现。

大数的存储格式同样有讲究。最常见的表示方式是单独用一个标志位存储正负号，其余字存储绝对值。这与有符号整数常用的二进制补码截然不同。

#### 1.1.2 实数与浮点数
如果说任意精度整数解决的是“范围不够大”的问题，那么实数表达面对的是更本质的困难，实数的数量是不可数的，而比特序列的数量是可数的。这意味着任何用有限比特表达实数的方案都必然遗漏几乎所有的实数。最终，我们必须接受一个现实，计算机中的“实数”只是对连续数学的离散逼近。

##### 1.1.2.1 定点数
定点数（Fixed-point Number）是最朴素的解决方案。约定小数点固定在某个位置。例如32位定点数可以约定高16位为整数部分、低16位为小数部分。定点数的运算完全复用整数指令，效率极高，在早期没有浮点硬件单元的嵌入式系统和DSP（数字信号处理器）中广泛使用。然而，定点数的动态范围严重受限。如果小数点位置靠前，整数部分范围小；靠后则小数精度不足。这个限制催生了浮点数。

##### 1.1.2.2 浮点数和IEEE 754标准
浮点数将小数点位置浮动，用类似于科学计数法的形式表达实数：$N = (-1)^S \times 2^{E - bias} \times (1.M)$。IEEE 754标准是这一思想的工程化结晶，定义了两种主流格式：
- 单精度浮点数。1位符号位 + 8位阶码（偏置量127）+ 23位尾数，共32位（4字节）
- 双精度浮点数。1位符号位 + 11位阶码（偏置量1023）+ 52位尾数，共64位（8字节）

浮点数的阶码采用**偏置表示**而非补码，这使得两个浮点数在比较大小——即使作为无符号整数比较——也能得到正确的大小关系（符号位相同的条件下）。这一设计的精巧之处在于，浮点数的比特模式与其数值大小在序关系上保持一致，硬件可以直接使用整数比较电路。尾数部分隐含了一个前导的`1`（规格化数），这多出一个有效精度位而不占用存储空间。

浮点数将实数轴划分为不均匀的区间：在0附近，浮点数极为稠密；随着绝对值增大，浮点数越来越稀疏。这种非均匀采样恰好对应了科学计算中对精度的需求特征。我们通常关心小数的相对误差而非绝对误差。理解了这一点，就自然明白为什么浮点数能仅用32位覆盖从约$10^{-38}$到$10^{38}$的惊人范围。

浮点数的运算需要专门的浮点运算单元（FPU）将阶码和尾数分离处理。两个浮点数相加时，需要先对阶（将较小阶码的数尾数右移，使两者阶码一致），然后尾数相加，最后规格化（将结果调整到`1.M`的形式并舍入）。对阶过程中的右移会丢失尾数低位，这就是浮点数加法精度损失的根本来源。因此舍入误差是浮点计算的固有特征，而非实现的缺陷。

浮点数有三个特殊值值得深入理解：
- 无穷大（Infinity）。当阶码全为1且尾数为0时表示无穷大，符号位决定正负。无穷大的引入使得溢出不再是灾难性错误，`1.0/0.0`产生正无穷，程序可以继续运行，并在后续操作中传播该值。
- 非数/NaN（Not a Number）.当阶码全为1且尾数非0时表示非数，用于表达未定义或不可表示的运算结果（如`0.0/0.0`、`∞ - ∞`）。NaN的传播语义（任何包含NaN的运算结果仍为NaN）使得异常值可以在大规模计算中自然扩散而不立即中止程序，便于调试和容错。
- 有符号的零（+0和-0）。符号位为0或1而其余位全为0。+0和-0在大多数比较操作中相等，但`1.0/+0.0 = +∞`而`1.0/-0.0 = -∞`，这在某些数值算法的分支处理中有用。

绝大多数实数在计算机中无法精确表达。例如十进制的`0.1`在IEEE 754中是一个无限循环小数（二进制为`0.000110011001100...`），任何浮点表示都只是近似值。有些人惊奇地发现`0.1 + 0.2`并不等于`0.3`，这并非计算错误，而是浮点数表达方式的结构性局限。`0.1`和`0.2`的浮点近似值相加后不等于`0.3`的浮点近似值。理解这种限制可以让我们在合适的场景中选择更好的实数表达。如金融计算中常常使用十进制小数类型而非浮点数，正是为了避免这种二进制-十进制转换误差在累计运算中被放大。

##### 十进制浮点数与特殊场景
IEEE 754还定义了十进制浮点数格式，尾数以10为基数而非2。这种格式能精确表达`0.1`这样的十进制有限小数，在金融、会计、税率计算等需要十进制精确性的场景中不可或缺。但其代价是运算速度远慢于二进制浮点数，且硬件支持较少，通常由软件库实现。

#### 1.1.3 复数、向量与矩阵
数字的表达并不止于标量。现代计算中常见的高维数字类型本质上是基本数值类型的结构化组合。

**复数**由实部和虚部两个浮点数或定点数表达，遵循复数的加法和乘法规则。IEEE 754-2019标准正式定义了复数的算术规范，统一了此前各实现的混乱。

**向量**是一组有序的数字序列，可以来自同一物理量的多个分量（如三维空间的坐标），也可以来自同类型的多个独立样本（如一批浮点数）。向量运算强调逐元素操作和规约（求和、求最大/最小）。

**矩阵**是二维的数字阵列，是线性代数计算和图形学变换的核心表达。矩阵乘法、求逆、特征分解等运算需要专门的线性代数库（如BLAS、LAPACK）来高效实现。矩阵在硬件层面的表达往往是行主序（C/C++风格，按行连续存储）或列主序（Fortran/MATLAB风格，按列连续存储），不同的存储顺序影响缓存命中率，选择不当会对大规模矩阵运算性能产生数量级差异。

支持向量和矩阵的运算硬件也在不断演进。SIMD（单指令多数据流）指令集允许一条指令同时对多个数据进行操作，这是现代CPU性能优化的重要方向。GPU（图形处理器）则将数千个计算核心组织为SIMT（单指令多线程）架构，专为大规模向量和矩阵运算设计，成为深度学习和科学计算的核心算力来源。

#### 1.1.4 数的表达范围与代价
从任意精度整数到浮点数，从定点数到复数，我们在“数字的表达”这一维度上需要始终清醒地认识到三个基本的权衡关系：

| 表达方式     | 范围          | 精度       | 速度                     | 适用场景           |
| -------- | ----------- | -------- | ---------------------- | -------------- |
| 固定位宽整数   | 有限（$2^n$种值） | 精确（范围以内） | 最快                     | 计数、索引、位运算      |
| 任意精度整数   | 无限（受内存限制）   | 精确       | 慢（$O(n)$~$O(n\log n)$） | 密码学、精确大数计算     |
| 定点数      | 固定          | 固定小数精度   | 较快                     | 嵌入式、DSP、无FPU场景 |
| 浮点数      | 极大          | 相对精度固定   | 快（硬件加速）                | 科学计算、图形学、通用数值  |
| 十进制浮点数   | 与二进制浮点相当    | 十进制精确    | 慢（软件实现）                | 金融、会计、税务       |
| 复数/向量/矩阵 | 组合扩展        | 继承元素类型   | 取决于运算和硬件               | 物理模拟、机器学习、图形学  |

可以看出，没有普遍最优的数字表达方式。每一种表达都是在范围、精度、速度三个维度上的权衡。理解这些表达方式的内部结构，就可以帮助我们在具体场景下做出合理的选择。

### 1.2 文字的表达
文字是人类最早系统化表达的产物。当文字进入计算机，如何用一个有限的状态序列（比特）表达一个几乎无限、且不断演化的符号集合，就成为一个问题。

#### 1.2.1 以英语中心的ASCII编码
1963年诞生的ASCII（美国信息交换标准码）用7个比特定义了128个符号：
- 控制字符（0–31），如`NUL`（空）、`LF`（换行），用于通信与设备控制
- 可打印字符（32–126），包含大小写英文字母、数字、常用标点

ASCII的设计有很多精巧之处：
* 最高位始终为0，这使得它编码和解码速度极快，在存储和传输上非常节省资源。
* 大写字母（65–90）与小写字母（97–122）之间恰好差32（一个比特位翻转），这意味着大小写转换只需一次比特操作。这是硬件友好的设计典范。
* 数字9的值比数字0更大，字母Z的值比字母A更大（小写字母同理），这个直接对应到日常的默认排序规则，带来了极大的便利。

ASCII最致命的短板是字符集的严重不足。ASCII默认了“文字=字母表”，最多只有128个字符，这远远无法满足英语以外的西欧语言（如带重音符号的法语字母é），更不用说像中文这样的数万个象形文字了。欧洲人通过扩展ASCII到256个码位来支持更多的字符，带来了不少混乱。

#### 1.2.2 分层编码的过渡方案
为了支持多语言，一个曾经的过度方案是“代码页+编码”组织方式，它本质上是一个二维的、分层的寻址系统。

代码页（Code Page）好一个书架，它定义了一个“语言区域”。比如，代码页 `936` 代表“简体中文（GBK）”，代码页 `1252` 代表“西欧语言（拉丁字母）”。每个代码页都代表了针对不同文字的编码规则。

编码则是确定了书架之后的编号。比如在代码页936（GBK）里，`D6D0` 这个数字对应汉字“中”；而在代码页1252（西欧）里，同样的 `D6D0` 可能对应的是两个不同的西欧字符（ÖÐ）。

这种编码方案为计算机支持广泛的语言文字提供了解决思路。但它有一个致命的隐患，无法实现多语言混排。在一个页面或文档里，你很难同时显示中文、阿拉伯文和日文。因为你无法告诉系统“这一个字用936查，下一个字用1256查”。系统只能使用一种代码页，导致其他文字全部显示为问号或乱码。很多软件现在都有选择显示编码的选项，其实就是选择不同的代码页，由人类确定哪一个才是可视正确的。

真正解决这个问题的，是统一编码方案。

#### 1.2.3 统一编码方案Unicode
Unicode的目标是统一所有文字，为地球上每一个符号分配一个唯一的码点（code point），如U+4E2D代表汉字“中”。码点范围从U+0000到U+10FFFF，需21比特表示，最高可以表示大约110万个独立的字符，这个数字看起来不大，但是足够我们使用了。截至今天，Unicode已定义超过15万个字符，覆盖160余种文字及数千个表情符号。

Unicode的革命性在于严格区分字符集（Charset）和编码（Encoding）。前者为每一个需要表示的字符赋予一个唯一的码点，确定有哪些符号可用，而后者则决定了这个字符的存储格式，如何用比特表示。对于ACSII来说，二者是完全一致的。

这种分离带来的好处是，同一个字符可以根据不同的情况使用不同的编码方案实现，最常见的有：
- UTF-8。兼容ASCII，对英文文档极度节省空间。如果主要是处理英文，它是首选，也是绝大多数网络应用的首选
- UTF-16。对中、日、韩文字（CJK）比较友好，大多数常用汉字只用2个字节，比UTF-8（需3个字节）更省空间，常用在系统内部，如Windows内部就是用它，Java虚拟机也用这个
- UTF-32。固定4个字节，字符集和编码等价，处理速度最快，因为其太浪费空间，用得不多。

Unicode的设计有一个重要的原则，编码字符，而非字形。这意味着，Unicode只为抽象的“字符”分配码点，而字符最终在屏幕上长什么样（字形），是字体和渲染引擎的工作。然而现实中存在大量带变音符号的字符（如 `é`, `ü`）。如果为每一个可能的变体都分配一个独一无二的码点，Unicode的码点空间会迅速被耗尽。因此，Unicode引入了“组合字符”的概念，这种机制为复杂的文字表达提供了可能，如：
- `é`（U+00E9）作为一个独立码点，这是预组装字符
- `e`（U+0065）+ `◌́`（U+0301，组合尖音符），得到同样的视觉`é`。这是组合字符序列
- `👨‍👩‍👧‍👦`看起来是一个字符，它在 Unicode 里是由 7 个码点（`👨` + `ZWJ` + `👩` + `ZWJ` + `👧` + `ZWJ` + `👦`）组成的表情符号序列

这两种表达在语义上等价，但在存储和比较时会带来巨大挑战。这是规范等价性问题，需要复杂的归一化算法处理。

##### 1.2.3.1 向后兼容的工程奇迹UTF-8
Ken Thompson 和 Rob Pike 在1992年设计 UTF-8 时面临一个苛刻约束。如何在已有海量ASCII文本的系统上，不破坏现有数据，并支持Unicode？他们的解决方案堪称编码史上的杰作。UTF-8的编码规则如下：

| 码点范围             | UTF-8编码（二进制）                          | 字节数 |
| ---------------- | ------------------------------------- | --- |
| U+0000–U+007F    | `0xxxxxxx`                            | 1   |
| U+0080–U+07FF    | `110xxxxx 10xxxxxx`                   | 2   |
| U+0800–U+FFFF    | `1110xxxx 10xxxxxx 10xxxxxx`          | 3   |
| U+10000–U+10FFFF | `11110xxx 10xxxxxx 10xxxxxx 10xxxxxx` | 4   |

这个设计的优雅体现在：
1. 任何ASCII字符的UTF-8编码与ASCII完全相同（首字节为`0`），原有系统无需修改即可处理UTF-8字符串
2. 从一个任意位置读入字节，最多回退3个字节就能找到字符边界——容错性强
3. 传统C字符串以`\0`终止，UTF-8不会在非ASCII字符中产生`\0`，可以安全使用现有API
4. ASCII文本保持1字节/字符，其他字符2字节，CJK等常用字符3字节，极少使用的字符4字节
5. 多字节字符中，所有后续字节以`10`开头。这意味着你可以通过字节的最高位模式快速判断角色（单字节为`0`，首字节为`110`/`1110`/`11110`，后续字节为`10`）。

UTF-8是网络时代事实上的编码，几乎所有的网络应用都默认使用该编码。新兴的操作系统也开始把该编码作为内置编码。

##### 1.2.3.2 缝合怪UTF-16
UTF-16值得一提，完全因为其历史原因。在Unicode刚刚诞生的上世纪90年代初，人们曾经地认为，`2^16 = 65536` 个字符足够覆盖全人类的所有文字了，于是设计了固定2字节的UCS-2。然而现实很快打了脸，仅汉字就有近十万个，再加上emoji、古文字和各种符号，65536个位置根本不够用。

但此时Windows的内核已经绑定了UCS-2，Java的`char`类型也设计成了2字节。为了不推翻重来，Unicode联盟和各大厂商想出了一个“打补丁”的方案：在不改变2字节基本结构的前提下，从基本平面中借出2048个空位作为“路标”，通过两个16位单元拼接出额外的100多万个位置，这就是代理对（Surrogate Pair）机制。

这个方案看似巧妙，实则带来了无尽的麻烦。它既没有定长编码的便利，有没有变长编码的效率，还因为这个代理对的机制，直接限定了最多17个平面，划定了最大的码点数只有110多万个的物理上限。成为人类设计历史上最奇葩的一个妥协设计产物。

因为Windows和Java这两个庞大生态，UTF-16至今还很活跃。不过在新系统开发中，几乎没有人使用UTF-16作为内置编码了，UTF-8才是如今唯一正确的选择。

#### 1.2.4 字形，字体和字体家族
编码仅仅确定了一个字体如何被表达。如何呈现，使得人类可以阅读，看到具体的文字，完全是独立的工作。

##### 1.2.4.1 字形（Glyph）
字形就是字的视觉形状，是某个字符在屏幕上或纸上显示出来的具体模样或图形。 它是可以被肉眼识别的最基本单位。同一个字符，在不同字体下，字形可以完全不同。如字符“A”在黑体里显示为一个简洁的三角形加横杠，而在在手写体里可能显示为一个带有装饰性卷曲的字母。

同一个字符可能对应多个字形。比如阿拉伯字母在词首、词中、词尾的写法不同，虽然都是同一个字符，但字形不一样。

##### 1.2.4.2 字体（Font）
字体是字形的一套完整集合，是存储在电脑里的一个文件。字体文件保存了两部分信息，字体的编码，以及对应的字形。

字体按照其构建方式，主要分为点阵字体和矢量字体。

点阵字体将每个字符存储为一个固定大小的像素网格（如16×16）。每个像素要么点亮、要么熄灭，直接对应屏幕上的显示。点阵字体的渲染速度极快，不需要任何计算，直接从内存复制像素到显存即可。早期的计算机显示器和打印机都使用点阵字体。

点阵字体的缺陷是放大时候严重的锯齿问题。现在现实更多使用矢量字体。

矢量字体将每个字符的轮廓描述为一系列数学曲线，最常见的是二次贝塞尔曲线（ttf字体）和三次贝塞尔曲线（otf字体）。字符不再是固定的像素网格，而是一组控制点坐标和曲线的数学定义。显示时，渲染引擎根据目标字号动态地"绘制"出字符轮廓，然后填充内部区域，这个过程称为光栅化。

矢量字体的核心优势在于缩放无关性。无论什么字号，渲染引擎都可以用相同的轮廓数据、以任意精度重新计算像素位置，提供了极其良好的显示效果。

实际显示的时候，首先从指定的字体文件中查找字形，找不到的话，根据字体回退原则，从备选字体中找，最终到操作系统全局的备选字体列表中找，直到找到一个字形用于显示。如果还是没有找到，则会显示一个 “缺失字形” 的占位符，俗称豆腐块，用于表示“这里有个字符，但我不知道长什么样”，如Windows使用一个空心的方框（□），Mac使用一个带问号的方框（�）等。

##### 1.2.4.3 字体家族（Font Family）
字体家族是包含多个相关字体的大家庭。 通常用于表示一个基础字体的风格变体，如不同字重（粗细）、字宽（胖瘦）、样式（正斜体）等。用于让用户在不同风格之间切换，同时保持视觉上的统一感。在设计领域有广泛的使用。

#### 1.2.5 文字的表达和结构
如果说字形是文字的视觉表达，码点则是文字的逻辑表达，而编码则是文字的存储表达，这些表达本身又有内部的结构。文字处理程序，渲染程序等负责在不同的表达之间的做映射和转换，共同构成了我们这个支持几乎所有的语言和文字的数字世界。

从文字编码的演进，我们可以看到表达系统的三个深层特征：
- 表达的扩展性。必须为未知的未来预留空间
- 表达的经济性。常见符号用短码字，生僻符号用长码字，这正是哈夫曼编码的直觉基础
- 表达的兼容性。向后兼容是一种极端重要的工程约束，它决定了新技术能否被采纳

文字本身是离散的。对于图像，声音和视频这类连续的信息，该如何表达呢？ 

### 1.3 媒体的表达
对于连续的物理世界，有效的表达来源于两个方面：
- 切分。首先把物理世界分隔为足够细的“原子”
- 编码。对原子编码

#### 1.3.1 图像
连续图像的原子是像素。两步操作为：
- 分隔原子。在二维平面上以固定间隔取样（分辨率），叫做空间采样
- 对原子编码。把每个像素的属性（亮度/颜色值等）从连续域映射到有限整数（如0–255），叫做幅度量化

从RGB三原色模型出发，我们可以有效表达不同的色彩。用三个字节分别表达一个像素的三种元色，每通道8位，这已是行业标准。 很多现代格式使用RGBA（4字节/像素，含Alpha通道），即便不带Alpha，在内存对齐时也常使用4字节以提高访问效率。

但RGB并非唯一选择。YUV色彩空间将亮度（Y）与色度（U、V）分离，因为人眼对亮度更敏感，因此可以对色度进行亚采样（如4:2:0格式，色度分辨率只有亮度的一半），这是视频压缩的核心手段之一。

8位/通道只能表达256级亮度，不足以覆盖真实世界的高动态范围。HDR使用10比特甚至更高位深来表表达精细的图像。位深的选择本质上是在表达精度与存储成本之间做权衡。

#### 1.3.2 音频
连续声波的原子叫做样本（不叫音素，这个词别有他用），两步操作为：
- 分隔原子。采样率是每秒采样的次数，44.1千次每秒是CD音质标准，这个采样率足够覆盖人的听力的所有细节   
- 对原子编码。位深度是每个采样的精度，16比特提供65536级

为什么采样44.1kHz就够了呢？其背后的原理是奈奎斯特-香农采样定理。为了无失真地重建一个频率为f的信号，采样率必须大于2f。这是信号处理中“不可能绕过”的下界。

> 20kHz 是人类耳朵的“上限”，40kHz是数学计算出的“及格线”，44.1kHz则是因为历史原因：怎么把音频信号伪装成视频信号存进录像带里。PAL 制式每帧 625 行，每秒 25 帧。有效视频行为 588 （去掉同步信号等），计算$25 \times 588 \times 3$（每行存3个样本）= 44,100 Hz。

采样定理表明离散信号可以完全用于连续信号的表达。如果这个连续信号的带宽有限，其可以被无限精确地表达。

直接存储每个样本的量化值，这是WAV和AIFF等文件格式的基础，直接但是冗余巨大。而更高效的方式是感知编码（如MP3），它丢弃人耳听不到的频率成分，利用心理声学模型在噪音中藏匿失真。

#### 1.3.3 视频
连续视频的原子叫做帧，也就是一张图，两步操作为：
1. 分隔原子。24帧每秒（fps）是电影的拍摄基础要求，有参与感的游戏一般要求60fps甚至更高
2. 对原子编码。直接把帧按照完整图片保存完全浪费了帧之间巨大而重复性。以H.264为例，帧分为三种
	- I帧。完整编码的独立图片，可独立解码，是后面所有帧参考基准
	- P帧。只记录相对于前一帧的变化
	- B帧。参考前后两帧进行双向预测，压缩率最高，但是因为需要参考未来帧，这引入了编码延迟。实时场景下，禁用B帧

视频有大量的冗余信息，这使得有效的压缩成为可能。视频可以压缩的机会来自三个方面：
1. 空间冗余。同一帧内相邻像素相似（图像压缩）
2. 时间冗余。连续帧之间变化很小（运动估计与运动补偿）
3. 感知冗余。人眼对高频细节和色度不敏感

流行的视频格式如mp4，mkv等，都是极限压缩之后的结果。在保持效果的同时极大减少了存储需求。

#### 1.3.4 媒体编码的统一视角
图像、音频、视频的编码技术共享一个核心逻辑：

| 媒体类型 | 表达域 | 变换域        | 核心冗余        |
| ---- | --- | ---------- | ----------- |
| 图像   | 空间域 | 频域（DCT/小波） | 平滑区域的低频成分   |
| 音频   | 时域   | 频域（MDCT）   | 心理声学掩蔽      |
| 视频   | 时空域 | 频域+运动矢量    | 时间连续性+空间平滑性 |

所有先进媒体编码标准（JPEG、MP3、H.264/AVC、HEVC）都在做同一件事，将信号从自然表达变换到一个更容易压缩的表达，然后对变换系数进行量化和熵编码。DCT（离散余弦变换）是有损压缩是常用的手段，它将图像能量集中到少数低频系数上，高频系数可被粗量化甚至舍弃。
 
解决了怎样表达的问题之后，面对面对更大规模的信息（比如整部百科全书）时，新的挑战出现了，如何表达得更短（压缩）？如何在噪音中恢复正确内容（纠错）？这就引出了编码理论。

### 1.4 从编码到信息论
当我们深入编码实践时，会遇到两个看似矛盾的挑战：

**挑战一：如何表达得更短？（信源编码）**
- 文本压缩：字典方法（LZ77、LZ78、LZW）通过引用重复片段来缩短表达
- 图像压缩：变换编码（DCT、小波）利用频域的能量集中特性

然而越短的表达往往对错误越敏感，一个比特翻转可能摧毁整个意义

**挑战二：如何在噪音中表达得可靠？（信道编码）**
- 增加冗余（如重复发送同一比特3次）
- 汉明码、里德-所罗门码、Turbo码、LDPC码

然而越可靠的表达往往越长，冗余与效率是对立的

这两个挑战看起来对立，但香农在1948年证明了一个惊人的事实：它们可以在理论上被同时解决，并且存在一个极限边界。

#### 1.4.1 香农的三个核心洞察
香农首先认为，信息可以量化，这就是信息熵。熵不是混乱度，而是不确定性。如果一个事件有概率p，那么它包含的信息量是$log_2(1/p)$比特。整个信源的熵是各项的加权平均：
$$
H = -\sum p_i log_2 p_i
$$
熵的意义是表达信源所需的最短平均码长。如果英语的熵是约1.5比特/字符，这意味着理论上我们可以将英语压缩到平均1.5比特/字符。但实际压缩算法通常做不到这么理想，因为真实文本不是独立同分布的随机过程。

香农注意到，存在一条“压缩的底线”，这就是信源编码定理。存在着编码方案使平均码长任意接近熵，同时任何编码方案的平均码长都不可能低于熵。

当概率分布已知的情况下，哈夫曼编码可以看做是这个定理的构造性证明。

香农同时注意到，存在一条“可靠传输上限”，这就是信道编码定理。信道容量$C = B·log_2(1 + S/N)$（其中B是带宽，S/N是信噪比）。只要传输速率R < C，就存在编码方案使错误率任意趋近于零；如果R > C，错误率必然趋近于100%。

> 香农不仅证明了极限的存在，还证明了这个极限可以无限接近。但他是用随机编码和概率方法证明的，没有给出实际构造。后来的Turbo码（1993）和LDPC码（重发现于1996，最初由Gallager在1963年提出）在实践上逼近了香农极限。这被称为“编码史上的炼金术”，从存在性证明到工程实现，整整花了五十年。

#### 1.4.2 从信息论回看编码实践
信息论为编码实践提供了理论框架：
- 文字编码（ASCII、Unicode、UTF-8）解决的是语义保真问题（无损）
- 媒体编码（JPEG、MP3、H.264）解决的是失真优化问题（有损）
  - 在给定失真容忍度下，最小化比特率
  - 在给定比特率下，最小化失真
- 信道编码解决的是错误容忍问题

这三个看似独立的领域，在信息论视角下统一为同一个命题：“如何在约束下最优地表达信息？”

### 1.5 编码是约定的艺术
可以做出结论，编码的本质可以定义为“转换映射 + 约定”
- 转换映射。从源符号集到目标符号集（通常是比特）的一个函数
- 约定。编码和解码双方共同遵守的规则

同样的比特串`01000001`，在ASCII约定下是'A'，在整数约定下是65，在IEEE 754的某段区间上是一个浮点数的一部分。这个意义完全由上下文决定。

#### 1.5.1 编码是分层系统
编码不是单层的。当我们说“UTF-8编码”时，实际涉及多层约定：
- 语义层：U+4E2D代表汉字“中”（Unicode分配的码点）
- 字节表示层：U+4E2D → `E4 B8 AD`（UTF-8编码规则得出的存储字节序列）
- 物理层：`E4 B8 AD` → 存储系统的物理机制

这种分层设计是计算机系统的通用模式。每一层只关注本层的约定，不应窥探下层或上层的实现细节。

#### 1.5.2 编码是悬置承诺
任何编码都隐含着这样一个结构，现在我们用一种表达方式编码，它可以被替换成另一种，只要转换规则是确定的。这是表达之间如何转换的规则。
- 一个ASCII文本文件可以被转换成UTF-8，且转换后可逆
- 一个BMP图像文件可以被转换成JPEG，但这是不可逆的（有损）
- 一个比特流可以被视为整数、指令或像素值，取决于解释器

这个悬置特质极其深刻。它意味着编码本身不是目的，而是通往更高层表达的桥梁。当我们说“理解编码”，实际上是在说我们掌握了在不同表达方式之间进行映射的能力。

#### 1.5.3 结构、变换与未来悬念
一旦理解编码是约定的映射，就会自然追问两个问题：

问题一：能否不改变内容，只改变编码形式来降低存储成本？ → 答案是压缩（已经讨论）
问题二：能否不改变编码，只改变数据的组织方式，从而获得新的计算能力？ → 这是结构的问题。

例如一个数组是连续存储（结构A），一个链表是分散存储+指针（结构B）。同样的比特序列，组织成二叉树就有了对数级查找能力，组织成哈希表就有了常数级平均查询时间。

编码处理的是“怎样用比特表达信息”。结构处理的是“怎样组织这些比特使得访问和修改更高效”。后者引入了时间和空间维度的效率考量。

更进一步的，当我们允许信息在不同结构之间转换时，就进入了变换的领域。例如：
- 快速傅里叶变换（FFT）不改变信号的信息内容，但改变我们在时域和频域之间观察的能力
- 排序不改变集合内容，但改变了检索效率

编码是表达之根，可以看做是最底层的构造。从算法设计到数据结构，从数据库索引到机器学习，真正的计算能力都建立在这个根基之上，无论是通过结构选择还是变换设计做到。

### 1.6 本章小结
从一对截然区分的状态（比特）开始，我们讨论了信息表达的途径和遇到的问题。文字编码（ASCII→Unicode→UTF-8）解决了跨语言符号的表达问题，媒体编码（采样、量化、变换）跨越了从连续到离散的鸿沟。香农的信息论揭示了表达的理论边界，熵是压缩的下界，信道容量是可靠传输的上界。而编码的本质，是一座分层约定的塔，每一层都是对下层比特的解释，每一层也都为上层结构的涌现提供基础。
