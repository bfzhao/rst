## 第七章 类型理论：程序逻辑的结构化证明

> 类型系统不是被发明出来的，它们是被发现的。
> 						Philip Wadler

类型初看起来是数据的分类。这是整数，那是字符串，这是文件，那是猫。每个类型支持不同的操作等等，这些构成了程序的基础语义原子。

类型更是一种适用于程序的轻量级形式化验证系统。每当编译器检查类型时，它实际上在证明程序的某些属性，如这个表达式总是产生整数，这个函数不会访问未定义的内存，这个资源的生命周期不会超出其所有者等。

类型是程序逻辑的结构正确性证明，嵌入在语法中，并由编译器验证。

### 7.1 类型系统基础

#### 7.1.1 类型作为约束与承诺
当一个程序员写下 `x: Int`，这意味着：
- 对编译器的约束。`x` 只能存储整数值，任何试图将字符串赋给 `x` 的操作都会被拒绝
- 对编译器的承诺。存储在 `x` 中的值可以安全地参与整数运算（加法、乘法、位运算）
- 对人类的文档。这个变量承载的是整数语义

类型系统就是这些约束和承诺的形式化集合。

类型首先是值的分类，这个分类具有以下性质：
- 离散性。每个值属于唯一的类型（或类型集合，取决于子类型规则）
- 操作性。类型决定了哪些操作可以应用于该值
- 语义性。类型隐含了值的解释方式（`0x41` 作为整数是65，作为ASCII是'A'）

#### 7.1.2 类型检查
类型检查是在程序运行之前（编译时）验证所有接口匹配的过程，确保静态结构的一致性。

```haskell
-- Haskell的类型检查过程
add :: Int -> Int -> Int   -- 类型签名：承诺
add x y = x + y            -- 实现

-- 使用
result = add 5 "hello"     -- 类型错误：期望Int，实际得到[Char]
```

编译器在检查 `add 5 "hello"` 时：
1. 查看 `add` 的类型：`Int -> Int -> Int`
2. 检查第一个参数 `5`：推断为 `Int` —— 匹配
3. 检查第二个参数 `"hello"`：推断为 `String`（即 `[Char]`）—— 不匹配
4. 报告类型错误，拒绝编译

类型检查是一种抽象解释，编译器不用执行程序，就可以模拟部分计算（类型层面的计算）以验证一致性。

#### 7.1.3 类型安全
类型安全意味着，类型系统保证程序不会执行违反类型规则的操作，意味着不会因类型层面的非法操作而崩溃。

| 语言      | 类型系统    | 类型安全？ | 风险                      |
| ------- | ------- | ----- | ----------------------- |
| Haskell | 强静态     | 是     | 无（除 unsafe 逃逸口）         |
| Rust    | 强静态+所有权 | 是     | unsafe 块                |
| Java    | 强静态     | 基本是   | 数组协变可能导致运行时异常           |
| C       | 弱静态     | 否     | 指针强制转换、缓冲区溢出            |
| Python  | 强动态     | 运行时   | 鸭子类型可能导致 AttributeError |

类型安全的价值是显然的：
- 消除整类错误（类型混淆、不兼容操作）
- 使重构更安全（改变类型后，编译器指出所有受影响位置）
- 为优化提供依据（编译器可以假设类型约束永远成立）

#### 7.1.4 静态类型 vs 动态类型
这是编程语言中最被误解的二分法之一。我们从多个维度看一下：

| 维度 | 静态类型 | 动态类型 |
|------|---------|---------|
| 类型检查时机 | 编译时 | 运行时 |
| 类型信息附着 | 变量（或标识符） | 值 |
| 典型语言 | C, Java, Rust, Haskell | Python, Ruby, JavaScript |
| 错误发现 | 早期（编译时） | 晚期（运行时） |
| 运行时开销 | 无（类型信息可擦除） | 有（类型标签检查） |
| 灵活性 | 需要类型系统支持（泛型、trait） | 极高（猴子补丁、运行时修改） |
| 工具支持 | 强（自动补全、重构、跳转定义） | 弱（需要运行时信息） |

从静态类型的“正统”视角（来自Haskell/ML等函数式语言）看，类型是程序正确性的一部分。程序如果类型检查通过，就排除了整类错误。这些错误在动态类型语言中会潜伏到运行时才暴露。这是数据的属性。 

从动态类型的“实用”视角（来自Smalltalk/Python等命令式语言）看，类型是程序的行为属性。只要两个对象支持相同的操作，它们就可以互换（鸭子类型），这种情况下，静态类型只会束缚表达力。这是操作的属性。

现实情况下，大部分语言的类型系统融合了语义属性和数据属性：
- 静态类型语言增加了类型推断（如C++ `auto`、Rust `let`、Haskell 全程序推断）以减少注解负担
- 动态类型语言增加了可选的类型注解（如Python type hints、TypeScript、Ruby 3的RBS）以支持工具和早期错误发现

#### 7.1.5 类型推断
类型推断让编译器自动推导表达式类型，而不是强制程序员显式标注。

```rust
// Rust的类型推断
let x = 42;        // 编译器推断 x: i32
let y = x + 1;     // y: i32
let z = "hello";   // z: &str

// 当需要具体类型时，可显式标注
let a: u64 = 42;   // 覆盖推断
```

Hindley-Milner类型推断（ML/Haskell）基于合一算法（与Prolog的合一相同原理），可以推断出最一般的类型（最泛型、最灵活的类型），即使没有类型注解，也能推断复杂表达式的类型。

```haskell
-- Haskell：无需任何类型注解
map f [] = []
map f (x:xs) = f x : map f xs
-- 编译器推断出: map :: (a -> b) -> [a] -> [b]
```

#### 7.1.6 多态（Polymorphism）
如果一个函数能支持多种不同的类型，这被称为多态。

参数多态（Parametric Polymorphism）也称为泛型，是最纯粹的形式：
```rust
// Rust泛型函数
fn identity<T>(x: T) -> T {
    x
}

let a = identity(42);      // T = i32
let b = identity("hello"); // T = &str
```

泛型的关键性质在于“泛”，函数对所有类型 T 一视同仁（不能根据 T 的类型做不同的事情，因为 T 是未知的）。我们从类型签名就能推导出函数行为的诸多约束。

子类型多态（Subtype Polymorphism），即经典的面向对象继承，通过动态分派实现：
```java
Animal a = new Dog();   // Dog 是 Animal 的子类型
a.makeSound();          // 运行时动态分派到 Dog.makeSound()
```

特设多态（Ad-hoc Polymorphism），C++中叫做重载：
```c++
// C++函数重载
void print(int x) { ... }
void print(string s) { ... }

print(42);     // 调用第一个版本
print("hi");   // 调用第二个版本
```

类型类（Type Class）是一种更抽象的类型，它用来定义某些类型支持特定的操作。比如Rust trait / Haskell type class：
```rust
// Rust trait（类似接口但更强大）
trait Add {
    fn add(self, other: Self) -> Self;
}

impl Add for i32 {
    fn add(self, other: i32) -> i32 {
        self + other
    }
}

fn double<T: Add>(x: T) -> T {
    x.add(x)
}
```

类型类避免了OOP继承的局限性，同时保持了静态分派（性能无损）。

### 7.2 类型构造与代数数据类型
基础类型一般只是语言层面内置支持的类型，强调的是普适性，而不是完全性。利用基础类型组合出各种新类型，才是类型系统真正有用的前提。

#### 7.2.1 基本类型与组合方式
类型系统从一组原子类型开始：
- `Int`, `Bool`, `Char`, `Float`, `Unit`（或 `Void`）
- 指针/引用类型（`&T`, `Box<T>`）
- 函数类型（`T -> U`）

然后提供组合规则从原子构造复合类型。

#### 7.2.2 积类型（Product Type）
积类型包含多个字段，同时持有所有字段的值，类似于“与”操作。

```rust
// Rust元组（匿名积）
let point: (i32, i32) = (10, 20);

// Rust结构体（命名字段）
struct Point {
    x: i32,
    y: i32,
}
```

如果类型 `A` 有 `|A|` 个值，类型 `B` 有 `|B|` 个值，那么 `(A, B)` 有 `|A| × |B|` 个值，这就是“积”名称的来源。

不同语言的积类型有不同的表达：
- 结构体（struct）
- 元组（tuple）
- 记录（record）
- 类（class）的字段集合

#### 7.2.3 和类型（Sum Type）
和类型表示“这个值要么是类型A，要么是类型B”，类似于“和”操作。

```rust
// Rust枚举
enum Either<L, R> {
    Left(L),
    Right(R),
}

// 使用
let a: Either<i32, &str> = Either::Left(42);
let b: Either<i32, &str> = Either::Right("hello");
```

`Either<A, B>` 有 `|A| + |B|` 个值，“和”名称的来源。

不同语言的和类型也有不同的表达：
- 选项类型：`Option<T> = None | Some(T)`
- 结果类型：`Result<T, E> = Ok(T) | Err(E)`
- 布尔值：`Bool = True | False`（两个单例值的和）
- 枚举类型（enum）

对于和类型来说，精确表达是可能的。这比用特殊值（如 `-1` 表示“无结果”）更安全、更自文档。因为其结果的有限性，编译器可强制检查所有情况。

#### 7.2.4 代数数据类型（ADT）的数学本质
代数数据类型（ADT）是积类型和和类型的任意嵌套，再加上递归。

代数解释：
- 将类型视为代数表达式
- `+` 表示`Either`（和）
- `×` 表示元组或结构体（积）
- 类型“值”的个数满足代数运算律（符合直觉）

当然也符合代数定律：
- 交换律：`A + B = B + A`（`Either<A, B>` 与 `Either<B, A>` 同构）
- 结合律：`(A + B) + C = A + (B + C)`
- 分配律：`(A + B) × C = (A × C) + (B × C)`
- 单位元：`A + 0 = A`（`0` 是 `Void` 类型——没有值的类型）
- 吸收律：`A × 0 = 0`（任何类型与Void的积也是Void）

从这个角度看，函数类型怎么解释呢？函数类型可以解释为指数`A -> B` 类型的值数量是 `|B|^{|A|}`。从集合角度看，函数是从`|A|` 个元素到`|B|` 个元素的映射，总共有 `|B|^|A|` 种可能，例如从 `Bool` 到 `Bool` 的函数只有 4 种可能的组合，而不论如何实现。

#### 7.2.5 递归类型
递归类型允许类型在其定义中引用自身。

```rust
// 递归定义的链表
enum List<T> {
    Nil,
    Cons(T, Box<List<T>>),  // Box 是堆分配指针，打破无限大小
}
```

常见的递归类型有：
- 列表：`List<T> = Nil | Cons(T, List<T>)`
- 二叉树：`Tree<T> = Leaf | Node(T, Tree<T>, Tree<T>)`
- 自然数（皮亚诺编码）：`Nat = Zero | Succ(Nat)`

我们可以将递归类型看作方程，求其最小不动点（最小的满足该方程的类型）：
```
List<T> = 1 + T × List<T>
```
这个方程的解（忽略严格性）正是形式幂级数 `1 + T + T^2 + T^3 + ...`——每个系数对应固定长度列表的数量。

#### 7.2.6 模式匹配
模式匹配是根据值的**构造形式**进行分解和分支的控制流结构。
```rust
match list {
    Nil => println!("空列表"),
    Cons(head, tail) => println!("首元素: {}", head),
}
```

模式匹配的特性如下：
1. 分解。将复合值拆解为组成部分（如将 `Cons` 分解为 `head` 和 `tail`）
2. 分支。根据构造子选择执行路径
3. 穷尽性检查。编译器验证所有可能的构造子都被覆盖

```rust
// 非穷尽的匹配——编译器警告/错误
match maybe_value {
    Some(x) => println!("Got: {}", x),
    // 未处理 None 情况！
}
```

穷尽性检查很重要。当你向和类型添加新的变体（如增加 `Pending` 到 `enum Status { Ready, Busy }`），编译器会指出所有需要更新匹配的地方。这就像在整个代码库中精准地搜索并标记需要修改的位置，同时保证不遗漏。

### 7.3 依赖类型、线性类型与效应系统
这三个主题代表了类型理论的前沿，它们将类型从简单的分类工具发展为表达能力极强的契约和推理框架。

#### 7.3.1 依赖类型
依赖类型指的是，类型可以依赖特定值。比如一个长度的4的整数数组和一个长度为8的整数数组他们的类型不同，因为它们分别依赖于4和8这两个独立的值。以下是Haskell的例子：

```haskell
-- Idris（依赖类型语言）的例子
append : Vect n a -> Vect m a -> Vect (n + m) a
append Nil ys = ys
append (x :: xs) ys = x :: append xs ys
```

类型 `Vect (n + m) a` 中包含了结果长度的信息，编译器在类型层面证明了你没有越界访问，也没有丢失元素。

依赖类型的能力：
- 安全数组访问。`lookup : Vect n a -> Fin n -> a`，其中 `Fin n` 是小于 `n` 的自然数类型——类型系统强制保证索引不会越界
- 格式化输入。`read : (f : Format) -> String -> ParseResult (Interp f)`，解析结果的类型由格式字符串决定
- 协议验证。用类型表示协议状态（`Socket Closed` → `Socket Connected` → `Socket Authenticated`），非法状态转换被编译器拒绝

但是依赖类型不是主流，原因在于
- 类型检查可能变得不可判定（需要程序员提供证明/帮助）
- 类型签名中包含值表达式，使得类型变得复杂
- 学习曲线陡峭

Coq、Agda、Idris 用于定理证明和验证编译；Rust的常量泛型（`array: [T; N]`）是依赖类型的受限形式；TypeScript的字符串字面量类型也是依赖类型的一种。

#### 7.3.2 线性类型与所有权
程序中有许多资源需要唯一所有权（文件句柄、锁、套接字、内存）。如果多个变量可以引用同一资源，可能出现：
- 释放后使用（use-after-free）
- 双重释放（double free）
- 数据竞争（多线程同时修改）
这些是导致程序bug和安全问题的最大源头。

线性类型的核心原则是，每个资源在同一时刻只有一个“所有者”拥有修改权限。

```rust
// Rust的所有权系统
let s1 = String::from("hello");
let s2 = s1;  // s1 的所有权被移动到 s2
// println!("{}", s1);  // 编译错误：s1 已不再有效
```

所有权的三个规则：
1. Rust中的每个值都有一个所有者（owner）
2. 同一时刻只能有一个所有者
3. 当所有者离开作用域，值被自动释放（调用 `drop`）

借用（Borrowing）是指通过引用临时让出访问权限：
```rust
fn calculate_length(s: &String) -> usize {  // s 是引用
    s.len()
}  // 这里 s 被丢弃，但因为它没有所有权，它所指向的字符串不受影响

let s1 = String::from("hello");
let len = calculate_length(&s1);  // &s1 借用 s1，但不取得所有权
println!("{}", s1);  // s1 仍然可用
```

上面的借用是只读的，也可以写借用，叫做可变借用（Mutable Borrow）：
```rust
fn change(s: &mut String) {
    s.push_str(", world");
}

let mut s = String::from("hello");
change(&mut s);
```

为了防止数据竞争，同时只能有一个可变引用或任意多个不可变引用，且引用必须始终有效（生命周期验证）

生命周期（Lifetime）是一个引用的存活范围。编译器通过借用检查器验证所有引用的有效性，退出生命周期就直接回收。

```rust
// 生命周期注解（当编译器无法自动推断时需要）
fn longest<'a>(x: &'a str, y: &'a str) -> &'a str {
    if x.len() > y.len() { x } else { y }
}
```

Rust的所有权系统证明了一个长期存在的信念，内存安全可以在无垃圾回收、无运行时开销的前提下实现。这是类型理论对系统编程领域的最大贡献之一。

C++也有类似的思想，强调的是所有权的获取和释放必须严格限制在对象的初始化和清理阶段，叫做RAII，可以说是异曲同工。  

#### 7.3.3 效应系统
如果把类型仅仅看做是标记，我们可以使用类型做一些非常出乎意料的工作，如追踪副作用。

纯函数式语言（如Haskell）中，函数默认是纯的（无副作用）。但程序需要与外部世界交互（I/O操作）。如何在不破坏纯度的情况下引入I/O？

Haskell的解决方案是IO Monad
```haskell
-- 类型签名明确标记了“有副作用”
getLine :: IO String          -- 从标准输入读取一行
putStrLn :: String -> IO ()   -- 输出一行
main :: IO ()                 -- 整个程序有副作用
```

IO类型本身是一种标记，表示一种效应（effect）。它不参与实际的运算，但是可以提供额外的信息，而影响程序的执行结构。

效应是可以沿着调用链传播的的。如果A有某种效应，而B调用了A，B就自然拥有这种效应；效应也是可以组合的，达到积效应或者和效应的结果。

效应系统的强大远不止此。我们可以使用类型标记异步调用，异常传播等能想到的所有场景。恰当地控制其粒度和组合，就可以使得效应可推理可证明。我们使用类型系统就完全管理了副作用！           

效应系统目前在函数式编程语言中应用较多，毫无疑问它有着广阔的前景。

### 7.4 类型系统的统一视角

#### 7.4.1 类型作为逻辑命题
柯里-霍华德对应（Curry-Howard Correspondence）揭示了类型系统和逻辑之间的深刻同构：

| 逻辑     | 类型理论     | 程序语言      |
| ------ | -------- | --------- |
| 命题     | 类型       | 类型注解      |
| 证明     | 程序（项）    | 函数实现      |
| 蕴含 (⇒) | 函数类型 (→) | lambda 抽象 |
| 且 (∧)  | 积类型 (×)  | 元组/结构体    |
| 或 (∨)  | 和类型 (+)  | 枚举/联合     |
| 真 (⊤)  | Unit 类型  | ()        |
| 假 (⊥)  | Void 类型  | 无值的类型     |

其中Void 类型是没有值的类型。函数 `A -> Void` 在逻辑上对应“A是假的”。因为如果存在一个 `A` 的值，你就能从它构造出 `Void`，而这是不可能的。

这种对应给我们一个启示，编写函数 `impl<T> Option<T> for T` 的过程就是在证明“对于所有T，T 蕴含 Option\<T\>”，这就为自动证明提供了便利。你只需要一个类型检查器，就可以证明你的程序遵守其类型契约。

> 柯里-霍华德对应看起来的巧合，源于它们完全相同的构造性本质。也就是说，逻辑和类型的构造实际上是同源的。逻辑关心真假，有两个值；而类型一般有多个值，而每个值都可以看作是类型（命题）的有效构造，这就是证明（没有值的类型可以认为不可证明）。
> 在构造性逻辑中，一个命题是否成立，不取决于它是否“不可反驳”，而取决于你是否真的构造出了它。类型系统继承了这一思想：一个类型存在，不意味着它可用；只有真正构造出该类型的值，这个命题才算被证明。
> 函数是类型到类型的映射，这相当于打通了从一个类型到另一个类型的渠道，这其实就是推理。如果完成了一个函数的实现，而且通过了类型检查，这自然意味着这个推理已经被证明。

#### 7.4.2 类型系统谱系
我们按照类型系统的强弱排个序，就得到类型系统谱系如下：

```
更强的表达能力 / 更难的推理
        ↑
依赖类型 (Coq, Agda, Idris)
        ↑
效应系统 (Haskell, Koka, OCaml 5.0)
        ↑
线性/所有权 (Rust)
        ↑
代数数据类型 (ML, Haskell, Rust)
        ↑
泛型/参数多态 (Java, C#, Go)
        ↑
子类型多态 (大多数OOP语言)
        ↑
简单类型 lambda 演算 (STLC)
        ↑
无类型 lambda 演算 (动态类型语言的基础)
        ↑
纯机器码 (无类型)
```

#### 7.4.3 类型系统的权衡
设计一门新的编程语言，类型系统是设计的核心。一般涉及到多个技术权衡：

| 特性   | 更强的类型系统         | 更弱的类型系统     |
| ---- | --------------- | ----------- |
| 表达力  | 能编码更多契约         | 契约隐含在约定/文档中 |
| 安全性  | 更多错误被编译时捕获      | 更多错误推迟到运行时  |
| 编译速度 | 慢（需要复杂推理）       | 快           |
| 学习曲线 | 陡峭              | 平缓          |
| 灵活性  | 需要类型系统支持（如依赖类型） | 极高（动态修改）    |
| 错误信息 | 可能复杂难懂          | 简单直接        |

同时满足所有这些特性的类型系统是不存在的，这也是编程语言百花齐放的原因。我们故意放弃某些约束，目的是使得我们的选择针对某些问题更加有效。

#### 7.4.4 为什么现代语言越来越趋向“类型驱动”
现代软件越来越复杂。业务本身很复杂，运行在有多个核的CPU上需要支持并行操作；还需要执行在多个物理的计算接节点支持分布式的语义；和外部系统集成协作也越来越多，同步异步各有不同；内部状态组合爆炸，生命周期管理困难等。

这些问题看起来彼此不同，但本质上，它们都在问同一个问题：“程序在某个状态下，哪些操作是合法的？”
- 一个已经释放的内存，不能继续访问
- 一个未加锁的数据，不能被多个线程同时修改
- 一个未建立连接的Socket，不能发送数据
- 一个失败的结果，不能被当作成功值继续使用
- 一个异步任务，可能尚未完成

这些约束，本质上都是状态之间的合法转换规则。类型系统，恰好非常擅长描述这种规则。现代类型系统则试图将这些约束直接编码进程序结构中，并交给编译器机械验证。

把这些看起来迥异的复杂性，都可以通过类型系统恰当建模出来，这是现代类型理论的深刻洞察。

### 7.5 本章小结
从类型系统的基础开始，看到类型作为约束和承诺的双重作用。静态类型在编译时进行结构一致性验证，动态类型将检查推迟到运行时，两者各有优劣。代数数据类型（积×与和+）提供了精确描述数据结构的数学语言，模式匹配配合穷尽性检查让“永不漏情况”成为编译时保证。

前沿的类型系统扩展了表达能力：依赖类型让类型依赖值，可以在类型层面验证数组边界、协议状态；线性类型和所有权系统（Rust）实现了无GC的内存安全；效应系统将副作用纳入类型跟踪，使纯函数与副作用函数的边界被类型系统强制。

类型理论告诉我们，类型是由编译器机械验证的程序的部分正确性的证明。一个好的类型系统在不显著增加程序员负担的前提下，排除了整类错误，并使程序行为更加可预测、代码重构也更加安全。
