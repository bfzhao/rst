## 第六章 编译器：表达之间的转换
编译器进行的是世界上应用最广泛的转换系统，将高级语言表达（人类可读的源代码）转换为处理器可执行的低级机器表达。这是一个多层次的、精密的自动化系统。

编译器是表达层次之间的桥梁。它一边承接程序员的思维模型（声明式、函数式、面向对象等），一边面向机器的执行模型（寄存器、内存、指令流水线），在两者之间进行结构保持或语义保持的变换。

编译系统是转换的集大成者。深入理解编译器的工作方式，有助于理解转换的多样性和可靠性以及对结构的强烈依赖。

### 6.1 从字符流到抽象语法树
编译器前端的目标是将程序员书写的字符序列（源代码）转换为编译器可以操作的结构化表示，一种叫做抽象语法树（AST）的结构。

#### 6.1.1 从字符到词法单元
源代码的组合是无限的，需要转换为有限的基础单元。这一步叫做词法分析。词法分析的输入是源代码，它需要从其中识别出不同的“原子”。如关键字（`if`、`while`）、标识符（`counter`）、字面量（`42`、`"hello"`）、运算符（`+`、`*`）等，这个原子叫做Token（词法单元）。

对应的工具一般叫做词法分析器（Lexer）。它读取字符流，输出词法单元（Token）流。比如

```
源代码: if (x > 0) x = x + 1;

词法单元流:
  KEYWORD_IF   "if"
  LPAREN       "("
  IDENTIFIER   "x"
  OPERATOR_GT  ">"
  NUMBER       "0"
  RPAREN       ")"
  IDENTIFIER   "x"
  ASSIGN       "="
  IDENTIFIER   "x"
  OPERATOR_PLUS "+"
  NUMBER       "1"
  SEMICOLON    ";"
```

词法的定义当然是人为的，以满足精确表达人类思想为目的。但是需要注意词法定义不能有二义性。比如C中，+表示操作符+，但是++表示自增。词法处理的时候，往往需要使用贪婪算法，确保优先识别++，而不是两个连续的+。

常见的描述词法规则的是正则表达式，如标识符：`[a-zA-Z_][a-zA-Z0-9_]*`。实际使用有限自动机（DFA/NFA）执行识别过程。

手写词法分析器（逐个字符扫描，状态机转换）是一个很有意思的挑战，可以深入了解已有设计的细微之处。但是实践中，都是用使用词法生成器（如Lex、Flex）从正则表达式自动生成词法分析代码，这个工作完全可以自动化。

#### 6.1.2 从词法单元到树
词法单元流是扁平的，我们需要恢复出程序的结构，表达式嵌套、语句序列、函数定义等。这是语法分析器（Parser）的任务。这一步叫做语法分析。

语法分析器根据上下文无关文法（CFG） 将Token流转换为语法树（Parse Tree） 或更紧凑的抽象语法树（AST）。

以下示例定义了一个支持括号的简单算术运算表达式。
```ebnf
E → E + T | T
T → T * F | F
F → ( E ) | NUMBER | IDENTIFIER
```
我们知道，乘的优先级大于加。但是我们不是在运行的时候判断那个有更高的优先级，而是在定义的时候，确保最终生成的语法树中，乘会比加更早执行。这就是**结构的意义**。这种技术在很多时候都可以大大简化运行时的设计。在这种依赖结构的设计中，越是高优先的操作，最终越是在语法树的底层，我们稍后就可以看到。  

实际的解析技术有不少，都非常成熟，列表如下

| 技术   | 方向   | 特点                 | 典型语言       |
| ---- | ---- | ------------------ | ---------- |
| 递归下降 | 自顶向下 | 手写、直观、易于调试         | 大多数手写编译器   |
| LL   | 自顶向下 | 预测分析表、左递归需消除       | ANTLR      |
| LR   | 自底向上 | 强大（几乎覆盖所有CFG）、自动生成 | Yacc、Bison |
| PEG  | 自顶向下 | 无歧义、优先级自然          | 解析表达式文法    |

其中需要提一下，PEG 通过显式优先级消除了歧义，使文法表达更符合直觉，这对于自然地表达语法非常有好处。

手写编译器的语法解析曾经被认为是屠龙之技。即便是现在，也不是每一个专业程序员可以轻松完成的工作。你不必完全掌握手写解析器的所有技巧，但是熟悉这些，特别是局部自重复的分形特征，会强烈塑造你的设计能力。

语法树包含所有语法细节（包括终结符如括号、分号）；AST只保留语义相关的结构。实际构造中，我们甚至可以直接跳过语法树，直接输出AST。

```
语法树（Parse Tree）:
        E
       /|\
      E + T
      |   |
      T   F
      |   |
      F   NUMBER(1)
      |
    NUMBER(0)

AST（抽象语法树）:
        +
       / \
      0   1
```

AST是编译器的核心中间结构，它剥去语法噪音细节（分号、括号、多余逗号，注释等），而完整保留程序层次语义，为下一步的处理提供的基础 。

#### 6.1.3 AST的语义检查
正确的语法可能有完全错误的语义，如`4+"abc"` 语法上是完全正确的，但是毫无意义。识别并报告这种语义错误的的阶段叫做语义分析。

语义分析器的任务是为AST添加语义信息，通常包括：
**1. 符号解析（Name Resolution）**
- 建立符号表（Symbol Table）——从标识符到其定义的映射
- 处理作用域（Scope）：块作用域、函数作用域、文件作用域
- 检测未定义的变量、重复定义

**2. 类型检查（Type Checking）**
- 推导每个表达式的类型（类型推断）
- 验证操作符是否适用于操作数类型（如`int + string`是否合法）
- 验证函数调用的参数类型匹配

**3. 其他语义检查**
- 控制流语句的正确性（`break`只能在循环内）
- 访问控制（私有/公有成员）
- 模式匹配的完整性（如Rust/Scala的match）

**类型系统的角色**：
- 静态类型语言：在编译时完成类型检查
- 动态类型语言：将类型检查推迟到运行时（但现代动态语言也多有编译时的类型注解检查，如TypeScript、Python type hints）

语义分析极大地依赖所谓的类型系统。类型系统是区别两个语言的核心标记之一。关于类型系统深入讨论，我们放在下一章。

语义分析之后，我们拿到的就是合格、有效的AST。解释器可以直接遍历AST并执行其语义；而编译器则进一步将其转换为目标代码。

前端输出的AST已经包含了程序的完整信息，但AST不适合直接进行优化：
- AST保留了源语言的语法结构（判断，循环，递归，跳转等），隐藏了计算本质（控制流）
- AST保留了源语言的数据访问结构（对象，数组，全局，局部等），隐藏了数据依赖的本身（load/store依赖）
- AST的深层嵌套使某些分析困难（如数据流分析）
- 不同类型的语言构造可能干扰统一处理

这就是中间表达（IR） 的引入动机。说到底，AST是语言结构，而IR是执行结构。 

### 6.2 中间表达（IR）
IR（ Intermediate Representation）是编译器前端和后端之间的桥梁，它既“离前端足够远”以支持多种源语言，又“离后端足够近”以有效生成目标代码。它的核心是统一的控制流和数据依赖处理。

#### 6.2.1 IR的设计目标
1. 语言无关。从C、C++、Rust、Swift等不同语言前端生成的IR应具有统一形式
2. 便于优化，IR应暴露程序的“数据流”和“控制流”结构
3. 目标无关，不预设特定的目标机器特性（寄存器数量、指令集）
4. 结构紧凑，IR不应过大，编译速度可接受

#### 6.2.2 IR的三种形式

| 形式            | 特点               | 例子             |
| ------------- | ---------------- | -------------- |
| 结构化（AST-like） | 保持树形结构，接近源语言     | 高层IR（HIR）      |
| 线性（三地址码）      | 每个指令最多三个操作数，接近汇编 | 底层IR（LIR）、字节码  |
| SSA（静态单赋值）    | 三地址码 + 每个变量只赋值一次 | LLVM IR、GIMPLE |

三地址码（Three-Address Code）示例：
```
t1 = a + b
t2 = t1 * c
t3 = t2 - d
```

它的优点是平坦、易于分析、每个临时变量只被定义一次（但若需多次赋值，需不同变量——这正是SSA的前奏）。

#### 6.2.3 SSA（ Static Single Assignment）
SSA核心规则是每个变量在程序中只被赋值一次，这非常适用于数据刘的改造。

```c
// 原始C代码
x = a + b;
x = x * c;
y = x - d;

// SSA形式（引入版本号）
x1 = a + b;
x2 = x1 * c;
y1 = x2 - d;
```

但分支怎么办？

```c
if (cond) {
    x = a;
} else {
    x = b;
}
y = x + 1;
```

在SSA中需要引入φ函数（Phi函数）：

```
x1 = a;          // if分支
x2 = b;          // else分支
x3 = φ(x1, x2);  // 根据执行路径选择x1或x2
y1 = x3 + 1;
```

φ函数不是真实的机器指令，它是告诉编译器：“此处变量的值来自于哪个分支，由到达路径决定”。

SSA的优势是：
- 数据流显式化。每个变量的“定义-使用链”直接可见，便于跟踪，这在数据流分析里至关重要
- 优化简化。常量传播、死代码删除、冗余计算消除等变得简单
- 单一赋值。让许多优化算法从O(n²)降至O(n)

SSA的构造算法主要涉及两部分：
1. 在基本块边界插入φ函数，可以把φ函数理解为多个执行路径合并后的值选择器
2. 对每个变量进行“重命名”，确保每个定义有唯一新版本

#### 6.2.4 控制流图（CFG）
IR通常组织为控制流图（Control Flow Graph, CFG）：
- 节点是基本块（Basic Block），顺序执行的指令序列，只有入口和出口可能分叉
- 边是控制流转移（分支、跳转、函数调用）

```
   [B1: x1 = a + b]
        |
        v
   [B2: cond = x1 > 0]
      /        \
    (true)     (false)
    /            \
   v              v
[B3: y1 = x1*2]  [B4: y1 = x1+5]
   \              /
    \            /
     v          v
    [B5: z1 = y1 + 1]
```

CFG和SSA组合起来，就是优化的理想舞台：
- 数据流通过SSA显式可见
- 控制流通过CFG结构可见
- 两者正交，但可以联合分析

#### 6.2.5 LLVM IR
LLVM IR是当前最成功的编译器中间表达之一。它有以下特点：
- 严格的SSA形式。函数内所有虚拟寄存器都是SSA形式，便于优化和目标代码生成
- 类型系统丰富。基础类型（i32、f64）、派生类型（指针、数组、结构体、向量），便于适配各种风格的语言
- 显式控制流。`br`（条件/无条件分支）、`switch`、`indirectbr`等，便于支持各种语法构造
- 内存模型。使用`alloca`（栈分配）、`load`/`store`访问内存，便于支持直接或者间接的资源管理  
- 内建元数据。调试信息、别名分析信息、循环信息等，这对于生成调试信息、静态分析等场景非常重要

这是一个LLVM IR示例，计算阶乘：
```llvm
define i32 @factorial(i32 %n) {
entry:
  %cmp = icmp sle i32 %n, 1
  br i1 %cmp, label %return, label %recurse

recurse:
  %sub = sub i32 %n, 1
  %call = call i32 @factorial(i32 %sub)
  %mul = mul i32 %n, %call
  ret i32 %mul

return:
  ret i32 1
}
```

LLVM IR的设计哲学包含语言无关，目标无关，复杂度前置等，它的流行无疑证明这些设计都达到了目标

### 6.3 语义保持的优化转换
优化是编译器的智慧所在，通过语义保持的结构重写，生成更高效的目标代码。

#### 6.3.1 优化的分类

| 分类 | 范围 | 例子 |
|------|------|------|
| 局部优化 | 单个基本块内 | 局部公共子表达式消除、常量折叠 |
| 全局优化（过程内） | 整个函数/过程 | 常量传播、死代码删除、循环优化 |
| 过程间优化（IPA） | 跨函数边界 | 内联、过程间常量传播、死函数删除 |
| 链接时优化（LTO） | 跨编译单元 | 跨文件内联、全局变量优化 |

这些优化的本质都可以看作：
- 消除重复计算（1）
- 消除不必要的计算（2）
- 能计算的提前计算（3）
- 使用简单的计算代替等价的复杂计算（4）

以下分别介绍。

#### 6.3.2 局部优化
**常量折叠（Constant Folding）**，属于（3），例如
```
x = 3 + 5;  →  x = 8;
```

**局部公共子表达式消除（Local CSE）**，属于（1），例如
```
t1 = a + b;
t2 = a + b;  →  t1 = a + b; t2 = t1;
```

**强度削弱（Strength Reduction）**，属于（4），例如
```
x * 2  →  x << 1
x * 15 → (x << 4) - x
```

**窥孔优化（Peephole Optimization）**：
对很小的指令窗口（通常2-5条指令）进行模式匹配替换：
```
ADD R0, #0  →  (删除)
MOV R0, R1; MOV R2, R0 → MOV R2, R1
```

#### 6.3.3 全局优化
**常量传播（Constant Propagation）**，属于（3），例如
```c
int x = 5;
int y = x + 3;  // y = 8, 可对y的使用传播
```

在SSA中，常量传播极其简单：跟踪每条SSA值的取值，如果是常量就替换所有使用。

**死代码删除（Dead Code Elimination, DCE）**，属于（2），例如
- 局部死代码：赋值后未使用的变量
- 全局死代码：不可达的基本块、永不调用的函数

SSA简化了死代码检测：如果一个SSA值从未作为ϕ函数的输入或return的参数，它的定义就可以删除。

**循环不变量外提（Loop Invariant Code Motion）**，属于（1），例如：
```c
for (int i = 0; i < n; i++) {
    int t = a * b;  // a和b在循环内不变
    x[i] = x[i] + t;
}
```
↓ 外提后：
```c
int t = a * b;
for (int i = 0; i < n; i++) {
    x[i] = x[i] + t;
}
```

**归纳变量消除（Induction Variable Elimination）**，属于（4），例如
```c
for (int i = 0; i < n; i++) {
    int idx = i * 4;  // 归纳变量
    a[idx] = ...;
}
```
↓ 优化：
```c
int idx = 0;
for (int i = 0; i < n; i++) {
    a[idx] = ...;
    idx += 4;
}
```

#### 6.3.4 过程间优化
内联（Inlining）用函数体替换函数调用，属于（4）。它的优点是消除调用开销（保存/恢复寄存器、参数复制），并且为后续优化创造上下文（如常量传播可进入内联后的函数）。

内联是有条件的，处理不当可能导致负优化。所以编译器会用启发式的规则来决定是否内联，小函数（如getter）、热函数、通过函数指针直接调用的函数往往是良好的可内联候选。

**过程间常量传播**，属于（3），例如：
```c
int get_const() { return 42; }
int x = get_const() + 1;  // 计算后得到43
```

**死函数删除**，属于（2），发现从未被调用的函数，从编译单元中移除。

#### 6.3.5 优化的数学性质
所有优化都可以理解为：
1. 在程序P上定义等价关系`≡`（语义相同）
2. 找到一个变换`T`使`T(P) ≡ P`且`cost(T(P)) ≤ cost(P)`

优化器就是是在程序的等价类中搜索代价最小的表示。

优化必须保守。只有在百分百确信变换保持语义时才能执行。否则，编译器引入了微妙的bug，这种bug往往是最难调试追踪的bug。

未定义行为（UB）指的是语言规范中明确说明没有定义语义的行为，如C/C++的整数溢出。UB是优化的机会，也是危险的来源。下面的这个例子不是好例子，代码不应该这样写：
```c
int foo(int x) {
    return x + 1 > x;  // 总是为真，因为整数溢出是UB
}
// 编译器可优化为 return 1;
```

#### 6.3.6 针对现代CPU的优化
随着 CPU 架构的发展，单纯的标量（Scalar）优化已经难以充分发挥硬件潜力。现代处理器普遍配备了强大的SIMD（Single Instruction, Multiple Data，单指令多数据）单元，能够在一条指令中同时对多个数据元素执行相同操作。这类优化被称为向量化（Vectorization），是当今高性能计算、科学计算、机器学习和多媒体处理中最重要的编译器优化之一。

向量化的本质是将标量循环转换为向量指令，让一条指令同时处理 4、8、16 甚至 32 个数据元素。

```c
// 原始标量循环
for (int i = 0; i < n; i++) {
    c[i] = a[i] + b[i];
}

// 向量化后（以 256-bit AVX2 为例，可同时处理 8 个 float）
for (int i = 0; i < n; i += 8) {
    __m256 va = _mm256_load_ps(&a[i]);
    __m256 vb = _mm256_load_ps(&b[i]);
    __m256 vc = _mm256_add_ps(va, vb);
    _mm256_store_ps(&c[i], vc);
}
```

编译器若能自动完成这一转换，就是自动向量化（Auto-Vectorization）。

自动向量化的主要形式有：

|类型|说明|典型适用场景|
|---|---|---|
|循环向量化（Loop Vectorization）|最常见，对 for 循环进行向量化|数组遍历、线性代数|
|SLP向量化（Superword Level Parallelism）|对基本块内相邻的标量操作进行向量化|表达式树、结构体数组|
|函数向量化（Function Vectorization）|将标量函数转为向量版本|数学库（如 sin、exp）|

流行的编译器如LLVM 和 GCC都会在激活高优化选项时，积极尝试自动向量化。

编译器进行自动向量化时必须严格保证语义等价。主要困难是对数据依赖的了解程度。为此，编译器会在 IR 层面（尤其是 SSA + CFG）进行精细的数据依赖分析和循环变换（如循环展开、循环分布），并结合运行时检查生成多版本：一条向量化版本 + 一条标量备份版本，根据运行时条件选择。

向量化和自动向量化体现了编译器优化的最新趋势。从传统的消除低效转向主动挖掘硬件的并行潜力。它不再只是对程序的局部重写，而是结合目标机器微架构特征进行的深度语义保持变换。这也正是现代编译器越来越依赖具体目标平台优化的原因。

### 6.4 从IR到机器码转换
编译器后端接收优化后的IR，生成特定目标机器（如x86、ARM、RISC-V）的可执行代码。这个过程中有三个核心子问题：指令选择、寄存器分配、指令调度。

#### 6.4.1 指令选择
指令选择是如何将IR映射到机器指令。IR操作（如`t1 = a + b`）可以用多种机器指令序列实现，不同选择在性能和大小上差异巨大。例如（ARM架构）：

```
// IR: t1 = a + b
可能的指令序列：
  ADD t1, a, b        （1条指令）
  
// IR: t1 = a * 3
可能的指令序列：
  MUL t1, a, #3       （1条指令，但MUL可能较慢）
  ADD t1, a, a        （1条指令，加法比乘法快）
  ADD t1, t1, a       （2条指令，但可能更优）
```

现代指令选择通常会把IR看作表达式图，寻找最适合目标机器的指令组合。实践中，往往使用表驱动的指令选择（如LLVM的TableGen）。毕竟一个架构的指令数量是有限的。我们用声明性语言描述指令模式，就可以自动生成指令选择器。

#### 6.4.2 寄存器分配
每个变量都可以看作是一个虚拟的寄存器。所有IR使用无限虚拟寄存器（如LLVM IR里的`%t1`、`%t2`），但真实CPU只有有限个物理寄存器（如ARM有16个通用寄存器、x86-64有16个）。寄存器分配的目标是将大量虚拟寄存器映射到少量物理寄存器，当物理寄存器不足时，将某些值溢出（spill）到内存。

因为访问内存比访问寄存器慢10-100倍（取决于缓存层次）。好的分配器尽量减少溢出，或者将“冷值”溢出到内存，保留“热值”在寄存器。有效的分配可以大大提升程序的运行速度。

图着色寄存器分配（Graph Coloring）是寄存器分配的常见算法，它通过分析虚拟寄存器的依存关系来决定那些需要溢出。其核心思路如下：   
1. 构造干涉图（Interference Graph）
   - 节点是所有的虚拟寄存器
   - 如果两个虚拟寄存器同时存活（生命周期重叠），就给它们连线为边。这意味它们不能分配到同一个物理寄存器
2. 着色。用K种颜色（K = 物理寄存器数）对干涉图进行着色，颜色相同的节点可共享同一物理寄存器
3. 处理溢出。如果图不能K着色，选择部分节点“溢出”，将其值写到内存，后续需要时再读回

示例干涉图：
```
虚拟寄存器: v1, v2, v3, v4
活跃期重叠关系:
  v1 与 v2, v3 都冲突 → 边(v1,v2), (v1,v3)
  v2 与 v1, v4 冲突 → 边(v2,v1), (v2,v4)
  v3 与 v1 冲突
  v4 与 v2 冲突

干涉图类似于图着色问题（NP-hard），但启发式(Kempe's algorithm)在工程上高效。
```

常见的着色办法是启发式着色，以下以K=3物理寄存器为例：
- 先移除度数<3的节点（可保证着色）
- 剩下的图可能包含K+1团，需溢出某些节点

在 JIT 场景中，更常用的是速度更快的线性扫描（Linear Scan）算法，虽然质量略有牺牲。

#### 6.4.3 指令调度
现代CPU非常复杂，也非常强大。一个CPU往往包含多个核。每个核内部的执行都是流水线性质的，一条指令分解为取指、译码、执行、访存、写回等多个阶段。如果下一条指令不依赖当前结果，可以进行指令级并行，同时执行多条指令，也叫做指令重排。

指令调度的任务是在不改变程序语义的前提下，重排指令顺序以隐藏延迟、减少停顿（stall）。

重排有严格的限制，以下情况下不能重排：
- 真依赖/写后读（RAW）。`a = b + c; d = a + e`，指令2必须等指令1写完
- 反依赖/读后写（WAR。`a = b + c; b = d + e`， 可以通过寄存器重命名解决
- 输出依赖/写后写（WAW）。`a = b + c; a = d + e` → 第二次写冲突

在SSA IR中，由于每个变量只赋值一次，WAW和WAR自然消失，这就是SSA便利性的表现。

常见的调度算法是列表调度（List Scheduling）：
1. 计算每条指令的优先级（如后续指令数主导路径长度）
2. 维护就绪队列（所有依赖已满足的指令）
3. 在每个周期，从就绪队列中选择优先级最高的指令发射
4. 更新依赖关系，将新就绪的指令加入队列

更激进的是猜测调度（Speculative Scheduling），将指令移动到分支之前执行（在分支结果确定前执行），使用谓词执行或推测性加载，需要正确性保证（异常处理、内存访问安全检查）。

#### 6.4.4 发射与装配
经过指令选择、寄存器分配、指令调度后，编译器最后做
1. 指令编码。将汇编指令翻译为机器码（二进制）
2. 填充重定位信息。标记符号地址、外部调用、全局变量引用
3. 输出目标文件。（ELF、Mach-O、COFF）或直接输出汇编文本

现代编译器通常依赖汇编器（如GNU as、LLVM's MC）完成最后的编码步骤。

### 6.5 编译器的完整流程
整个编译流程回顾如下：

```
源代码 (C/Rust/Swift/...)
    ↓ [词法分析]
Token流
    ↓ [语法分析]
AST
    ↓ [语义分析 + 符号解析]
带注解的AST
    ↓ [IR生成]
IR (SSA形式)
    ↓ [优化 - O1/O2/O3]
优化的IR
    ↓ [指令选择]
机器指令 (虚拟寄存器)
    ↓ [寄存器分配]
机器指令 (物理寄存器 + 溢出代码)
    ↓ [指令调度]
调度的机器指令
    ↓ [汇编 + 链接]
可执行文件 (.exe, .out, .app)
```

还有一些涉及的编译的扩展：
- JIT编译。运行时编译（如Java JVM的C2编译器、JavaScript V8的TurboFan），混合解释+编译
- 增量编译。只编译变化的模块，适用于大型项目（如C++的`-MMD`）
- 分布式编译。多机器并行编译（如distcc、Incredibuild）

### 6.6 编译器的统一视角
从结构的角度看，从字符流，token流，AST，优化的AST，IR，优化的IR，指令流，二进制流，我们经过一系列转换。每一种不同的表达都对应了不同的结构，底下是分工明确的层次化系统。

#### 6.6.1 翻译的层次系统
编译器体现了从最高抽象到最低抽象的精心分层：
- 源语言层：程序员思维（循环、对象、闭包）
- AST层：语法结构（if、while、函数调用）
- IR层：数据流+控制流（SSA、CFG）
- 指令层：机器操作（ADD、MOV、JMP）
- 二进制层：比特模式

每一层都有更丰富但更模糊（抽象）的上层视角，也有更精确但更复杂（具体）的下层视角。编译器工程师的核心工作是，保持语义的同时，在各层之间做等价变换。

#### 6.6.2 编译器验证
编译器做了大量的工作，它的正确性如何保证呢？特别是优化引入的问题
1. 优化过激。改变了程序语义（尤其是未定义行为的误用）
2. 优化器Bug。实现错误导致生成错误代码

CompCert C编译器是经过形式验证的C编译器，使用Coq证明辅助器证明其生成的汇编代码与源程序语义等价。CompCert成功证明了“小步语义保持”的编译器是可以正式构造的，尽管性能优化时不总能达到极致。

然而这些验证都不是完全的，更多的验证的来自于全面的测试和回归。一个编译器往往经过几年的实际使用才能得到市场的认可。

#### 6.6.3 解释器 vs 编译器
我们从更多的维度看一下解释器和编译器： 

| 维度 | 解释器 | 编译器 |
|------|--------|--------|
| 执行时间 | 边分析边执行（较慢） | 编译后执行（快） |
| 启动时间 | 立即执行 | 需等待编译完成 |
| 代码生成 | 不生成/懒生成 | 生成独立目标代码 |
| 交互性 | 可交互（REPL） | 难交互 |
| 内存占用 | 存储AST/字节码 | 存储机器代码 |
| 经典例子 | 早期Python、Ruby、早期JS | C、Rust、Swift |

尽管如此，混合方案（JIT）还是非常流行：
- 开始解释执行（快速启动）
- 检测热点代码（频繁执行函数/循环）
- 动态编译热点为机器代码（后续执行加速），甚至可进行优化的再编译（如Java的C2、JS的优化JIT）
- 可进行去优化（若假设失效，回退到解释执行），这是动态编译器的独特能力。

### 6.7 本章小结
编译器是构造展现了表达转换的丰富。它从前端词法、语法、语义分析开始，将源语言结构化为AST；通过中间表达（IR）实现语言与机器的解耦，SSA形式的IR暴露数据流以简化优化；优化系统在等价类中搜索更高效的表示；后端通过指令选择、寄存器分配、指令调度完成从IR到机器码的映射。

我们从编译器可以得到的的核心启示是表达可以在不同层次之间无缝转换，只要保持语义。这种分层抽象是计算机系统的通用模式，在复杂系统，如操作系统、数据库、网络协议栈都在广泛使用。
