## 第三章 转换之道：算法设计的策略谱系
算法是精确规定的转换过程，从输入结构到输出结构的确定性映射。如果说数据结构是“骨架”，算法就是“肌肉”，二者共同构成计算的能动力量。

不同的算法范式，本质上是对问题内在结构的不同利用方式。结构决定了哪种转换策略是可能的，自然的、甚至高效的。

针对不同的问题，我们定义了不同的结构，这个过程叫做建模。基于这些结构，我们演化出不同的处理问题的策略。这些策略，构成了算法设计的彩虹。

讨论算法，势必会涉及到算法复杂度的讨论。我们这里仅仅给出一个算法复杂度的定性或者定量结论，而不去证明它。需要指出，算法复杂度本身和问题结构密切相关，可以说结构定义了针对此结构的算法的复杂度下限。

最基础的算法策略是分治，这个直接对应到结构的递归特性。

### 3.1 递归结构的分治
分治可以理解为分而治之的简称。其基本思想是把一个大问题分解为小问题（划分阶段），然后对小问题求解（征服阶段），然后综合得到最终结果（合并阶段）。当小问题依然较大时，继续分解直到足够可以处理（分治的重复调用）。

这个粗看起来毫不稀奇，也是我们日常工作时最常用的策略之一。然而这个最常用的模式对应着深刻的结构内涵。

#### 3.1.1 分治的本质
仔细观察分治的实现流程，可以发现其中的递归特征，使用相同的策略不断下沉处理，直到完成。这个之所以可行，是因为要求解的问题本身是递归结构的。也就是说，分治策略之所以有效，并非因为它创造了什么新结构，而是因为它忠实地反映了问题本身就具备的递归本质。

分治策略的核心在于，问题结构如果具有“自相似性”（递归的本质特征），就能通过递归分解来降低复杂度。

以下是几个分治在不同的领域的例子。

#### 3.1.2 归并排序
归并排序是分治的典型样例，它将分治的三段式执行得最为清晰：
```
归并排序(A, l, r):
  如果 l == r: 返回
  mid = (l + r)/2
  归并排序(A, l, mid)     // 分解
  归并排序(A, mid+1, r)   // 分解
  合并(A, l, mid, r)       // 征服 + 合并
```

合并是归并排序的核心和主导。其实现非常简单。两个已排序子数组的合并是线性的，每次取两数组头部较小者放入结果即可。

归并排序的时间复杂度是`O(n log n)`

归并排序的优雅在于，它不依赖输入数据的任何特殊性质。无论输入如何，复杂度总是`n log n`。这种“最坏情况保证”是以额外`O(n)`空间为代价换来的。

如果以划分过程为主导，我们得到了快速排序。

#### 3.1.3 快速排序
与归并排序不同，快速排序将“合并”步骤省略了，它在划分阶段就完成核心工作。
```
快速排序(A, l, r):
  如果 l >= r: 返回
  p = 划分(A, l, r)   // 重排：A[l..p-1] ≤ A[p] ≤ A[p+1..r]
  快速排序(A, l, p-1)
  快速排序(A, p+1, r)
```

这里划分算法是核心，确保重排之后的结果符合预期。通过原地交换重排，可以在几乎不需要额外存储的情况下，完成划分。

快速排序的复杂度依赖于每次划分时选择的划分基准值。理想的平衡划分的情况下，复杂度是`O(n log n)`，糟糕的情况下，则退化为`O(n²)`。随机选择划分基准可以使得糟糕情况的概率极大减小，所以可以认为快速排序的期望复杂度是`O(n log n)`。

快速排序和归并排序展示了分治的两个变体。一个在划分阶段重排数据，另一个在合并阶段重组数据。它们是对称的，但快速排序的空间优势（原地排序）使其在实践中更常用。

有些结构中的递归并不显然，如乘法。

#### 3.1.4 乘法中的递归
传统乘法中，两个n位数相乘需要`O(n²)`次个位数乘法。Karatsuba（1960）发现，通过把乘数和被乘数分解为独立的部分，可以通过三次乘法完成。

设 
```
x = a·10^{n/2} + b
y = c·10^{n/2} + d
```
那么
```
x·y = ac·10^n + (ad + bc)·10^{n/2} + bd
```

这个表面上需要四次乘法（ac、ad、bc、bd）。但是Karatsuba观察到：
```
z₁ = ac
z₂ = bd
z₃ = (a+b)(c+d) - ac - bd = ad + bc
则：x·y = z₁·10^n + z₃·10^{n/2} + z₂
```

其复杂度约为 `O(n^{log₂3}) ≈ O(n^{1.585})`，对于海量的大数乘法，这个优化是显著的。

Karatsuba打破了一个隐含假设，认为必须按照问题的自然分解来分解。他创造性地重新组合了子表达式，通过减少了子问题数量（从4个减到3个），以加法换乘法，达到优化的目的。这是“变换视角，简化结构”的早期范例。

当然，乘法支持这样的优化，也意味着乘法规则本身的数学结构本身的支持，我们需要更深入地了解问题的结构才能得到更优化的表达。

#### 3.1.5 傅里叶变换中的递归结构
傅里叶变换是处理时序信息的利器，可以把信号从时间的视角转变为频率的视角，这在图像，声音和视频的高效压缩领域扮演了核心的角色。

离散傅里叶变换（DFT）是把一段看似毫无头绪的离散数字信号转换为多个规则正弦波的叠加。这需要把 N 个独立点代入，分别逐个频率计算，时间复杂度是 `O(N²)`。

Cooley 和 Tukey在1965发表了一篇文章，把一个长期在各个领域独立使用的技巧通用化，形式化了，提出了被称为快速傅里叶变换（FFT）的计算方法，其时间复杂度是`O(N log N)`，这个巨大的进步使得数字信号的实时处理成为可能。

FFT的核心是利用了DFT计算公式中的周期性和对称性。拆分阶段，要计算的点分为两部分，一般按照奇偶下标区分，这一步本身不涉及计算；合并阶段，因为有这个周期性和对称性，旋转因子的重复计算项被消除，四次复数乘法转换为一次复数乘法和两次加法！这个合并计算叫做“蝴蝶操作”，因为其数据流图恰如一只起舞的蝴蝶。

FFT的有效性利用的不是问题（信号本身）的结构，而是处理问题的DFT算法的数学结构。分治是对DFT算法的分治。

> 传统教科书通常把FFT的思想归结到分治，我们这里也按照这个分类处理。但是需要指出，FFT 是一个代数结构驱动的重复计算优化器。 它之所以被归为分治，只是因为它的递归分解方式长得像分治。它的递归只是用来摊平计算路径，而真正推动效率的，是旋转因子的周期性和对称性的复用，而不是问题的独立可分性。

#### 3.1.6 分治的限制
应用分治的关键是识别问题结构中的显式或隐式的递归部分，然后用结构化的方式处理之。

分治假设子问题是不重叠的。当子问题重叠时，直接递归会导致指数级冗余计算。比如计算斐波那契数列 `fib(n) = fib(n-1) + fib(n-2)`，分治处理会导致灾难性的复杂性后果 `O(φ^n)`。

想让子问题的结果记忆化，我们需要动态规划。

### 3.2 重复子问题的动态规划
以上面的斐波那契数列计算为例，如果按照递归的方式计算，大量重复计算，时间爆炸。例如算`fib(5)`要反复算`fib(2)`很多次。

#### 3.2.1 记忆化搜索和表填充
动态规划的关键是将子问题的解存储在表格中，每个子问题只计算一次，后续需要的时候查表获取。一般有两种实现路径：
- 自顶向下的记忆化搜索。保持递归结构，但计算结果后存入缓存。容易理解，但是递归本身的调用开销不减少
- 自底向上的表填充。从小规模子问题开始，逐步推导出大规模解。使用迭代代替递归，几乎无调用成本开销，但是理解稍微复杂

记忆化搜索的自顶向下的，以下是记忆化搜索的斐波那契数列计算：
```python
# cache
memo = {}

def fib(n):
	if n <= 1:
		return n

	if n in memo:
		return memo[n]

	res = fib(n - 1) + fib(n - 2)
	memo[n] = res

	return res
```

而表填充是自底向上的的，以下是表填充的实现，可供对照：
```python
def fib(n):
	dp = [0] * (n + 1)
	dp[1] = 1

	for i in range(2, n+1):
		dp[i] = dp[i-1] + dp[i-2]

	return dp[n]
```

#### 3.2.2 动态规划的条件
可见动态规划不是某个具体算法，是一种处理重复子问题的策略。然而并不是所有的问题都适用，有两个必要条件：
1. 最优子结构。问题的最优解包含子问题的最优解。这意味着问题可以递归分解，且子问题的最优解可以组合成全局最优解。
2. 重叠子问题。相同的子问题会被多次遇见。这是DP与分治的区别。没有重叠，DP只是分治加空白的表。

识别一个问题是否具有最优子结构不是显然的。一个规则是，如果一个问题有全局最优解，那么这个解必然可以由它若干子问题的最优解组合得到。

尝试识别是否具有最优子结构一般使用一种类似反证的东西。假设已经有了全局最优解，识别其中对应某个子问题的部分。如果这个部分不是全局最优的，那意味着这替换这个部分我们可以得到更优的，这就证反了最初的假设。

一个问题可以有多个最优解。那么可能有多种最优子结构。最优子结构从属于特定解，而不是一个全局属性。

最短路径问题，爬楼问题都是有最优子结构的，适用于DP；而最长简单路径问题则没有。一个关键的结构洞察是，子问题之间相互独立、互不干扰往往更容易具备最优子结构。

识别出最优子结构，关键就是状态转移。

#### 3.2.3 状态，状态依赖和状态转移
动态规划的核心是状态空间，一个缓存了所有状态的计算结果的集合，这是应对重复子问题的关键。求解过程转化为在状态空间上的最长/最短路搜索。

背包问题说的是，有一堆物品`n`个，其体积和价值分别是`w_i`和`v_i`。假设有一个背包，其总容量为W，求可以放入背包的物品的最大价值。

这里有两个相互纠缠的约束条件，物品的价值要求最大，物品的体积和满足背包的限制。最简单的情况，把所有的物品的组合结果得出来，然后分别计算其价值和体积，选择体积满足要求的最大价值的那一个。这是暴力搜索的思路。需要计算的结果集有$2^n$个，每一个对应一个最终的状态，这个计算是指数增加的。

我们很容易看到其中的巨大浪费。比如假设已经选择了m个物品的集合，其体积之和以及价值是已知的。再选择一个新物品加入集合构成新的集合，完全可以复用之前集合的结果而不用重新计算。这意味着状态的依赖。

从被依赖的状态转入新的状态是状态转移。对于背包问题，假设`dp[i][w]` 是前 i 个物品的所有可能组合，在容量 w 下的最优值，考虑第i个物品的情况，只有两种可能：
- 不选择了物品i，那么`dp[i][w] = dp[i-1][w]`。选择的物品不变，容量不变
- 选了物品i，`dp[i][w] = dp[i-1][w - w_i] + v_i`。因为可以选择，这意味着不等式`w ≥ w_i`必然成立

综合这两种情况，`dp[i][w] = max(dp[i-1][w], dp[i-1][w - w_i] + v_i)`。这样递归下去，直到初始条件 `dp[0][w] = 0`。如果到达了这里，说明我们得到一个最优解。记录每次做出选择，我们甚至可以知道最优结果对应的选择集合！

一般把描述状态转移的数学表达叫做状态转移方程，这个是问题状态依赖的精确对应。

从这个方程出发，我们可以根据这个自然的记忆化递归。也可以预计算可能值填表，使用迭代避免递归的实际使用问题，如调用深度的限制等。

然而需要看到，记忆化搜索只计算实际被访问到的状态，递归的结构本质自动完成了无效状态的裁剪；而迭代因为无法预先获悉这些信息，只能预计算整个状态空间。当状态空间极大或者很稀疏时，记忆化递归比预填充迭代可能更有效。

常见的处理思路是，先用迭代填表，然后在填表过程中只保留必要的信息（如果滚动数组压缩）。或者反过来，用记忆化搜索作为默认实现，只在需要时手动转为迭代。

常见的算法教材中普遍使用预计算迭代，这是因为在大多数经典 DP 问题中，状态空间是稠密的，复杂度是确定的（`O(nW)`），而且有大量的优化预计算的技巧存在。

任何DP问题都对应一个隐式的状态转移图，这是一个有向无环结构（DAG）。求解DP就等价于对这个DAG进行拓扑排序，然后按顺序计算每个状态的值。

#### 3.2.4 DP的限制
DP的问题有一种“纠缠”的属性，当前选择不仅仅依赖于当下，还可能因为未来的选择而无效，这是我们需要遍历状态图的根本原因。当每个状态的最优选择不依赖于未来决策时，问题简化为只根据已知的情况作最好的选择即可，这是贪心策略。

### 3.3 局部最优的贪婪选择
最简单的贪心样例，从一堆已知价值的物品中选择五个物品，使其价值最大。选择最大价值的物品必然不被未来的选择左右。然而情况往往没有这么简单。

#### 3.3.1 贪心的特征
贪心算法的本质特征是未来的步骤不能否认当下步骤的有效性。这个和DP的要求有着本质区别。DP是从有效的全局最优解反推最优子结构，有全局视野（状态转移图）；而贪心是从最优子结构推演全局最优解，通过限定范围，确保最优子结构必然是全局最优解的一部分。

贪心的结构前提：
1. 局部最优选择必然包含在某个全局最优解中
2. 做出贪心选择后，剩余子问题的最优解与贪心选择组合即得全局最优

满足了这些条件，对状态图的遍历简化为一个串行的最优选择处理，最终结果必然是一个最优的全局结果。

#### 3.3.2 活动选择
活动选择问题是适用贪心算法的典型。假设给定n个活动`[s_i, f_i)`，选择最多的互不重叠活动。

策略很简单。每次选择结束时间最早且与已选活动不冲突的活动即可。

这个问题可以用贪心求解，是因为是一个区间调度问题，其相容性关系构成了一个偏序结构。结束时间最早是全局最优的充分条件。

#### 3.3.3 贪心在图结构上的两个变体
最小生成树（MST）问题需要求解的是如何从一个带权重的连通图中生成一棵树。保留所有的节点，且边长之和为最小。

这个问题适用于贪心策略有理论上的解释。直观地说，如果把图上的所有点任意分为两组非空基，这两组中的链接中最短的那一条，必然在最小生成树中。这直接对应到之前提到的约束，一个局部的最优解必然是全局最优解的一部分。

贪心的实现是直观的，如果以点为中心，就是Prim算法。从空集开始，选择每一个点，每次选择时候，总是选择连接已选集合与外部的最小边的那个。

如果以边为中心，则得到Kruskal算法。首先对所有的边排序，然后不断选择最小的边，如果加入这条边不会形成环，就把它加入树中。

#### 3.3.4 贪心构建最优前缀码
哈夫曼编码要解决的问题是如何给字符串编码，使得编码之后的长度最小。因为字符的频率不同，比如英文中e出现的频率最高，而q，z等则很低。正式地说，给定字符频率，构造前缀码使加权平均码长最小。

哈夫曼算法的过程也不复杂。首先把所有的节点放到一个集合中，频率就是权重。每次把其中最小的两个节点拿出来，合并为一个新节点，其权重为两个节点之和，而两个节点分别为这个节点的左右节点，然后放回到集合。不断重复，直到集合中只有第一个节点。最后得到的就是以这个节点为根的哈夫曼树。

所有的节点都在叶子节点上，而频率最小的两个放在最深层。贪心在这里不仅构造编码，还同时构造了前缀码的结构约束。因为所有编码是叶子，没有任何字符的编码是另一字符的前缀。这样可以不需要分割符号就对连续字符编码而不损失精确性。

#### 3.3.5 贪心的边界
如果错误地理解的问题结构，不满足贪心策略的前提条件。那么看起来正确的贪心结果可能是错误的。一个经典的反例的是找零问题。硬币面额\[1, 2, 10, 15, 25\]，金额30。贪心的结果(25+2+2+1)是错误的，最优的是(15+15)。

然而如果面额是真实的常见硬币组合，如\[50,25,15,10,5,1\]，贪心的结果是(25+5)。这是正确的。所以是否使用贪心不仅仅在是什么问题，还在于具体问题的结构。核心的判断准则，还是未来的选择是否可以否决之前的选择。

结论就是，贪心算法的正确性必须证明，不能凭直觉。结构洞察的越深入，越能判断贪心的有效性。

### 3.4 搜索与回溯
当问题没有已知的构造性解法时，或者我们对问题结构的理解不够深入时，我们站在茫茫状态前也不能束手无策。尽管困难，我们可以枚举所有可能性，搜索状态空间。直至找到解。对于规模不大的问题，这个兜底策略依然有效。

#### 3.4.1 可能性空间的结构性搜索
任何搜索问题都可以表示为状态空间树：
- 根节点是初始状态
- 边是动作/决策
- 节点是状态
- 叶节点是终结状态，可能无解

搜索算法就是在树上遍历，直到找到目标节点。

#### 3.4.2 深度优先（DFS）和广度优先（BFS）
深度优先搜索是特征是深钻，通过递归的方式一条路走到黑，直到叶子结点。适用于解空间深但窄，需要找到任意解（而非最优解）的情况。

广度优先搜索的特征是大水漫灌，逐层扩张。适用于寻找最短路径之类的问题。

#### 3.4.3 启发式搜索
当状态空间巨大，盲目搜索不可行，需要启发函数`h(n)`估计从n到目标的代价。

A\*算法是典型的启发式搜索：
- 评估函数：`f(n) = g(n) + h(n)`，其中g(n)是从起点到n的实际代价
- 维护优先队列，按f(n)扩展节点

启发式搜索把对问题的直觉的，局部的理解化为可衡量的函数，嵌入到搜索结构之中。好的启发函数是对问题本质结构的近似，它将问题结构的部分信息编码为估值导向，使搜索能够有目的地聚焦于最有希望的区域，从而大幅减少探索节点数量。

这是我们对问题的结构失去全局理解之后最后的挣扎。

#### 3.4.4 通过剪枝消除不可能的分支
如果可以提前识别并放弃不可能包含解的分支，搜索的效率会大大提升，这个技巧叫做剪枝，就如同给果树剪去不需要的枝丫。

常见的剪枝策略有：
1. 博弈树的α-β剪枝
	- α：当前玩家能保证的最大值（从MAX角度）
	- β：对手能保证的最小值（从MIN角度）
	- 当α ≥ β时，当前分支不可能影响最终决策，可剪枝
2. 约束传播。如数独游戏中，每填入一个数字，删除同行/列/宫的可能值；如某单元格可能值变为空，立即回溯
3. 分支定界。维护当前最优解的上界（或下界），若某分支的下界（或上界）已劣于全局最优，剪去即可

#### 3.4.5 搜索的限制
除了效率上的问题，状态遍历所有还有其他的问题。一个典型的就是搜索选择问题。确定性算法总是选择确定的值作为开始。这容易陷入最坏情况而无法脱身。随机化算法为这种问题提供了逃逸之路。

### 3.5 用随机打破转换的最坏情况
确定性算法对最坏情况的执念，有时是以放弃平均情况的效率为代价。随机化算法用概率打破最坏情况的僵局。

#### 3.5.1 随机化的两种类型
第一种是总是正确但是可能慢的拉斯维加斯算法：
- 总是返回正确结果
- 运行时间是随机的

样例是随机化快速排序。确定性快速排序的最坏情况（已排序输入）是`O(n²)`。随机化主元选择后，其期望时间复杂度不变，但是最坏概率小于`1/n!`，实际可完全忽略不及。

第二种是快速度但是可能不正确的蒙特卡洛算法：
- 以高概率返回正确结果
- 错误概率可降（通过重复运行）

样例是π的计算。在一个边长为2的正方形内随机生成大量点，统计落在内切圆中的点所占比例，即可估算出π的值。采样点越多，估算值越精确，但永远只是逼近而非精确得出。

随机化还可以用于构建平衡结构以及降维。

#### 3.5.2 随机化的使用
跳表（Skip List）用随机化代替平衡树中的复杂旋转操作：
- 每个元素以概率`1/2`提升到上一层
- 期望层数：`O(log n)`
- 查找/插入/删除期望复杂度：`O(log n)`

跳表使用随机化简化数据结构。传统平衡树的“强制结构”被“概率结构”替代，不需要维护严格的全局平衡，只需保证概率意义下的平衡，我们就得到平衡树的几乎所有优点。

高维空间中的最近邻搜索（`O(n·d)`）在百万级数据上不可行。临近敏感哈希（LSH）不去计算这个值，而是把高维向量映射到低维哈希桶，确保相似向量以高概率落入相同桶中。

其构造过程如下，选择随机超平面，然后用向量在超平面哪一侧作为哈希比特。随机独立重复多次，用“与”连接形成最终哈希。这个构造有这样的性质，距离越近的向量，哈希值相同的概率越高。

LSH是在表达层面进行随机化。它不是改变算法流程，而是改变数据的表达方式（将欧氏空间映射到汉明空间），使问题结构在表达后变得易于处理。

#### 3.5.3 随机化的哲学
随机化补上了传统算法理论的确定性缺陷，使得最坏情况出现的概率大大降低，也可以大大优化某些问题上的性能
- 拉斯维加斯：正确但可能慢（最坏情况概率极低）
- 蒙特卡洛：快速但可能错（错误可控制到任意小）

这是工程上的实用主义，也是结果与效率的终极权衡，更是对问题结构深入理解之后的灵活运用。很多时候，足够好且足够快比完美但不可能更有价值。

### 3.6 转换的结构化视角
回过头来，重新审视一下不同约束下的算法设计，可以看到明显的关系。这个关系然我们更深刻理解转换本质上是对结构的映射。

#### 3.6.1 转换范式之间的深层关系
这些范式中，当存在可递归的模式时，动态规划是核心：
- 任何分治问题都可以写成DP，如果子问题结构显式化。
- 任何贪心算法都是DP的特例，当转移简化为取一个确定的方向
- 任何搜索问题都可以看作DP，状态空间作为表格，但往往太大导致“维度灾难”

其中，递归分解和表填充分别对应了相同策略的两种不同处理模式；深度优先和广度优先的遍历顺序对应不同问题结构。贪心和搜索则是两个极端，前者要求局部最优必然对应到全局最优的一部分，后者则没有任何要求。

按照时间复杂度，我们可以给出一个直观的谱系：

**时间复杂度谱系**
```
O(1) ← O(log n) ← O(n) ← O(n log n) ← O(n²) ← O(2^n) ← O(n!)
常数     对数      线性    线性对数     平方     指数     阶乘
                                     ------工程上不可行------
哈希    分治/DP                           
       二分搜索      
        贪心               搜索
```

#### 3.6.2 范式的选择取决于结构洞察
不同问题需要不同范式，但范式的选择取决于对问题**内在结构**的洞察：

| 问题结构特征       | 推荐范式 |
| ------------ | ---- |
| 子问题独立、可合并    | 分治   |
| 子问题重叠、有最优子结构 | DP   |
| 局部最优隐含全局最优   | 贪心   |
| 结构未知、需枚举可能性  | 搜索   |
| 最坏情况罕见、可接受概率 | 随机化  |

最短路径问题为例，不同的结构特征决定了合适的算法
- 结构是“层次化的”，无权图广度优先
- 边的非负性， 经典Dijkstra
- 经过k条边的最短路径， 负权Bellman-Ford
- 一般图的最短路径问题，DP范式的Floyd-Warshall

同一个问题，在不同的约束条件下，就浮现出不同的算法，这是结构决定策略的最佳印证。

#### 3.6.3 再看自顶向下和自底向上
自顶向下是分解式的。从问题本身出发，不断递归分解，分治、DFS、记忆化搜索都是直接使用。最大的优点是符合直觉，结构自然。递归的现实开销可能很大，重复计算也在所难免
- 缺点：可能重复计算，递归开销

自底向上是组合式的。从小结构开始，组合成更大结构，这是充分理解的问题本质后对求解问题的优化。DP填表、贪心（逐步构建解）、Kruskal（逐步合并边）都是这样的例子。它没有递归开销，容易优化空间，但是需要预先识别状态空间和计算顺序，理解起来也相对复杂。

### 3.7 本章小结
这是非常困难的一章。恭喜你，穿越重重迷雾和艰难险阻，和我们一同抵达了转换的终点。

几乎所有的可解问题，都对应到一个复杂的内部状态转移过程，最终的解决方案就是在迷宫一般的状态转移中找到那条我们期望的道路。

理想情况下，大问题可以拆分为小问题并且小问题的解可以合并为大问题的解，我们可以用分治策略来处理。不断地分治，直到小问题有明确解，这是一个递归的过程。优化方向有：
1. 当小问题出现重叠时，这意味着优化的空间。动态规划通过记忆化消除冗余计算，并且根据状态转移方程将状态空间结构化为有向无环图。这个途中，所有的无效节点已经被剔除。问题转化为图算法，拓扑序求解，甚至最短路径都可以派上用场。
2. 当小问题的最优解必然是全局最求解的一部分时，将动态规划可以简化为贪心的串行决策。这个约束意味着，未来的决定必然不能影响已经完成的决定

没有划分小问题求解的结构性解法时，我们只能在状态空间中漫游。低效，但是是寻求可解方案的唯一办法。随机化处理有助于用概率打破最坏情况诅咒。

这些范式的统一视角揭示了算法设计的本质。不同策略是对问题内在结构理解之上做出的假设和操作方式。我们对问题结构了解的越深，策略就越高效。

递归是处理第一类问题最强大的思想武器，迭代是给出可行实现的最可靠出路。但对结构的细微理解无法被有效地形式化表达，目前我们还做不到从递归思路到迭代算法的一般性自动转换。这个领域，正是当前计算理论研究的热点之一。
