## 网络系统：应对连接复杂性的结构

### 10.1 网络是什么
我们在理解网络时，关注的是带宽、延迟、具体协议。但真正决定网络本质的，是哪些问题被放在哪一层解决，以及复杂性最终由谁承担。

协议分层并非单纯的工程技巧，而是对责任边界的划分。网络更强调的是结构，而非表达或者转换。很快，我们就会看到在不同的复杂性领域，网络要解决的问题是什么，以及最终的结构又是什么。

网络亦即网状联络。它首先是可见的物理链接，然后是其上的应用。一个网络可以使用，首先需要回答几个问题：
- 比特如何传输？
- 相邻节点如何通信？
- 数据如何跨网络转发？
- 丢包、乱序、拥塞如何处理？
- 共享状态如何同步，数据语义是什么？

这些问题，分别对应物理传输，节点链路，网际交换，数据可靠性和一致性等不同的抽象层级。不同的应用使用了不同的方案，最终得到完全迥异的网络结构。所以不同网络的最大区别，不是其协议，而是使用什么结构应对复杂性。我们从计算、存储与通讯三个方面展开。

### 10.2 计算网络
对计算网络来说，算是唯一的目的，越快越好，所以性能是计算网络的关键。

芯片运行急速，但是伴随的存储（内存，显存以及HBM）速度却无法匹配，这导致了“内存墙”问题。这个问题在单芯片时代已经很严重。到了多核、多 GPU、分布式计算时代，它变成了决定性的瓶颈。然而，多核的情况下，芯片本身如何互联是第一个需要解决的问题。

#### 10.2.1 从全互连到 Chiplet
我们把一个完整的物理封装的计算单元叫做芯片，其中一般有多个独立的核心。当芯片中的独立核心较少时，全互联是最简单的。但是现代芯片有甚至超过上百个独立的核心，全互联 已经不能满足要求。典型的链接拓扑列表如下：

| 核心数  | 典型拓扑           | 代表产品                  | 特点            |
| ---- | -------------- | --------------------- | ------------- |
| 2-4  | 全互联            | 早期双核/四核 CPU           | 简单直接，但 N² 布线  |
| 4-16 | 环形总线           | Intel Ring Bus        | 核心串成一圈，延迟均匀   |
| 8-64 | 2D 网格/网状       | Intel Mesh, Apple M系列 | 二维寻路，适合更多核心   |
| 64+  | Chiplet + 片内网络 | AMD Infinity Fabric   | 多个小芯片通过高速链路互联 |

Chiplet 是当前的技术前沿。不再追求把 128 个核心做在一个巨大芯片上（良率太低，成本太高），而是做多个小芯片（每个 8-16 核），然后用高速串行链路把它们“缝合”起来。对上层软件来说，它们看起来像一颗统一的 CPU。

AMD 的 Infinity Fabric 是目前最成熟的 Chiplet 互联方案，片内带宽达 TB/s 级，片间点对点链路也达到数百 GB/s。

当很多芯片连在一起之后，面临的巨大挑战是如何保持他们的状态一致。毕竟每个芯片都有缓存，而且不止一级。

#### 10.2.2 MESI 协议
MESI（Modified-Exclusive-Shared-Invalid）是最经典的缓存一致性协议。它为每个缓存行定义了四种状态：

|状态|含义|体现的性质|
|---|---|---|
|M (Modified)|本核心已修改，其他核心的副本都过时了|该核心“拥有”这行数据|
|E (Exclusive)|本核心可修改，其他核心没有副本|独享但尚未修改|
|S (Shared)|多个核心都有干净副本|共享状态|
|I (Invalid)|本核心的副本无效|需要重新加载|

当一个核心要写入一个处于 S 状态的缓存行时，它需要先广播一条“RFO”（Read For Ownership）消息，告诉所有其他核心：“我要独占这行，你们的副本都变成 I。”

MESI 是硬件状态同步协议，不需要网络和传输层，更不需要重传机制。这一切缘于固定的拓扑和确定的路径（以微米计）带来的确定延迟，硬件可以极致优化。

MOESI 增加了 O（Owned）状态，允许某个核心代表所有共享者把数据写回内存，减少不必要的内存访问。AMD 的 Infinity Fabric 广泛采用 MOESI。

#### 10.2.3 GPU 互联的特别要求
GPU是用于图形加速计算的芯片，因为其强大的并行性，成为AI时代的核心计算牛马。GPU 互联与 CPU 互联的核心区别在于，GPU 几乎总是成组工作。

在 AI 训练中，常见的模式是数据并行。每张 GPU 拥有模型的一个完整副本，被喂入不同的数据批次，算出梯度后，所有 GPU 的梯度需要求和取平均（All-Reduce），然后每张 GPU 再用相同的平均梯度更新自己的模型。

这意味着GPU互联的核心挑战是，如何支持高效的集体通信。

##### 10.2.3.1 全互联的NVLink
NVLink 是 NVIDIA 的私有高带宽互连协议，它的设计目标非常明确，让 GPU 之间像访问本地内存一样访问对方的内存。

关键特性：
- 点对点、全双工。每对 GPU 之间有一条独占的“车道”
- 统一内存地址。GPU A 可以直接解引用 GPU B 内存空间中的指针
- 硬件转发。访问远程 GPU 内存的请求由硬件自动处理，无需软件介入

NVLink 的带宽演进表：

|代际|单向带宽（每链路）|每 GPU 链路数|聚合带宽|
|---|---|---|---|
|NVLink 1.0|20 GB/s|4|80 GB/s|
|NVLink 2.0|25 GB/s|6|150 GB/s|
|NVLink 3.0|50 GB/s|12|600 GB/s|
|NVLink 4.0|100 GB/s|18|900 GB/s|

作为对比，PCIe 5.0 x16 的单向带宽约为 64 GB/s，NVLink 4.0 的带宽是它的 14 倍。

##### 10.2.3.2 NVSwitch
当 GPU 数量超过 8 张，点对点全互连就不再可行（因为连接数按 N² 增长）。NVSwitch就是引入作为专门的 GPU 交换机。但 NVSwitch 与传统交换机有一个本质区别，它可以在内部执行计算。具体来说，NVSwitch 支持所谓交换机内的 All-Reduce 加速：
- 多个 GPU 把梯度数据发送到同一个 NVSwitch
- 交换芯片再转发过程中对数据执行归约
- 归约结果再广播回所有 GPU

传统交换机只负责转发，NVSwitch 负责转发+归约。这是典型的网络计算化。

NVIDIA DGX H100 系统用 NVSwitch 将 32 个 H100 GPU 连接成一个逻辑上的巨型 GPU。任意两个 GPU 之间的带宽都是 900 GB/s，延迟一致。

##### 10.2.3.3 InfiniBand 与 RoCE
当GPU数量继续增加，仅仅NVSwitch不够了，需要用外部网络连接多个 NVSwitch 系统。InfiniBand 是目前 AI 训练的首选。这是一套为高性能计算从头设计的网络协议栈：
- 天生支持 RDMA
- 硬件级的自适应路由
- 基于信用的流控，天然无损
- 端到端延迟低至 300-500 纳秒

RoCE（RDMA over Converged Ethernet）是后来者。不同于InifiBand，RDMA 可以直接跑在标准以太网上。这样成本更低，生态更开放，当然性能也不如InfiniBand，还需要额外的机制（PFC、ECN）来保证无损，配置复杂度高。UEC（Ultra Ethernet Consortium）尝试为 AI/HPC 重新设计以太网栈，目标是让以太网在未来具备与 InfiniBand 竞争的能力。

跨节点互联目前没有统一的技术架构，下面是几个相互竞争的方案：

|厂商|GPU 互联方案|特点|
|---|---|---|
|NVIDIA|NVLink + NVSwitch + InfiniBand|最成熟的生态，性能最强，但生态封闭|
|AMD|Infinity Fabric + XGMI|开放（CXL 支持），但缺少统一交换机，奋力追赶中|
|华为|星脉（自有协议）|在交换机层面实现集合通信加速，网络能力强大，潜力选手|
这些方案尽管技术实现不同，但是架构上大同小异。

#### 10.2.4 CPU 和 GPU 的互联 
传统上，CPU 和 GPU/加速器之间通过 PCIe 总线连接。PCIe 的设计要求所有通信都要经过 CPU，GPU 无法直接访问 CPU 内存，数据需要在 CPU 内存和 GPU 内存之间来回拷贝。MESI也无法管理 GPU 缓存，一致性无法简单保证。

##### 10.2.4.1 CXL
CXL（Compute Express Link）运行在 PCIe 物理层之上，但提供了三种子协议，分别解决不同的问题：

| 子协议       | 全称     | 功能                     |
| --------- | ------ | ---------------------- |
| CXL.io    | I/O    | 设备发现、配置、中断（标准 PCIe 功能） |
| CXL.cache | Cache  | 加速器可以缓存 CPU 内存，并维护一致性  |
| CXL.mem   | Memory | CPU 可以访问加速器上的内存（如 HBM） |

CXL 的革命性在于缓存一致性跨设备扩展。CPU 缓存、GPU 缓存与外部内存池可共享同一套一致性语义（基于 MESI 变体），CXL 3.0 甚至引入了交换能力，允许构建由数百个设备组成的内存池。

数据中心正在从机器的集合演变为统一内存系统。Google、Meta 已经在数据中心内部试点 CXL 内存池化。

##### 10.2.4.2 远程直接内存访问（RDMA）
DMA机制允许我们不经过CPU，直接在IO设备和内存之间传输数据。RDMA更进一步，允许我们直接写入远端内存。

传统的IO路径中，数据包到达需要CPU介入处理以及昂贵的内核态用户态拷贝，典型流程如下
```
NIC → 内核 → TCP → Socket → 用户态
```
整个过程延迟约 10-30 微秒，且占用 CPU。

RDMA 路径中，数据直接经网络写入远端内存：
```
NIC → 远程内存
```
RDMA 绕过内核和 TCP，直接把数据写入远程内存，端到端延迟可低至 1-2 微秒，CPU 几乎不参与，这是数量级的提升。

RoCEv2 是目前 AI 集群中广泛采用的 RDMA 方案，它在以太网上承载 RDMA，但要求网络提供无损传输，这需要 PFC（优先级流控）和 ECN（显式拥塞通知）等机制配合。

#### 10.2.5 计算互联的未来
计算互联技术在飞速发展中，新的思想和技术不断涌现。这为新的计算结构开辟了方向。

##### 10.2.5.1 光互连
电信号的损耗随距离急剧增长。光互连（硅光、CPO）的核心价值是延迟与功耗不再随距离线性增长。这意味着：
- Chiplet 之间的“距离”变得不那么重要了 
- 整机柜可以被视为“一个芯片”
- 甚至跨机箱的连接也可能达到接近片内的延迟

这会使得当前的硬件设计可以完全从头开始。

##### 10.2.5.2 可编程的计算网络
AI 模型规模增长的速度超过了单节点扩展的速度。未来可能需要动态组合计算资源，今天这 1000 张 GPU 训练模型 A，明天重新配置，换成 2000 张训练模型 B。

这提出全新的要求，可以动态改变计算网络的拓扑。这是可编程交换机（P4）的思路。

##### 10.2.5.3 网络即计算
如果物理距离可以随意改变，连接拓扑可以动态调整，我们得到的就不是计算网络，而是一个完全消融了计算核心，存储支持，网络互联的超级计算机，一个被距离拉伸的超级计算机。我们可以选择在哪里计算、在哪里存放数据、在哪里做一致性同步。

#### 10.2.6 计算网络的结构
回顾为了榨干计算核心的每一次设计，我们可以看出一些明显的设计方向。

**逻辑上统一的地址空间**
MESI、NVLink、CXL 都在追求同一件事，让远程内存看起来像本地内存。统一地址空间消除了“消息传递”的开销，同时保证了一致的语义，让硬件可以透明地处理数据移动。

**拓扑上尽可能固定的网络**
无论是固定网格（CPU 互联），预定拓扑（NVSwitch 系统），还是静态配置（CXL 池化），都不喜欢动态路由，因为动态慢而且无法有效硬件支持。有了相对固定的拓扑，极致的优化才有了基础。 

**能力下沉到硬件**
无论是从 NVSwitch 的交换机内 All-Reduce，还是 SmartNIC 的 RDMA 卸载，更不用说保持一致性性的MESI，核心就是通过硬件直接实现来提供极致性能。它的前提就是可靠，可预测的硬件延迟。   

现代计算网络正在系统性地把复杂性下沉到硬件，如NIC、交换机、DMA 引擎。

这些设计原则，导致一个明显的后果就是，部分失败是不允许的。假设你的一个16核的CPU上面有一个core坏掉了，坏掉一个似乎无足轻重，但是因为一致性协议会陷入死锁或报错，整个系统的内存状态变得不一致且不可预测。对于硬件来说，这不是一个可以恢复的错误。CPU会触发一个最高优先级的硬件异常，操作系统内核收到这个异常后，唯一安全的动作就是立即停止所有操作，输出诊断信息，整个系统宕机。

计算网络是以性能和可靠性为基础设计的。就像一辆一级方程式赛车。F1赛车在赛道上性能无敌，但一个螺丝钉出问题就可能让它彻底停摆！

现代大规模计算网络使用不同的连接拓扑，可以做到优雅降级和软隔离（隔离坏掉部分所有的直接相连core，而不是仅仅那个坏掉的），代价是毫秒级的恢复时间。这是在性能和可用性之间取得新平衡。最后我们会重新回到这个主题。  

### 10.3 存储网络 
对存储网络来说，存是核心的责任。存储必须有效，这意味着可靠。

对于大部分人来说，计算机看的存储都是直接连接到计算机的，可能是主板上的SATA和NVMe接口，也可能是通过USB或者雷电接口直连的移动存储设备。这种直连存储（DAS）的结构主要依赖于其连接的接口，而存储网络则是专门为存储而设计的网络。

#### 10.3.1 存储网络要解决的问题
存储网络的需要保证，数据一旦确认写入，就永远不会丢失。

这个目标听起来简单，但它带来的约束是根本性的：
- 写操作必须是同步的。当存储系统说“写入成功”，意味着数据已经安全落盘（或至少落入了可以恢复的持久化介质）。不能是“可能成功了，也可能还在缓冲区里”。
- 顺序必须被保证。如果应用先写A再写B，那么任何时候读取，都不能出现B在A之前的情况。
- 故障必须可恢复。无论因为什么原因中断，已确认写入的数据都必须能被读出来。

这些约束到来的挑战有：
- 可靠性要求数据“立即确认”。写入操作必须等待数据真正安全后才能返回
- 距离和介质带来延迟。网络传输需要时间，磁盘写入需要时间
- 但应用希望越快越好。

解决这个矛盾的方法只有一个，用尽可能多的机制保证不丢，然后把速度做到能接受的水平。

存储网络不追求计算网络的纳秒级延迟，也不追求通讯网的全球覆盖。它追求的是在控的范围内（如数据中心），在可预测的毫秒/微秒级延迟下，保证数据读写的确定性。

#### 10.3.2 存储网络的评价指标
理解存储网络，需要记住这三个核心指标：

| 指标 | 含义 | 典型数值 |
|------|------|----------|
| **延迟** | 从发出写入请求到收到确认的时间 | FC: 3-10μs, NVMe-oF RoCE: 10-20μs, NVMe/TCP: 50-100μs |
| **可靠性** | 数据不丢不损的保证程度 | 端到端CRC + 信用流控 + 多路径，设计目标可达10^-20以下的未检出错误率 |
| **可用性** | 系统持续提供服务的能力 | 双控制器 + 双Fabric，典型设计目标99.9999%（每年宕机<30秒） |

这三个指标，存储网络都要，但可靠性是第一优先级的硬约束，延迟和可用性可以为此妥协。

#### 10.3.3 光纤通道（FC）
光纤是现代互联网的基础设置。存储网络也是光纤，叫做FC。它有自己完整的5层协议栈，自己的交换机，自己的命名空间，自己的流控机制。于是经典问题来了，FC为什么不用IP？

因为TCP/IP在设计时做了一个存储网络无法接受的妥协，它允许丢包。FC的选择是，不在网络内部丢包。 为此，它设计了一整套机制。

##### 10.3.3.1 FC协议栈
FC有完整的5层协议栈，每一层都为存储做了专门优化：

| 层级 | 名称 | 功能 | 特点 |
|------|------|------|------|
| FC-0 | 物理层 | 光/电接口、信号传输 | 支持1G到128Gbps速率 |
| FC-1 | 编码层 | 8b/10b或64b/66b编码 | 保证DC平衡、时钟恢复 |
| FC-2 | 帧/流控层 | 帧结构、信用流控、路由 | 核心：信用机制保证无丢包 |
| FC-3 | 公共服务层 | 加密、压缩、别名 | 可选功能 |
| FC-4 | 上层映射 | SCSI、NVMe、IP等 | 把上层协议映射到FC帧 |

FC-2层同时处理了“帧”和流控，以及路由。它没有像IP那样把网络层和传输层严格分开。因为存储网络的拓扑是相对静态的，不需要动态路由的复杂性。

##### 10.3.3.2 FC的流控
FC不采用TCP那种“发送→等待ACK→再发送”的模式，而是使用基于信用的流控（Credit-based Flow Control）。其基本原理是：
1. 链路初始化时，接收方告诉发送方，“我有N个缓冲区可用。”
2. 发送方每发送一个帧，就消耗一个信用。
3. 当信用为0时，发送方必须停止发送。
4. 接收方处理完一个帧后，发回一个R_RDY（Receiver Ready）信号，归还一个信用。

这意味着发送方永远不会发送接收方无法处理的帧， 网络内部永远不会因为缓冲区满而丢包。代价则是信用管理需要额外的硬件开销，且信用协商增加了初始化的复杂性

##### 10.3.3.3 FC的拓扑
FC支持三种拓扑：

| 拓扑 | 名称 | 特点 | 状态 |
|------|------|------|------|
| 点对点 | 两台设备直连 | 最简单 | 仍在使用 |
| 仲裁环(FC-AL) | 多台设备串成环 | 共享带宽，一个故障影响整个环 | 基本淘汰 |
| 交换结构(Fabric) | 通过FC交换机互联 | 每台设备独占带宽，支持大规模 | 主流 |

现代FC网络几乎都是交换结构（Fabric），通常配置为双Fabric（A/B网）以实现高可用性。

因为是基于光纤，主流的速率可到32G甚至64G。最新的128GFC的标准已经完成。

FC因其可靠性和性能，在中高端存储阵列中普遍使用，在金融、医疗、政府等对可靠性要求极高的领域，更是首选。然而，新型的NVMe-oF正在对其形成挑战。 

#### 10.3.4 NVMe-oF
FC上跑的存储协议主要是SCSI，一种为旋转介质（如磁带，磁盘）设计的存储协议。NVMe（Non-Volatile Memory Express）是专为SSD设计的本地访问协议。它比古老的SCSI/SAS协议高效得多：

| 特性 | SCSI/SAS | NVMe |
|------|----------|------|
| 队列深度 | 1个队列，深度254 | 64K队列，每队列深度64K |
| 中断处理 | 每个IO一个中断 | 中断合并，减少CPU开销 |
| 延迟 | 50-100微秒 | 10-20微秒 |

但NVMe是本地协议，只能访问本机的SSD。NVMe-oF（NVMe over Fabrics）把NVMe的命令语义扩展到网络上，让一台机器可以像访问本地SSD一样访问远程的NVMe设备。

不是简单把SCSI命令换成NVMe命令，NVMe-oF重新思考了整个存储协议栈，它把NVMe的精简、并行、低开销的设计理念带到网络中，形成了一个专门针对固态介质设计的高效存储网络。

NVMe-oF有三种完全不同的实现方式，各自体现了“复杂性由谁承担”的哲学。
- NVMe/FC，这个最简单，直接在FC上跑NVMe协议。这是全闪存阵列的直观实现方式。非常适合现有FC基础设施的用户，成本较高，最成熟。
- NVMe/RoCE，在以太网上跑NVMe协议，同时引入RoCE（RDMA over Converged Ethernet），把RDMA的零拷贝、内核旁路能力带到以太网上。NVMe/RoCE是性能/成本平衡最好的方案，但也是最娇气的，因为它要求底层以太网必须无损，而这又不是以太网的设计目标。可用通过开启PFC（优先级流控），配置ECN（显式拥塞通知）以及精心规划缓冲区大小和拓扑达到要求，但是相当脆弱。
- NVMe/TCP，完全依赖以太网。这是最亲民的方案，既不需要专用硬件，也不需要特殊的网络配置，标准TCP/IP网络就能跑，随时随地都可以部署。代价是延迟最高，CPU开销最大。但它的优势也很明显，可以在任意支持TCP的地方部署，包括云环境。

三种NVMe-oF实现方式的对比如下：

| 维度 | NVMe/FC | NVMe/RoCE | NVMe/TCP |
|------|---------|-----------|----------|
| 延迟 | 5-10μs | 10-20μs | 50-100μs |
| CPU开销 | 低 | 极低（RDMA） | 高 |
| 网络要求 | 专用FC网络 | 无损以太网 | 标准以太网 |
| 配置复杂度 | 中 | 高 | 低 |
| 成本 | 高 | 中 | 低 |
| 适用场景 | 传统企业存储 | AI/HPC存储 | 云存储、边缘 |

#### 10.3.5 存储网络的可靠性
存储网络为了实现不丢数据这个目标，建立了一整套可靠性机制。

**信用流控**
已经在FC部分详细说明。核心作用是防止拥塞丢包。

**端到端校验**
端到端校验指的是存储网络几乎在每一层都提供了校验能力：  

| 机制 | 层级 | 保护范围 | 检出能力 |
|------|------|----------|----------|
| 链路CRC | FC-2 / 以太网FCS | 单条链路 | 链路层错误 |
| T10 PI (DIF) | SCSI/NVMe | 主机到存储设备 | 端到端完整性 |
| NVMe端到端保护 | NVMe | 主机到NAND | 覆盖全路径 |

T10 PI（保护信息） 是存储领域最重要的端到端校验机制。它在每个数据块（通常是512字节或4KB）后附加8字节的保护信息，包括：
- CRC校验。检测数据损坏
- 引用标签。检测写错位置（比如应该写块100，实际写到了块101）
- 应用标签。应用层自定义的保护

**多路径（MPIO）**
存储网络通常设计为双Fabric（A/B网）：

```
       主机
      /    \
   HBA    HBA
    |       |
   FC      FC
  Switch  Switch  (A网和B网独立)
    |       |
  Storage Controller 1
        |
    后端存储
```

可以看到
- 主机同时通过两个HBA、两个Fabric连接到存储
- 操作系统MPIO驱动管理多条路径
- 正常时负载均衡或主备模式，故障时自动切换到健康路径，应用无感知

**控制器高可用**
存储阵列本身通常采用双控制器（Active-Active）设计：

| 特性 | 说明 |
|------|------|
| 缓存镜像 | 两个控制器的缓存互相镜像，一个故障时数据不丢 |
| 故障切换 | 一个控制器故障，另一个接管其存储单元（LUN），通常<30秒 |
| 在线升级 | 可以轮流升级控制器，不中断业务 |

**持久化与恢复**
恢复机制可以是存储网络的，也可以是应用层的：

| 机制        | 说明                    |
| --------- | --------------------- |
| 预写日志(WAL) | 写入数据前先写日志，断电后可恢复      |
| 检查点       | 定期保存完整状态，缩短恢复时间       |
| 幂等操作      | 同一操作多次执行结果相同，支持安全重试   |
| NVRAM保护   | 写缓存有电池或电容保护，断电时数据写入闪存 |

#### 10.3.6 可靠性层级
把所有机制放在一起，可以看到一个分层的可靠性模型：

| 层级 | 机制 | 解决的问题 |
|------|------|-----------|
| 应用层 | T10 PI、NVMe端到端保护 | 静默数据损坏 |
| 传输层 | 信用流控、TCP重传 | 丢包、乱序 |
| 网络层 | 多路径、双Fabric | 链路/交换机故障 |
| 控制器层 | 双控制器、缓存镜像 | 控制器故障 |
| 设备层 | RAID、副本、擦除码 | 硬盘/闪存故障 |
| 机房层 | 双活数据中心、异步复制 | 站点级灾难 |

这些分层设计使得任何一层的可靠性问题不会穿透存储网络，为存储网络提供了无以伦比的可靠性。 

#### 10.3.7 存储网络设计原则
回顾所有技术，我们可以提炼出存储网络的设计原则：
- 写操作必须同步确认
- 在每层都做可靠性，而不是推给边缘
- 可用性通过冗余获得。通过双控制器、双Fabric、多路径，任何单一故障都可以被掩盖
- 性能在可靠性之后优化
- 确定性优于极致性能

#### 10.3.8 未来的存储网络
FC的市场份额在缓慢下降，NVMe-oF（特别是RoCE和TCP版本）在上升。但FC在金融、医疗、政府等极端重视可靠性的领域，它仍然是标准。

一个可能的趋势是NVMe/TCP + 智能网卡。随着SmartNIC/DPU的普及，原来由CPU处理的TCP/IP协议栈可以被卸载到硬件，NVMe/TCP的延迟有望降低到接近RoCE的水平，同时保留其易于部署的优势。

计算存储（Computational Storage）是另一个重要趋势。在存储设备内部嵌入计算能力（ARM核心、FPGA），让数据在原地被处理，而不是搬到主机。

| 场景 | 传统方式 | 计算存储 |
|------|----------|----------|
| 数据库过滤 | 把整列数据搬到主机，CPU过滤 | SSD内部过滤，只返回结果 |
| 备份去重 | 备份软件读取数据并计算哈希 | SSD内部计算哈希，只返回指纹 |

NVMe-oF已经支持计算存储命令，这可能会改变存储网络的流量模式，从搬运大量原始数据变成只搬运少量计算结果。

CXL（Compute Express Link）允许CPU和内存设备以缓存一致的方式共享内存；这极大地扩充了内存的范围，跨系统的抽象内存池意味着几乎无上限的内存。这是计算网络的架构，但是必然深刻影响存储网络的使用和设计。     

> 持久内存（PMEM）曾经做了一个被证明错误的尝试。它直接消除内存和存储网络的界限。事实证明这打破了计算网络和存储网络的抽象隔离，优势不足以抵消其带来的复杂性，最终商业上失败了。 

### 10.4 通讯网络
对于通讯网来说，通就是唯一的信仰。网络需要尽可能通达，这是可用性要求。

计算网络和存储网络的规模都是严格局限的，但是通讯网络截然不同。通讯网络的规模完全没有限制，从两个计算的小网，到跨越全球的大网，甚至未来跨越地月系统的星际网络，都完全适用。

这导出一个简单的事实，通讯网络不是为了快或准而设计的，它是为了通而设计的。让世界上任意两个节点，在任意时间，无论中间发生了什么，都能找到彼此。

在计算网络中，一个核心坏掉，整个CPU可能宕机。在存储网络中，一个比特写错，整笔交易可能作废。在通讯网络中，一台路由器坏了，数据包会自动绕路，你可能永远不会知道。

#### 10.4.1 通讯网络要解决什么问题
计算网络连接的是几十个CPU核心，最多几千个GPU。存储网络连接的是几百台服务器和存储阵列。通讯网络要连接的是数十亿个设备。手机、电脑、服务器、传感器、摄像头、汽车……它们分布在全世界，由成千上万个独立的运营商管理，随时可能加入或离开。

这个规模，是计算网络和存储网络无法想象的。

计算网络可以假设“拓扑固定、路径已知”。存储网络可以假设“规模有限、管理员可控制”。通讯网络不能做任何假设，它不能假设任何东西是可靠的。

通讯网络面对的核心矛盾有：
- 规模要求去中心化。没有单一机构能管理全世界的网络。
- 容错要求冗余路径。任何节点都可能故障，网络必须自动绕行。
- 异构要求分层解耦。不同运营商、不同技术、不同年代的设备必须能互通。
- 但用户希望“看起来像一个网络”。上层应用不应该关心底层是光纤还是5G，是国内还是跨国。

解决这个矛盾的方法就是把复杂性推到边缘，让核心尽可能简单，只保证可通可达。

通讯网络不追求计算网络的纳秒级延迟，也不追求存储网络的绝不丢数据，而是在任何条件下，尽力而为地传递数据，同时保持网络本身不崩溃。

#### 10.4.2 核心指标
理解通讯网络，需要记住这三个指标：

| 指标 | 含义 | 典型数值 |
|------|------|----------|
| **规模** | 能连接的节点数量 | 数十亿（IPv4的极限是43亿，IPv6几乎无限） |
| **可用性** | 网络持续运行的能力 | 设计目标：局部故障不影响整体，无单点故障 |
| **延迟** | 数据从A到B的时间 | 从局域网微秒级到跨洋百毫秒级，高度可变 |

通讯网络不把“极低延迟”或“绝不丢包”作为核心指标。它能接受延迟变化，能接受丢包。它最不能接受的是网络整体不可用。

#### 10.4.3 以太网和MAC
最早的以太网（10Base5，1980年代）是一个物理上的总线，一根同轴电缆，拖着几十台电脑。任何时候，只有一台电脑可以发送数据。如果两台同时发送，信号就会冲突，谁都收不到。解决这个问题的解决方案是CSMA/CD（载波监听多点接入/冲突检测）：
```
1. 先听后说：发送前先听一下线是否空闲
2. 边说边听：发送的同时也监听是否发生冲突
3. 冲突则停：如果检测到冲突，立即停止发送
4. 随机再发：等待一个随机时间后重试
```

这是一个纯分布式的解决方案。没有中心控制器，没有任何设备有特权。每台设备靠自己“听”和“说”来协调。CSMA/CD是通讯网络“边缘智能”哲学的早期体现。复杂性不在网络核心（电缆什么都不知道），而在边缘设备（每台电脑自己决定何时发送）。

随着设备增多，冲突概率上升，有效带宽急剧下降。效率在设备数量超过几十个时就变得不可接受。这就引入了交换机。 

交换机的出现彻底改变了以太网，共享线路进化为独享线路：
- 交换机学习每个端口（连接一条线路）上的MAC地址，构建转发表
- 当一个帧进来，交换机只把它发到目标端口，而不是所有端口
- 不同端口上的传输可以同时进行，互不干扰

结果很显然，冲突完全消失，现代以太网已完全淘汰CSMA/CD；每个端口获得独占带宽，带宽大大增加，全双工成为默认（可以同时发送和接收）。

现代以太网本质上是分布式交换结构。交换机是最底层的设施，它只做一件事，根据MAC地址转发帧。MAC地址是以太网设施的唯一标识，48位，厂商生产时分配，用于唯一标识转发目标。

以太网的核心特征是：

| 特征 | 说明 |
|------|------|
| **无连接** | 发送前不需要建立连接 |
| **尽力而为** | 不保证不丢包、不保证延迟 |
| **MAC地址** | 48位硬件地址，扁平空间，不反映位置 |
| **帧结构** | 有头有尾，带CRC校验 |
| **流控** | 802.3x PAUSE帧（可选） |

以太网的成功是因为其足够简单。它不做可靠性保证，不做拥塞控制，不做路由，不做任何复杂的事情。这些复杂性被推到了更高的层次（TCP/IP）或边缘设备。

以太网解决了同一个局域网内通讯的问题。但世界上有无数个局域网，家里的Wi-Fi、公司网络、运营商骨干网……，它们使用不同的技术（以太网、Wi-Fi、光纤、DSL、5G……）。让它们可以互联互达，我们需要IP网络。

#### 10.4.4 IP网络和地址
IP（互联网协议）是网际协议，通过在以太网上增加统一的网络层，做到了跨网互联。IP的核心设计有：

| 设计 | 含义 |
|------|------|
| **统一的地址** | IP地址（32位IPv4，128位IPv6）标识每个节点，不论底层是什么网络 |
| **无连接数据报** | 每个包独立转发，不需要建立连接 |
| **尽力而为** | 不保证可靠、不保证顺序、不保证不重复 |
| **分片与重组** | 适应不同网络的MTU差异 |
| **路由** | 每个路由器独立决定下一跳 |

IP是无连接的，这是一个关键的设计选择。IP没有像电话网络那样采用电路交换（先建连接再通话），而是采用无连接数据报。核心原因只有一个：网络规模。

如果每个路由器都要维护流经所有连接的端到端状态，系统会立刻崩溃。互联网有数十亿设备，活跃连接数无法计数。没有任何路由器能存储这么多状态。

无连接模型有如下好处：
- 路由器不关心这个包属于哪个流
- 每个包独立处理，独立转发
- 路由器之间不需要同步状态
- 局部故障不会扩散

代价是：
- 同一个连接的包可能走不同路径
- 可能乱序、可能丢失、可能重复

如果说以太网MAC地址是身份证的话，IP地址则是一个位置。它反映网络拓扑归属。IP地址有两部分组成，网络前缀和主机号。前者告诉你是在那个网路中，后者说的是网络中的那个主机。这是一个层次化的结构，非常适合聚合路由。

IP地址的层次化是互联网可扩展的关键。一个路由器不需要知道“所有”IP地址的位置，只需要知道“192.0.0.0/8”这个网段往哪个方向发。通过路由聚合，路由表的大小从“数十亿”压缩到“数十万”。

路由器仅仅查表来决定一个目标为具体某IP的包应该往哪里发。路由表一般通过两类协议来解决：

**域内路由（IGP：OSPF、IS-IS）**
- 在一个自治系统（AS，通常是一个运营商或大企业）内部
- 基于链路状态（每个路由器知道完整拓扑，计算最短路径）
- 收敛时间秒级

**域间路由（EGP：BGP）**：
- 在AS之间
- 基于路径向量（交换“经过哪些AS”的信息）
- 策略重于算法（运营商可以决定）

域间路由不能对网络拓扑有任何假设，这导致BGP的复杂性远超OSPF。BGP不是一个找最短路径的协议，而是一个“商业关系”的协议。运营商之间谁付谁钱、谁允许谁通过、哪些流量优先，这些策略都可以通过BGP属性（Local Preference、AS_PATH、MED等）表达。

BGP是互联网能连接数千个独立AS、同时保持各自自治的关键。但它也是互联网最脆弱的环节之一，BGP的配置错误可能导致全网路由泄漏。

IP网络的的选择是优先保证超大规模网络的可通，而把位置可靠性的职责交给更上层或者边缘设备。  

#### 10.4.5 TCP网络
IP网络把全球连在一起，如果你需要可靠、有序的字节流（大多数应用都需要），你需要TCP网络。TCP（传输控制协议）网络可以在不可靠的链路上，维持可靠的传输。TCP的核心机制有：

| 机制 | 功能 |
|------|------|
| **确认（ACK）** | 接收方确认收到的数据 |
| **重传（Retransmission）** | 发送方超时未收到确认则重发 |
| **序列号** | 每个字节有编号，用于排序和去重 |
| **窗口** | 控制发送速率，避免淹没接收方 |
| **拥塞控制** | 探测网络容量，避免拥塞崩溃 |

TCP的设计体现了计算机系统设计中最著名的原则之一端到端原则（End-to-End Argument）。这个原则的核心思想是，如果一个功能如果能在端节点完全实现，就不应该放在网络核心中。

对应到这里，那就是
- 如果你需要可靠性，由TCP端节点负责，网络核心不负责
- 如果你需要安全性，由TLS端节点负责，网络核心不负责
- 如果是应用语义，如HTTP、SMTP等，各自端点负责 ，网络核心不负责

端到端原则是互联网能够支持成千上万种应用、同时保持核心简单的根本原因。

TCP除了做可靠传输，还有一个更重要的任务是避免拥塞崩溃。拥塞崩溃是网络中最危险的故障模式，当路由器缓冲区满时，它会丢弃数据包。发送方超时后重传，但重传的包可能再次被丢弃，导致网络吞吐量几乎为零，因为所有带宽都被重传的重复数据占满。

TCP的拥塞控制通过慢启动和拥塞避免两个核心算法来防止这种情况：
- 慢启动。开始时发送一个很小的窗口，然后指数增长，直到遇到丢包。这本质上是测试对端的处理能力
- 拥塞避免。丢包后窗口减半，然后线性增长，对端处理能力不足时自动背压

TCP是在不导致拥塞的前提下尽可能快地发送。每一个TCP流都谦逊地主动限速自己，让所有流可以公平地共享带宽。

#### 10.4.6 从以太网到万维网
把以上各层组合起来，就是完整的TCP/IP协议栈，这是我们全球互联网的核心基础。

| 层级 | 协议 | 功能 | 哲学 |
|------|------|------|------|
| 应用层 | HTTP、SMTP、DNS… | 应用语义 | 边缘负责 |
| 传输层 | TCP、UDP | 端到端可靠或不可靠 | 边缘负责 |
| 网络层 | IP、ICMP | 跨网络路由 | 核心简单 |
| 链路层 | 以太网、Wi-Fi | 相邻节点通信 | 局部有序 |
| 物理层 | 光纤、双绞线、无线电 | 比特传输 | 物理约束 |

#### 10.4.7 通讯网络的设计原则
回顾所有技术，我们可以提炼出通讯网络的设计原则：

**核心简单，边缘复杂**
这是通讯网络的心法。网络内部（路由器、交换机）只做转发数据一件事包。所有复杂性（可靠性、安全性、应用语义）被推到边缘（端节点）。

**无状态优先**
IP路由器不维护每个流的状态，每个包独立转发。这允许任意数量节点加入网络，允许路由器任意替换，允许局部故障不扩散。

**尽力而为**
通讯网络不保证任何事情。这种最低承诺让网络可以在任何条件下运行，尽管可能很慢。

**分层解耦**
每一层只解决自己的问题，不依赖下层的特定特性。IP可以跑在任何链路层上（以太网、Wi-Fi、PPP、甚至鸽子）。TCP可以跑在任何网络层上（IPv4、IPv6、甚至用户态网络栈）。

> 1990年4月1日，IETF的工程师们发布了一篇恶搞性质的RFC（Request for Comments，互联网标准征求意见稿），编号RFC 1149，标题是《A Standard for the Transmission of IP Datagrams on Avian Carriers》，提出了以信鸽作为互联网的物理层。以纸条上写数据作为链路封装，每个IP包用防水墨水写在微型纸条上，塞进鸽子脚上的小管子里。MTU则取决于纸条大小和鸽子能飞多远。
>
> 2001年，一群挪威的Linux爱好者真的动手做了这个实验（名为 CPIP - Carrier Pigeon Internet Protocol）。他们让信鸽在约5公里的距离内传输了9个IP包，每个包带1字节的数据（内容是`ping`命令），往返延迟大约是1小时，丢包率约50%！

这种解耦让每一层可以独立演进，是互联网存活了几十年而长盛不衰的原因。

**生存性优先**
这个是通讯网络的第一优先级，可通。网络可以慢、可以丢包，但不能整体停摆。这是通过去中心化设计（无单点故障）、动态路由（故障自动绕行）、降级服务（拥塞时主动丢包）实现的。

#### 10.4.7 通讯网络的演化
IPv4地址只有32位，约43亿个地址。当年设计时以为够用了，但今天显然不够。NAT（网络地址转换）把私有地址映射到公网地址，是权宜之计，不是根本解决方案。

IPv6的地址是128位，足够给地球每粒沙子分配一个IP。但迁移缓慢，全球采用率仅40%左右。原因是NAT目前还足够好，升级需要成本，没有足够强的驱动力。

TCP是1980年代的设计，内核实现，演化缓慢。现代互联网的问题包括：
- 队头阻塞。一个包丢了，整个连接都被阻塞（因为要按序交付）
- 连接迁移困难。从WiFi切到5G，TCP连接需要重建
- 握手延迟。TCP三次握手 + TLS四次握手 = 多个RTT

QUIC（Quick UDP Internet Connections）是Google开发的、目前作为HTTP/3底层的新传输协议，它基于UDP，用户态实现，可快速迭代，内建内建TLS 1.3，0-RTT即可连接恢复，内置的连接ID独立于IP，可以支持网络切换，非常适合与移动网络时代。QUIC其实是把传输层的功能从内核移到用户态。这延续了边缘智能的传统。

移动通信网络（3G/4G/5G）是通讯网络的巨大分支。它的独特挑战是无线频谱有限、信号受干扰、终端位置移动，需要动态切换网络。移动通讯网络经历了这么几代 

| 代际   | 核心变化                                            |
| ---- | ----------------------------------------------- |
| 3G   | 引入分组交换，移动网络第一次数据化，语音仍是电路交换                      |
| 4G   | 彻底IP化，语音也走IP（VoLTE）                             |
| 5G   | 网络切片、边缘计算、大规模MIMO                               |
| 6G趋势 | 通信+感知融合（基站同时做雷达）、AI-native 网络（AI 直接参与控制面）、天地一体化 |

5G最大的创新不是更快，而是网络切片。一个物理网络切出多个逻辑网络，分别服务自动驾驶（低延迟）、IoT（海量连接）、视频（高带宽）。这是通讯网络灵活性的极致体现。

### 10.5 网络的设计谱系
当我们说网络时，可能指的是CPU核心之间纳米级的轻声低语，也可能是全球数十亿设备之间的数据汪洋。这中间相差九个数量级，却共享同一个名字。

事实上，计算网络、存储网络、通讯网络是三种完全不同的存在。它们服务于不同的目标，遵循不同的设计哲学，面对不同的失败模式。它们唯一的共同点是都在传输数据。除此之外，几乎一切都不同。

#### 10.5.1 使用RAP观察
在深入具体细节之前，先建立一个统一的观察框架。任何网络都要面对三个根本诉求：

| 维度 | 核心问题 | 含义 |
|------|----------|------|
| **可靠性（Reliability）** | 数据会错吗？ | 完整性、一致性、确定性 |
| **可用性（Availability）** | 能一直用吗？ | 容错、生存性、持续服务 |
| **性能（Performance）** | 有多快？ | 延迟、带宽、吞吐量 |

这三个诉求构成一个不可能三角。任何网络都无法同时在三个维度上达到最优。必须在三者中做出取舍。优先保证哪两个，牺牲哪一个。

三种网络，正是这个三角的三个边：
- **计算网络**在性能-可靠性边上，远离可用性。它追求极致速度和数据一致，代价是系统脆弱。所有参与者共享一个时间参考，操作在规定时间内完成。这个假设让它可以做缓存一致性（MESI）、可以做硬件化的低延迟通信。但同步意味着脆弱，一个节点不及时响应，整个系统就不确定
- **存储网络**在可靠性-可用性边上，远离性能。它追求数据安全和持续服务，代价是延迟较高。每个写入必须得到“已持久化”的确认才能返回。这个假设让它可以做信用流控、双Fabric、端到端校验。但确认意味着等待，延迟必然高于不确认的系统
- **通讯网络**在性能-可用性边上，远离可靠性。它追求速度和生存性。每个数据包独立处理，不依赖历史。这个假设让它可以在任意规模下运行，允许节点随时加入退出。但无状态意味着无法保证顺序和可靠性，必须由上层（TCP）补救。

关键特征对比如下：

| 维度       | PA型（通讯网）         | RP型（计算网）         | RA型（存储网）         |
| -------- | ---------------- | ---------------- | ---------------- |
| **优先保证** | 性能+可用性           | 可靠性+性能           | 可靠性+可用性          |
| **牺牲**   | 可靠性              | 可用性              | 性能               |
| **代表网络** | 互联网、以太网          | CPU互联、NVLink、CXL | 光纤通道、NVMe-oF     |
| **核心机制** | 无状态转发、统计复用、边缘可靠性 | 硬件一致性、固定拓扑、链路级可靠 | 信用流控、双Fabric、持久化 |
| **丢包**   | 允许（拥塞时主动丢）       | 不允许（链路级重传）       | 不允许（信用流控）        |
| **乱序**   | 允许               | 不允许              | 不允许              |
| **延迟**   | 可变（微秒到百毫秒）       | 固定且极低（纳秒到微秒）     | 固定但较高（微秒级）       |
| **部分故障** | 自动绕行             | 崩溃-停止            | 故障切换             |
| **拓扑**   | 动态、不可控           | 固定、预知            | 静态、可控            |
| **规模**   | 全球（数十亿设备）        | 小（单芯片/单机箱）       | 中等（数据中心内）        |
| **典型延迟** | 1μs - 200ms      | 10ns - 1μs       | 3μs - 50μs       |

三者对应不同的世界需求，更本质上对应到网络本身的结构。需求驱动了结构，并最终导致了对应的设计选择。   

#### 10.5.2 隔离边界
这三种网络之间存在清晰的边界。这个边界不是历史的偶然，而是应对复杂性导致的结构差异的自然结果。

| 边界            | 分隔          | 价值                             |
| ------------- | ----------- | ------------------------------ |
| 计算网络 vs 存储网络  | 易失/持久、纳秒/微秒 | 让计算不需要关心持久化，让存储不需要关心速度         |
| 计算/存储 vs 通讯网络 | 本地/全局、同步/异步 | 让高性能系统不需要处理不确定性，让广域系统不需要处理极低延迟 |
| 存储 vs 通讯      | 确认/尽力、无损/有损 | 让存储可以保证不丢数据，让通讯可以无限扩展          |

边界的存在是有理由的，它隔离了不同类型的复杂性，让每个世界可以独立演进，各自优化。持久内存的失败恰恰证明了这一点：试图用单一介质同时满足计算网络（快）和存储网络（准）的需求，会导致两种复杂性的灾难性混合，而无法提供比组合方案（DRAM+SSD）更好的用户体验。

这个隔离边界在AI时代遇到了挑战。AI训练网络需要同时具备计算网络的低延迟、存储网络的可靠性和通讯网络的可扩展性。世界变了么？

其实并没有。我们不过是在不同的网络上叠加高级抽象。正如互联网本身是AP类型的，但是叠加了TCP，我们就有可靠的基础。事实上，因为各层的冗余和可靠性设计，互联网作为一个整体相当可靠。

AI训练网络是RP型的。但是通过在应用层引入checkpoint，就可以应对不可用带来的问题。

#### 10.5.3 复杂性的分配
三种网络最根本的区别，是把复杂性放在哪里。而这个选择，则由其结构决定 。

| 网络 | 复杂性分配 | 具体表现 | 结构特征|
|------|-----------|----------|---------|
| 计算网络 | **向下沉** | 一致性写进硬件，RDMA写进NIC，交换机参与计算 | 微短距，拓扑固定，延迟可预测 |
| 存储网络 | **向内集中** | 信用流控、双Fabric、端到端校验、事务日志 | 中短距离，拓扑静态已知，延迟可预测 |
| 通讯网络 | **向外推** | 网络核心简单无状态，可靠性由边缘TCP负责 | 距离不限，拓扑动态未知，延迟不可预测 |

这是理解整个网络谱系的基础框架。没有哪一种分配是绝对正确的，它们在各自的约束下都是最优解。

在三种网络中，通讯网络是最特殊的。计算网络和存储网络有一个共性，它们在追求某种程度的确定。计算网络追求确定的一致性（MESI保证每个核心看到相同的数据）。存储网络追求确定的持久性（写入确认意味着数据已落盘）。它们都试图消除不确定性。

通讯网络的选择是它接受不确定性，然后上层协议TCP在边缘补救这些不确定性，但补救本身也是不确定的，你无法知道丢包后重传需要多久。

这种接受不确定性的选择，让通讯网络获得了其他两种网络无法企及的性质，无限的可扩展性和极致的可用性。

#### 10.5.4 结构谱系和设计工具箱
从计算网络到存储网络到通讯网络，可以看到一个清晰的演化序列：

| 维度 | 计算 → 存储 → 通讯 |
|------|-------------------|
| 规模 | 从几十到数亿 |
| 延迟 | 从纳秒到百毫秒 |
| 确定性 | 从确定到不确定 |
| 控制力 | 从完全可控到完全不可控 |
| 设计哲学 | 从硬件复杂到边缘复杂 |

这是一个连续的光谱。在每个维度的不同选择决定了不同的网络形态。每一种网络都在不可能三角中选择了自己的位置：计算网络选了快且准，存储网络选了准且稳，通讯网络选了快且通。这些选择是需求的投射，是结构的反映，是复杂性处理的权衡，更是网络特征的来源。

理解这一点，就能看明白很多现象：
- 为什么CPU内部不用以太网？因为以太网的设计目标（可扩展、容错）与CPU互联的需求（极低延迟、一致性）不匹配。
- 为什么核心存储不用Wi-Fi？因为Wi-Fi的尽力而为模型与存储的“绝不丢数据”要求冲突。
- 为什么互联网不保证可靠性？因为如果要保证，它就无法扩展到今天的规模。

以下是针对不同网络的设计工具。

**PA型网络（性能 + 可用性 → 牺牲可靠性）**
优先保证性能(P)和可用性(A)，可以容忍可靠性(R)的缺失。可选的设计策略有：
- 无状态转发
- 统计复用
- 边缘可靠性
- 动态路由

**RP型网络（可靠性 + 性能 → 牺牲可用性）**
优先保证可靠性(R)和性能(P)，可以容忍可用性(A)的缺失。可选的设计策略有：
- 硬件化一致性
- 固定拓扑
- 链路级可靠性
- 同步模型

**RA型网络（可靠性 + 可用性 → 牺牲性能）**
优先保证可靠性(R)和可用性(A)，可以容忍性能(P)的缺失。可选的设计策略有：
- 信用流控
- 冗余与高可用
- 持久化与崩溃恢复
- 端到端校验

RAP三角的选择是递归的。 一个系统可能在宏观层面选择一种策略，但在微观层面嵌入另一种策略。

如互联网 + TCP，互联网本身是PA型（无状态、尽力而为）。但TCP在边缘提供了可靠性，这相当于在PA的顶层“嵌入”了RP或RA的子策略。

再比如NVMe-oF + RoCE，NVMe-oF的目标是RA（存储可靠性），但它跑在RoCE上，而RoCE要求以太网提供无损服务。这意味着存储网络迫使底层通讯网络暂时放弃PA策略，向RP/RA方向靠拢，这就是为什么AI集群需要PFC、ECN等机制。

还有一个例子是之前提到的AI训练的弹性检查点。AI训练本身使用RP型计算网络（GPU互联追求极致性能）。但为了应对大规模集群中的硬件故障，框架层引入了检查点和恢复机制。这本质上是把RA策略“叠加”在RP网络之上。

这个递归性质意味着，设计一个大型系统时，我们不需要在整个栈中统一选择一种策略。完全可以在不同层次做不同的取舍，只要接口和约束被清晰定义。

#### 10.5.4 连接拓扑
网络拓扑是网络的连接结构。节点如何相连，决定了消息如何流动、故障如何传播、系统如何扩展。在分析计算网络、存储网络和通讯网络时，我们发现一个反复出现的模式，随着规模增长，拓扑结构遵循一条清晰的演化路径。

这条路径不是偶然的，它反映了物理约束、成本效益和复杂性管理之间的平衡。

| 规模层级        | 拓扑结构                    | 典型场景                                     | 规模上限                    |
| ----------- | ----------------------- | ---------------------------------------- | ----------------------- |
| 极小规模（2-8）   | 全互联（Full Mesh）          | CPU核心互联、早期NVLink（4-8 GPU）                | 受限于端口数和布线空间             |
| 小规模（8-64）   | 交换拓扑（Switch-based Star） | NVSwitch（单节点内多GPU）、FC SAN单Fabric、以太网单交换机 | 受限于交换机端口密度              |
| 中等规模（64-数千） | 交换网络拓扑（Clos/Fat-Tree）   | InfiniBand子网、数据中心CLOS架构、大规模FC Fabric     | 受限于网络直径和交换机层级深度         |
| 大规模（数千-数万）  | 联邦网络 / 自治系统（AS）         | 互联网AS间互联、多云互联、多数据中心互联                    | 理论上无上限，但路由表规模和管理复杂度持续增长 |
| 超大规模（数十亿）   | AS间联邦                   | 全球互联网                                    | 受限于BGP路由收敛时间和路由表爆炸      |

##### 10.5.4.1 全互联拓扑（Full Mesh）
这是极小规模下的最优解，每个节点与所有其他节点直接建立链路。当只有两个节点时，就是点对点连接。其特性有：
- 任意两节点间仅需单跳，端到端延迟最低
- 单链路故障不影响其他节点间的通信
- 链路数量为 N(N-1)/2。8个节点需要28条链路，16个节点需要120条，64个节点需要2016条，布线、端口、信号完整性问题迅速失控

全互联是有钱任性的拓扑。当节点数很少时，它是最优的；当节点数稍多时，它迅速变得不可维护。

##### 10.5.4.2 交换拓扑（Switch-based Star）
当全互联的链路数超出物理可行边界时，引入中心交换设备。其特性有：
- 所有节点连接到一台交换机（或一对冗余交换机），通过交换机转发通信
- 增加了一跳（节点→交换机→节点），但中心交换机的背板带宽可提供无阻塞转发
- 节点数受限于交换机的端口密度（如48口、128口）
- 交换机故障会导致整个拓扑瘫痪。这是可用性的核心风险，通常通过双交换机（冗余）或堆叠（Stacking）来缓解

交换拓扑是用集中换扩展。它把O(N²)的链路复杂度压缩到O(N)的端口复杂度，代价是引入了一个潜在的单点，也是可用性问题的源头。

##### 10.5.4.3 交换网络拓扑（Clos / Fat-Tree / Spine-Leaf）
当单台交换机的端口数不够时，将多台交换机组织成交换网络。其特征是：
- 分为两层（Spine-Leaf）或三层（核心-汇聚-接入）。Spine层提供高带宽无阻塞转发，Leaf层连接实际节点
- 任意两节点间存在多条等价路径，支持负载均衡和故障切换，也需要路由支持
- 跳数增加（通常为3-5跳），但每跳的延迟极低（亚微秒级），整体仍处于微秒级
- 可扩展到数千节点。以标准的Clos拓扑为例，如果Leaf交换机有48个下行端口和8个上行端口，Spine交换机有32个端口，总容量可达 8×32=256 台 Leaf交换机，支持约 256×48≈12,288 个节点（非阻塞条件下）
- 如果上行/下行带宽比达到1:1，称为无阻塞（Non-blocking）；若小于1，则称为过载（Oversubscribed）。数据中心通常设计为3:1到5:1的过载比，以平衡成本和性能

Clos拓扑是“用更多的交换机换更大的规模”。它是数据中心在过去十五年间扩展性提升的核心引擎，也是几乎所有大规模系统的基础网络形态。

##### 10.5.4.4 联邦网络 / 自治系统（Autonomous System, AS）
当交换网络的规模继续扩大，任何单一管理实体都无法控制全部节点时，拓扑必然走向联邦化。其特征是：
- 多个独立管理的子网（AS），每个AS内部自由选择拓扑和协议，对外通过标准边界协议（如BGP）互联
- 无单一控制平面。每个AS独立运行内部路由协议（OSPF、IS-IS、EIGRP），自主决定内部拓扑和策略
- AS的自主策略左右了流量方向和流量优先规则
- 规模边界扩展到十万量级。这是当前互联网的规模，每个AS内部可达数万节点

从AS开始，管理策略开始替代算法。追求全局最优已经不现实，让每个自治体自主决策，通过经济契约而非技术协议实现互联以及优化。这时候，网络不仅仅是一个技术系统，而是一个由技术和经济共同驱动的社会系统。

##### 10.5.4.5 AS间联邦（BGP驱动的全球互联）
当AS数量增长到数万个，联邦本身也需要组织化，这形成了多级AS间互联。这是去中心化的，高度社会化组织的协同网络。其特征有：
- AS之间通过BGP（边界网关协议）交换路由信息，但BGP的传播不是无约束的。互联网形成了类似洋葱的层级结构：
  - Tier 1 运营商。全球骨干，无上游（不向任何人购买转接服务），约10-15家
  - Tier 2 运营商。向Tier 1购买转接，同时与同级对等互联
  - Tier 3 / 本地ISP。向Tier 2购买转接，服务于终端用户
- 路由收敛很慢。BGP收敛时间在秒级到分钟级，远慢于内部路由协议的毫秒级。这是可扩展性换速度的折中后果
- 路由表规模不断加大。全球BGP路由表约100万条前缀（IPv4 + IPv6），且仍在增长。路由表爆炸是互联网长期面临的挑战
- 无全局视角。没有一个节点拥有完整的拓扑信息。每个路由器只知道“往哪个方向走”，不知道“具体经过哪些链路”

AS间联邦是用层级联邦换无限扩展。没有中心、没有全局视图、没有统一控制。这也正是互联网的设计初衷。一个能在核战争中存活的网络，必然会采用这种去中心化的结构。

这是全球互联网的形态， 假设未来出现星际互联网，也必然是类似的样子。

##### 10.5.4.6 拓扑演化逻辑
观察这条路径，我们可以提炼出几条规律：
1. 连接数从O(N²)到O(N)。 全互联→交换→交换网络，核心改进是连接数从节点数的平方降为线性
2. 控制从集中到分散。 交换网络还有单一逻辑控制平面（虽然物理是多台），AS联邦已经没有全局控制器
3. 目标从最优到足够好。 全互联追求绝对最优（单跳），互联网追求“有路可走就行”（BGP不保证最优路径）
4. 路由信息从完整到摘要。内部网络交换完整拓扑（链路状态），AS间只交换可达性摘要（路径向量）
5. 可用性从“无故障”到“容忍故障”再到“预期故障”。全互联假设链路可靠，Clos设计容忍单交换机故障，互联网认为故障是常态

值得注意的是，这条演化路径并不只是通讯网络的专利。计算网络（从CPU核心到Chiplet互联）和存储网络（从直连到FC Fabric）也沿着相似的轨迹演进。拓扑结构的演化规律，跨越了“计算、存储、通讯”的边界。它是物理世界对不同规模的连接结构的普适约束。

### 10.6 本章小结
计算机科学有一个永恒的问题是，复杂性由谁承担？

网络的本质是应对复杂性的结构。我们从比特传输、节点通信、跨网转发、可靠性保证到状态同步这些核心问题出发，看到不同网络通过分层与责任划分，构建了迥异的解决方案。

计算网络为性能而生（RP型），追求纳秒级延迟与严格一致性。它将复杂性下沉到硬件。固定拓扑、MESI/MOESI缓存一致性、NVLink的统一地址空间、NVSwitch的计算卸载、CXL的跨设备一致性扩展。其代价是脆弱性，部分故障可能导致整体停止，适合高度同步的局部世界。

存储网络为可靠而设（RA型），核心目标是写入确认即永久安全。它将复杂性向内集中。信用流控防止拥塞、端到端校验与T10 PI对抗静默错误、双Fabric与MPIO实现高可用、双控制器与WAL保证故障恢复。性能让位于确定性，适合需要绝对数据安全的场景。

通讯网络为可用而立（PA型），服务于全球数十亿异构设备。它将复杂性外推至边缘。IP的无连接数据报、以太网的尽力而为、TCP的端到端重传与拥塞控制、BGP的策略路由。核心保持简单，从而获得近乎无限的可扩展性和生存性。

概括地说，在分层设计中，要性能就要往下层压（越接近硬件越快），要可用性就要往上层推（越接近应用边缘越理解可用性要求），只有可靠性需需要在本层完成，那就是端到端设计的威力。  

RAP不可能三角为我们提供了统一的观察框架。三类网络分别占据三角的一条边，在规模、延迟、确定性与控制力上形成连续光谱。这种差异不是偶然，而是需求驱动的结构选择。

复杂性的分配方式构成了网络设计的最根本谱系。只要底层结构不变，核心设计哲学就不会发生根本改变。新技术往往是在已有的结构之上，在更高抽象层引入不同的机制。

计算、存储、通讯三类网络之间的边界依然清晰。这种隔离让每类网络能在各自的约束下达到最优。CXL、NVMe-oF、RoCE、SmartNIC、光互连等新技术，并未消除这些边界，而是让我们能够在保持核心结构不变的前提下，更灵活地在不同层级组合策略，从而更好地应对复杂系统挑战。

网络拓扑是另一个层面，它只和网络的节点数有关。我们看到，同样的拓扑演变模式在三种网络中都重复出现，这个物理法则对结构的限制。

网络上的语义，如一致性，是分布式研究的范畴，本章仅仅浅略涉及。后面章节中，我们会重点探讨，在不同规模的网络上我们如何达成期望的行为。
