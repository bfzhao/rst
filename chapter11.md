## 第十一章 分布式系统：规模结构下的语义表达

### 11.1 分布式系统是什么
人们谈论分布式系统时，脑海里往往会蹦出一堆高大上的名词：微服务、共识算法、分布式数据库、Paxos、Raft、CAP定理、多机容错等，然而这些并不是本质。

分布式系统真正要处理的问题是，当语义表达跨越越来越大的物理结构时，如何维持状态转换的正确性。这是一个关于规模下语义保持的游戏。

在小规模系统中，状态是共享的，时间是统一的，顺序是明确的，故障模型也是简单的“要么全活、要么全死”（Fail-Stop）。这种系统中，指令的书写顺序就是真实的执行顺序。二者之间没有任何的语义摩擦，完全符合人类的大脑直觉。

随着规模不断扩大，物理定律开始彰显其冰冷的约束。存储开始隔离，时间开始漂移，顺序变得模糊，故障成为常态。系统逐渐失去了那个全局唯一的统一结构。

于是，表达不再天然一致，转换不再天然有序，正确性也不再天然成立。整个分布式系统理论，本质上都是在问一个问题，当失去统一的物理结构之后，如何在逻辑上重新建立起可验证的语义秩序？

理解分布式系统最好的方式，是从最简单的单处理器开始，沿着物理规模不断扩大的路径，像拉望远镜一样，观察每一步发生了什么变化：
- 随着规模扩大，我们得到了什么，又失去了什么？
- 什么假设被打破了？
- 为了弥补这种丧失，我们被迫发明了什么新结构？

### 11.2 从单处理器开始
最简单的计算模型，也是计算机开始发明时候的直接模型，就是单处理器系统。一个CPU，一个内存空间，一条执行流。

当我们写下代码：
```
A();
B();
C();
```

这意味着必然是先执行A，再执行B，最后执行C。这里表达是线性的，转换是顺序的，语义天然明确。单处理器世界拥有几个近乎奢侈的完美特征：

| 特征 | 含义 | 体现的本质 |
|------|------|-----------|
| **时间统一** | 所有操作共享同一时间轴 | 使用同样的硬件时钟（Crystal Oscillator） |
| **单一内存** | 所有状态集中 | 状态实时同步，写了就可以读到 |
| **全局有序** | 所有操作符合程序的顺序 | 程序序就是执行序  |
| **无真并发** | 无数据竞争 | 任意时刻只有一个主体在更新状态 |

因为这些特征的存在，无数恐怖的复杂性在源头上根本就不存在。例如`counter++`这样的一行代码，在单处理器中绝对不会发生并发冲突，因为没有第二个执行主体会同时去修改它。

> 这里不考虑非处理器因素导致的中断，如硬件中断，操作系统内核抢占等。

Redis 内存数据库就采用这个模型。Redis核心采用单线程、事件循环、顺序处理请求。它没有锁，没有竞争，可以在现代处理器上取得非常好的性能。

对于单处理器来说，时间是统一的，存储是统一的，顺序也是统一的。 

### 11.3 带缓存的单处理
因为CPU处理速度和内存访问速度的巨大差异（这个差异还在不断加大），几乎从一开始，缓存就是CPU设计的关键部分。

现代CPU有指令缓存，有数据缓存，还有融合的一致缓存，缓存也分好几级。然而缓存对用户是透明的。单处理器的所有优势，带缓存的单处理完全具备，没有任何的区别。

对于带缓存单处理器来说，时间是统一的，存储是统一的，顺序也是统一的。 

单处理器系统的缺点是单个核心的硬件处理上限。简单的办法是引入多个处理器核心，这就是我们现在看到的绝大多数CPU芯片的结构，叫做共享存储多处理器。

### 11.4 共享存储多处理器
在一块主板上放多个处理器核心，让他们通过总线共享同一个内存。这就是共享存储多处理器（SMP）架构。

我们当然可以让每个处理器运行完全无关的程序。我们既得到了单处理器架构的简单性，也得到了多处理器带来的处理能力的提升。然而，当两个程序想要访问相同的数据（他们有数据依赖关系），真正的复杂性就会迸发出来。

第一个问题是缓存透明被打破了。

#### 11.4.1 缓存一致性
我们上面提到，为了不让CPU闲着，每个核心都有了自己的私有缓存（L1/L2 Cache），问题由此而生。考虑两个核心同时缓存了同一个变量`X = 5`：
1. 核心A率先发起修改，将本地缓存的`X`改为`10`。
2. 此时如果没有外部干预，核心B依然认为`X = 5`。

如果A的缓存没有刷新到内存，B不可能知道A已经更新了X；即便A的更新刷新到了内存，B也不知道自己的缓存已经失效，这就是缓存一致性问题。

因为缓存设计的基础假设是对用户透明，所有我们需要用一种透明的方式处理这个问题。这就是MESI协议的基础。MESI协议用来协调缓存之间的同步问题。核心思想叫做“写入时驱逐通知”。亦即，当一个核心要修改一个缓存值时，通过片内网络向所有持有该副本的核心通知它们手里的副本已经失效。

| 状态                | 含义              | 体现的性质      |
| ----------------- | --------------- | ---------- |
| **M** (Modified)  | 本核心已修改，与其他副本不一致 | 核心独占了最新真相  |
| **E** (Exclusive) | 本核心独占该数据，且与内存一致 | 独享但尚未修改    |
| **S** (Shared)    | 多个核心都有干净的副本     | 共享状态       |
| **I** (Invalid)   | 本核心的副本已失效       | 数据过时，需重新加载 |

> 当核心数较少时（约4-16核），硬件采用物理广播（Snooping）方式通知。随着核心数扩展，为避免总线风暴，现代多核CPU（如Intel Mesh、AMD Infinity Fabric）已转向基于目录（Directory-based）的一致性协议。一致性请求仅发送给持有该缓存行的特定核心，而非全网广播。但无论是广播还是目录，其核心思想都是写入方负责通知所有可能持有副本的节点失效。
> 
> 多核缓存一致性协议可以看作是最早的分布式复制系统。只不过它运行在微米级的硅片内部，有着极高可靠的硬件互联作为支撑。

第二个复杂性来在数据竞争，这才打开了通往复杂性的大门 。

#### 11.4.2 数据竞争
对一个数据的基础操作只有读和写。两个执行体操作一个数据，有读读，读写，写读，写写四种组合。处理读读没有数据竞争，其他三个都有竞争关系：
1. 读写竞争。读了一半被覆写，读出来的不对
2. 写读竞争。写不完整被读出，读出来的还是不对
3. 写写竞争。一个写插入到另一个写中间，结果全乱了

出现数据竞争关系，是因为我们遇到了真正的并发（True Concurrency），而数据并不了解这一点。因为存储是共享的，所有的执行结果（含执行的中间结果）都对所有的执行体可见。数据竞争的原因是，我们希望这个中间过程是隐藏的，但是多处理器系统不能保证这一点。

想要把这个中间过程真正藏起来，我们需要新的东西。

#### 11.4.3 原子操作和锁 
把执行的中间状态藏起来，可以从执行主体角度想办法，也可以从代码想办法。从执行主体考虑，我们就得到了**原子操作**。

物理学中原子是不可分割的，原子操作就是不可分隔的操作。你不能在一个原子操作执行一半的时候就访问其操作的数据。这确保了每个操作看到其他操作的结果必然不是中间状态。

硬件层面增加的诸如`Compare-and-Swap`（CAS）、`Fetch-and-Add`等原子指令。它们利用硬件总线锁或缓存行锁，把多个状态转换压缩为一个不可分割的（Atomic）语义步骤。

以下代码表示对count加一：
```c
counter++; // 看起来是一步，其实包含了：读取、修改、写回三步
```

使用CAS指令，CAS会首先比较要修改的值是不是期望的（之前读取的值），是的话才完成修改（+1）和写回的操作。

从代码角度考虑，我们就得到了**锁**。

锁是一种用于保护数据的附加机制。当一个数据被锁保护时，除非持有锁，你就不可以访问它。这强化了对数据的线性访问机制，自然抑制了中间结果的暴露。

然而锁机制依赖于访问方使用锁的共识。如果一个访问方完全不考虑数据锁而直接操作数据（它可以直接访问数据，如数据地址，变量名等），问题依然存在。一般通过工程方法来规避：把使用该数据的入口限制起来，任何人访问只能有限的固定入口，自然可以强化锁的有效性。

从这个角度看，原子操作可以看作是硬件约束的强制锁，人人必需遵守；而锁可以看作是应用之间的协作原子操作，大家都遵守锁规范才有意义。 

锁可以进一步细化为**共享锁**和**互斥锁**。读读操作显然是共享的，其他的读写组合都互斥。

当需要处理的数据操作很简单简单，如计数器加一之类，原子操作比锁更高效。于是派生出来一类叫做无锁的数据结构。完全利用原子操作来完成需要锁才可以并发访问的高级数据结构，在性能攸关的领域较常见。

原子操作是针对单个操作，如果想让多个操作表示出原子性，我们需要事务。 

#### 11.4.4 事务
现实业务往往要跨越多个状态，事务可以让多个操作展示出原子性。比如最经典的银行转账：

- 账户A减去100元
- 账户B加上100元

如果执行到一半系统崩溃，钱就会凭空消失。为了解决多状态转换的组合问题，我们在软件层引入了**事务（Transaction）** 的概念。事务本质特征还是原子性，但是是在语义层面的不可分割，要么全做，要么全不做。可以看出来，还是在努力把中间状态藏起来不对外暴露。

> 传统的说法中，事务的特性为概括为ACID
> - A（原子性）。操作要么全做，要么全不作
> - C（一致性）。操作的外部语义要保持一致
> - I（隔离性）。两个事务要彼此隔离
> - D（持久性）。事务的结果不能丢
>
> 从现在的观点看，原子性才是根本特征。原子性自然保证了隔离（不向对方暴露自己的中间状态），一致性是应用层的概念，需要上层保证，可以算是无关；而持久性则是其他层次的概念，本质并不影响事务的有效性。   

毫不奇怪，锁是实现事务的基础手段，它们的解决的问题根本上是一致的。

#### 11.4.5 可见性和先行关系
从这里开始，我们需要引入一个重要的区分：一个操作实际发生的真实时间，与这个操作对外产生可见效果的时间。两者并不总是相同的。因为诸如不可见缓存之类的原因（我们后面还会讨论很多其他的原因），都可能让指令的实际执行顺序与我们直观想象的顺序不一致。此时，如果还用“哪个操作先发生”这样的物理时间概念来理解程序行为，事实上完全不可能。

简单思考就能明白，我们真正关心的，从来都不是操作“是在什么时候被执行的”，而是“它的外部效果什么时候被其他线程或观察者看到”。一个操作对外界产生的效果（例如对共享变量的写、对其他线程可见的内存更新等），就被称为该操作的**可见性（Visibility）**。

可见性强调的是“外部表现”的时机，而不是其发起，执行等时机。从指令在硬件上被真正执行，到写操作在 CPU 的缓存中完成，再到缓存同步、内存屏障生效、或特定同步原语被执行，其可观察效果可能才真正体现出来。

从此以后，我们提到操作的时间或顺序，除非明确说明，指的是产生可见效果的时间或顺序。   

基于这个可见时间，我们才引入**先行关系（Happens-Before）** 来形式化地描述两个操作之间的顺序约束。如果操作 A 对操作 B 存在先行关系，A 的外部效果对必须对 B 及其之后（先行关系有传递性）的操作可见。

如果两个操作互不先行，它们就是并行的。所以先行关系是偏序（偏的意思是部分，亦即并不是任意两个操作都有先行关系）的。  

#### 11.4.6 指令顺序的破缺
多个执行主体同时执行时，原生的天然程序序不存在了。对于没有数据依赖的代码，他们的顺序完全是并行。对于有数据依赖的代码，如果正确使用了原子操作，锁，甚至事务，我们得到的是一个交错的执行序。尽管代码可能被多个执行体执行，但是如果把所有的执行代码按时间顺序排开，你会发现，每个程序的代码还是和执行顺序严格一致的。

在现代CPU下，保持这种一致甚至都很难，因为CPU为了性能，早就打破了程序序。CPU的判断很简单，如果两个操作在程序序中没有关系，那就可以任意调整：
```java
// 线程1执行
x = 1;
flag = true;
```

这两个赋值显然无关。但是假如另一个线程的代码是：
```java
// 线程2执行
if (flag) {
    print(x); 
}
```
在某些平台上，线程2可能打印出`0`。

出现这种问题的原因可能是：
- CPU的指令乱序执行（Out-of-Order Execution）。CPU直接先执行了flag的赋值
- CPU的Store Buffer（写缓冲区）。CPU按照程序序执行了，但是首先被刷入Store Buffer而不是L1缓存
- 编译器重排。为了更好的寄存器分配或流水线利用，它交换了赋值顺序

你可能会指出，我们不应该假设 `x` 和 `flag` 无关，CPU和编译器应该分析他们是否依赖，然后决定。但是CPU和编译器做不到对所有的数据依赖做全局分析，谁也做不到。我们唯一的途径是，如何告诉CPU和编译器，那些优化在什么情况下需要更保守。

这是内存模型要解决的问题。  

#### 11.4.7 内存模型
执行序和程序序的不同，是内存模型的缘起。内存模型定义了规则，明确说明，那些不能重排，这样CPU或者编译器就可以安全地做任意激进的优化。

##### 11.4.7.1 平台的内存模型
平台指的是指令的执行平台，如CPU。对CPU的用户来说，可见的有寄存器，内存。透明的有CPU内的Store/Load Buffer以及CPU和内存间的多级缓存。

如果有两个读（Load）写（Store）操作，组合起来有四种可能。平台的内存模型严格限定了可以被重排的操作。  

**顺序一致性**（Sequential Consistency）模型是最强的内存模型，要求执行序严格和程序序保持一致，不允许做任何指令重排。这禁止了几乎所有的硬件优化可能，可理解性最好，性能最差。

几乎没有平台使用这个模型，它太严格了！

**弱序**（Weak Ordering）模型是最不严格的模型，CPU可以做任意优化，高效而省电，非常适合移动计算领域，现代ARM平台都使用这个模型。然后所有的复杂性交给上层软件处理。

**全写入序**（Total Store Order）模型是一个平衡的模型，允许读越过更早的写（Store-Load重排），但不允许写越过写。这是常见的下x86/64平台使用的模型。

以下表格总结了三种模型对重排的要求。

| 重排类型        | 顺序一致性  | TSO    | 弱序  | 典型影响                    |
| ----------- | ------ | ------ | --- | ----------------------- |
| Store-Load  | **禁止** | **允许** | 允许  | 读越过更早的写。TSO下可能读到旧值      |
| Store-Store | **禁止** | **禁止** | 允许  | 写-写顺序。TSO保证初始化-标志模式安全   |
| Load-Load   | **禁止** | **禁止** | 允许  | 读-读顺序。弱序下可能看到指针更新但内容未更新 |
| Load-Store  | **禁止** | **禁止** | 允许  | 读-写顺序。弱序下条件判断可能绕过数据准备   |
> Store-Load重排是CPU硬件层存在Store Buffer的自然后果，对性能的提升极大，这是TSO模型允许这个重排的原因。

应用层如果需要控制这些特性，就需要使用平台提供的指令，如专门的内存屏蔽指令（Memory Barrier），用于设立同步点。以Intel平台为例有：
- MFENCE。全屏蔽内存。最强的顺序保证。前后的读写都不可以越过该指令。后面的指令不能重排到MFENCE之前，前面的指令不能重拍到MFENCE之后   
- LFENCE。Load屏蔽。前后的读操作都不可以越过该指令
- SFENCE。Store屏蔽。前后的写操作都不可以越过该指令

这些指令是应用层控制CPU指令排序，设置强制同步点的基础设施。可以认为，FENCE之前的操作必然先于FENCE之后的指令。

现代编程语言不是直接执行在CPU上的，而是需要把编程指令转化为机器指令。这一层抽象，引入了语言的内存模型。   

##### 11.4.7.2 语言的内存模型
现代编程语言在硬件内存模型之上，提供了更高级的语言级内存模型。这允许程序员在不同场景下进行精细的权衡。

编程语言和平台的内存模型的差异，主要有这么几个方面：
1. 编程语言没有暴露寄存器这个概念给用户。这意味编译器可以利用寄存器做激进的优化。和CPU利用各级缓存做优化类似，隐藏的部分导致可见性的差异，内存模型就需要对此做出限定
2. 另外语言是运行在平台上的，对于常见的TSO和WO两种模型的CPU，它都需要支持，所以还需要统一二者的差异，提供一致的，高级的语义级别的内存模型
3. 语言层面区分普通变量和原子变量，只有后者受内存模型的控制

C++以其灵活性著称。它定义了六种内存模型，可以根据需要灵活选择，以下简单说明最常用的五种。

**宽松模型（relaxed）**
`relaxed`模型只要求操作的原子性，确保原子变量读写更新不暴露中间状态即可。允许任意的重排，这个直接对应到CPU的弱序模型。

**获取模型（acquire）**
`acquire`模型使用Load操作作为屏障，禁止Load之后的操作重排到Load之前（不禁止前面的操作后移 ）。TSO模型的CPU内置支持，WO模型的CPU需要指令控制。

获取模型类似于禁止Load-Load 和 Load-Store，但是是语义层面的。

**释放模型（release）**
`release` 模型使用Store操作作为屏障，禁止Store之前的操作重排到Store之后（不禁止后面的操作前移）。TSO模型的CPU内置支持，WO模型的CPU需要指令控制。

释放模型类似于禁止语义层面的Load-Store 和 Store-Store。 

**获取释放模型（acq_rel）**
综合了`acquire`模型和`release`模型的效果。TSO模型的CPU内置支持，WO模型的CPU需要指令控制。这个模型和x86的TSO模型表达的语义类似，但是TSO支持所有的内存操作。

**顺序一致模型（seq_cst）**
顺序一致模型不仅仅要求`acquire`+`release`的所有保证，还禁止了Store-Load重排。这里它和CPU的顺序一致模型表达的语义类似。

> 语言内存模型与平台内存模型看似相似，实则处于完全不同的抽象层次，服务于不同的对象。
>
> 平台内存模型定义的是硬件的行为规范，它规定了CPU在指令重排和缓存可见性方面能做哪些优化、不能做哪些优化。它是硬件对编译器和开发者做出的承诺。
> 
> 语言内存模型则是在平台模型之上建立的一套编程契约，面向的是程序员和编译器。它需要做两件事：一是屏蔽不同平台内存模型的差异，提供一致的并发语义；二是将语言自身的复杂性（如寄存器分配、编译器重排、逃逸分析等）纳入可见性的考量范围，让程并发代有一个确定的、可预期的行为基准。

总结下， 对于共享存储多处理器来说，时间依然是统一的，存储统一的，但是顺序开始混乱。我们引入了内存模型，应对顺序混乱的问题。

### 11.5 NUMA
当SMP架构的核心数继续膨胀（比如达到64或128核），所有核心去抢同一根内存总线就会演变成一场交通灾难，总线竞争加剧，延迟呈指数级上升。于是，NUMA（非均匀内存访问）架构应运而生。

在NUMA结构中，每组核心都有了自己的本地内存，访问它速度极快；如果要跨越互联链路（如AMD Infinity Fabric或Intel UPI）去访问别的节点的远端内存，延迟就会大幅增加。

操作系统把所有 Node 的内存合并成单一地址空间，不区分是近内存还是远内存。具体分配时，一般使用First-Touch分配策略，把内存分配到离执行核最近的近端内存中。如果程序被调度到了其他核，这些内存就都变成了远端访问。内核会逐步地把这些页面迁移到近端，最终目标是让线程尽量访问本地内存。

NUMA带来共享结构的一个核心转折。在逻辑上虽然看到的依然是一个统一的、大一统的内存空间；但在物理现实上不均匀的味道已经蔓延开来。这不过反映了一个铁律，局部性（Locality）才是物理世界的底层真理。

从NUMA始，调度器就必须感知数据的位置，确保执行体尽量靠近它要处理的数据。这种数据保持不动，计算按需调度的设计方法，在分布式大数据系统（如Hadoop、Spark、数据分片）中被广为使用。

对于NUMA系统来说，时间依然是统一的，顺序是混乱的，存储还是共享的，但是开始体现出差异。面对存储差异，我们开始衡量使用成本，局部性成为核心考虑，本地访问成为我们开始追求的目标。

当存储完全不共享，我们就进入了多进程的世界。

### 11.6 多进程和消息传递
操作系统从资源隔离和独立应用的角度，引入了进程这个概念。每个进程都被赐予了一个独立的虚拟地址空间，这个地址空间如同一个平行宇宙，与其他进程老死不相往来。一个进程的指针，永远不可能直接解引用到另一个进程的内存区域里。

这种强隔离带来了极高的安全性和稳定性。一个进程崩溃、内存越界、或者恶意操作，通常不会直接波及到其他进程，极大降低了系统整体崩溃的风险。同时，它也意味着隐式的直接共享内存默认不复存在。

想让不同进程之间通信吗？那就必须通过显式的进程间通信（IPC）机制来完成，比如管道（Pipe）、消息队列（Message Queue）、共享内存（作为一种特殊IPC）、信号（Signal），或者网络 Socket 等。此时，计算机的通信模式完成了一次史诗级的跨越，从共享内存转向消息传递（Message Passing）。

消息传递模型彻底消除了共享内存最令人头疼的数据竞争问题。所有的共享访问、锁、内存屏障等复杂操作，在消息传递的时刻被简化为序列化的发送和接收。消息的发出和接收天然形成了先行关系，这大大简化了并发程序的因果分析和调试难度。

> 实际使用中，本机（同一台机器）上的消息传递往往仍然底层基于共享内存实现。操作系统会为进程间通信提供高效的共享缓冲区，从而获得最好的性能和低延迟。只有当通信跨越网络时，才是真正意义上的、纯粹的消息传递。

多进程情况下，时间还是统一的，顺序早就不再存在，远端存储开始出现不可直接访问的特性。硬件保证的共享存储已经消失，我们引入了消息传递这种模式来沟通信息。消息传递的好处是强化的先行关系，但是消息本身的顺序成为新的问题来源。

多进程情况下，错误模式开始发生改变。之前全系统的简单Fail-Stop不适用了，一个进程的崩溃死亡不必然导致整个系统的崩溃。我们需要探测出这种失败尽可能提供容错的能力：
- 依赖外部控制。配置探测进程活动性的任务，不断检查进程的可用性（健康检查），发现失败则重启进程。主动的有看门狗机制，被动的则提供健康访问接口
- 依赖应用冗余。进程本身开发了高可用性冗余特征，如双活互检等。

即使在多进程模型下，系统依然保留了最后一个共享资源，统一时间。因为所有进程运行在同一台物理机器上，它们共享同一个操作系统内核、同一个时钟源和同一个进程调度器。我们可以精确判断分属两个进程的不同的操作的先后顺序，只要给它们一个足够精度的时间戳就行。我们仍然有对事件进行排序的绝对标准。

这种带有上帝视角的统一时间在某些情况下会有用，如不依赖因果来强制指定顺序。在走向真正的多机分布式世界之后，这个上帝视角也会被彻底打破。我们进入了真正的混沌之中。

### 11.7 多机分布式系统
在多机分布式系统中，共享的存储早就不存在，节点散落在不同的物理地点。它们之间唯一的纽带，是那条不确定的，充满延迟与丢包，甚至可能中断的网络。

欢迎来到真正的分布式系统，这是狂风与混沌的荒原。这里我们习以为常的假设不再存在：
- 时间完全失控。统一的时间不再存在，时间可能是漂移的，错误的，甚至是颠倒错乱的。唯一能依赖的顺序关系是先行关系
- 消息传递不再是可靠的
- 失败从概率上成为常态。更糟糕的是，因为消息传递的不可考，甚至无法识别失败

#### 11.7.1 部分失败
单机崩溃很简单，代码触发异常，程序退出，整个世界归于寂静，我们能做的就是从头开始。这是失败即终止（Fail-Stop）的含义。为了维持状态，我们会把重要的信息持久化，定时快照等，确保下一次开始的时候我们可以直接从最近的可靠状态恢复。

在多机系统里，更常见的情况是，机器A挂了，其他所有的机器一无所知。如果机器B依赖着A，B可能发现A不响应消息，重试多次也不行。B只能认为A出了问题。至于是A挂了，还是网络断了，对B来说并无区别。如果A对B至关重要，没有A的存在，B对外也会表现出异常，响应慢，甚至完全失败；但是如果A仅仅是可选的，B可能有替代的方案，这样B就可以基本不受A的影响。

这就是部分失败。对于多机分布式系统来说，部分失败在概率上是必然的。我们必需恰当地处理失败的情况，确保失败是可控的。

更糟糕的是，有些失败不是简单停止工作。比如网络中断了，多机被隔离为不同的部分。每部分都认为自己还在工作，其他的已经失败，这会导致精神分裂般的外部行为，我们谓之脑裂（Split Brain）。处理脑裂问题是多机分布式系统的最大挑战之一。

更多的情况是，没有失败，就是不可预测：
- 发送了消息，对方没收到
- 发送了消息，对方收到多个相同的消息   
- 发送了多个消息，但是对方接受到的顺序是错的
- 甚至发送了消息，但是半路被恶意三方篡改

这导出一个直观的结论，在分布式系统中，无法区分慢与死。正规地说，在完全异步的网络模型下，达成确定性共识是不可能的。 

> 分布式理论中，同步的含义是消息传递的延迟有已知上界；而异步是无上限的。这个和编程中一般说的同步异步不大一样，后者更强调的是调用是否阻塞当前执行流。多核架构的片内网络消息延迟有上限，这是同步网络；我们日常使用的网络绝大部分都是异步的。

在部分失败的系统中维持逻辑性，我们需要仔细分析时间以及暗含的因果。

#### 11.7.2 时间，因果和逻辑时钟
时间的本质特征是有序性。仅仅统一时间就可以决定所有操作的顺序，只需要对所有的操作时间做时间排序即可。这里，时间先后就是先行关系，自然包含了因果关系。注意，这个有序是全序（full ordered）的。这对应这个一种叫做线性一致性的关系，所有的操作都可以无异议的排列为唯一的序列。

从之前的讨论中，我们已经知道，绝大多视情况下，我们不需要这么强的排序要求。事实上，世界上绝大多数的事件发生的都是并行的，也是完全无关的，我们并不关心其顺序（你如你和千里之外的一位完全不认识的陌生人谁先吃了午餐）。你甚至会奇怪，谁会问这样的荒唐问题。

相反，我们最关心的是一个事件是否可能影响另一个事件（信息的因果依赖）。例如：
- 用户 Alice 的“发帖”事件，是否应该在 Bob 看到“评论”之前被 Bob 看到？
- 转账操作是否应该在余额查询之前生效？

这就是语义上的因果关系。我们希望系统能尊重这种因果，不能出现违反直觉的结果。而表达这种关系的，就是之前提到的先行关系。先行关系是潜在因果关系的操作定义。它把我们关心的“因果”变成了可以通过消息和本地顺序追踪的东西。

但是先行意味着，一个先于另一个发生。我们没有了统一时间，怎么描述这个“先于”？这就是Leslie Lamport在1978年的经典论文《Time, Clocks, and the Ordering of Events in a Distributed System》中给出的答案，用逻辑时钟（Logical Clock）替代物理时钟。

其核心思想极其简洁。每个节点维护一个计数器。每次发生本地事件，计数器加1。每次发送消息，附上当前计数器值。每次接收消息，将本地计数器更新为`max(本地计数器, 消息中的计数器) + 1`。

> 分布式系统中，逻辑时钟有明确的定义，那就是Lamport在论文中给出的定义。这其实是一个特化的逻辑时钟，用于跟踪因果关系的逻辑时钟。
> 一般语境下，逻辑时钟可以看作是一个单调递增的全序序列，这是对物理时钟的直接模拟。
> 后面，如果指代Lamport逻辑时钟，我们使用因果逻辑时钟。        

简单因果逻辑时钟混淆了所有外部消息的时间。只能初略描述因果。如果要维护独立每个节点的逻辑时间，我们就得到了向量时钟（Vector Clock）。这是目前最重要、最常用的因果逻辑时钟。

向量时钟可以维持全部的因果关系。但是我们知道，因果关系是偏序的，并不是所有的事件都构成因果关系，从定义上讲，它们是并行的。这意味着，A并行与B，从观察者的角度，A可能在B前，也可能在B后。

如果A和B是可交换的，这个先后顺序没啥关系，如A的操作是+1，B的操作是+2。但是如果不满交换关系，如B的操作是\*2。他们的顺序就至关重要了。注意，这里强调的不是A或者B那个在前面更重要，而是，所有的观察者看到一致的顺序很重要，要么是AB，要么是BA，但是不能是全部。

达成这个过程的叫做共识（consensus），是要从满足因果性偏序的的众多所有合理序列中选出唯一一个作为系统的整体结论。

#### 11.7.3 全局共识
前面已经提到，在完全异步的网络模型下无法达成确定性共识，这是FLP不可能性定理（FLP Impossibility Result）。 

然而FLP并没有判分布式系统死刑，它说前提是完全异步模型。如果网络在大部分时间内是可达的，这意味着消息传递是有上限的（如设定一个上限），这是部分同步模型（Partial Synchrony）。

在部分同步的网络模型中，消息在时间上限内没有传达，只能认为这个节点已经“死亡”。这种情况下，什么才叫共识呢？那就是多数派（Quorum），只要多数达成一致就行，少数服从多数。

分布式系统中达成共识的算法叫做共识算法。共识算法必须满足三大条件：
- 一致性（Agreement）。 所有非故障节点最终必须同意同一个序列
- 正确性（Validity）。 最终同意的那个序列 ，必须是某个节点真实提议出来的，亦即满足因果关系
- 活性（Liveness）。 系统不能无休止地卡死，最终必须能做出决定

工业界的共识协议就是基于这样的设定而工作的。采用了部分同步模型，并且从多数派构造确定性顺序。极端的情况下，这些算法可能永远无法得出结论，但是只要网络能恢复，我们就能取得一致，这已经被证明。

现代共识协议几乎都是Paxos的变体。Paxos与1989年提出，直到2001年才广为人知。

 初次接触Paxos共识协议，很容易被其中的细节搞迷糊，特别是难以清晰理解为啥这样做就可以达到目标。Paxos协议其实同时做了三件事情，确定leader，完成多数派决议以及决议的收敛。这也是为什么谈起来Paxos，几乎就是晦涩难懂的代名词。我们把算法细节放在文末，这里仅仅做一个简单的去神秘化描述。
 - 首先，需要始终记住，对于AB和BA两种都合法的顺序，我们需要选取其中之一，具体是那个并不重要
 - 自然地，假设可以手工指定一个仲裁者，它看到的顺序就是“选中”的哪一个，这是最简单的，其他所有的节点都从这个节点继承即可
 - 不幸的是，手工选定的这个可能失败。失败之后，除非重新指定一个，否则整个系统都会挂掉。更好的办法是，设定一个小范围（如五个节点），想办法自动选择一个出来，哪怕一个挂了，也不影响全局。问题转化为，如何从几个已知的节点中选一个出来
 - 再进一步，这等价于如何就一个值达成一致意见。这里注意，一致是目标，而不是值是什么，或者值由谁提出。我们进入Paxos的范围
 - Paxos有三个角色，分别来竞争leader，完成决议和收敛决议
 - 两个多数派必然有交集，处于交集中的节点自然成为决议的核心。Paxos要求使用全局唯一的提议号（就是一个简单逻辑时间）并且只能接受大于承诺提议号的提议。这个保证了不管有多个提议拿到多数派，它们的值必然相同

> 你可能会注意到，我们使用Paxos目的是选一个leader作为顺序的裁决。但是Paxos算法本身一开始就需要自己选一个leader来驱动整个算法的完成。只是Paxos的内部选定的leader是动态的，可以被抢占。这个和我们需要的稳定leader有很大不同。   

这个选举出来的确定值，是多因果链收敛的核心。典型的情况，一个leader作为多因果的自然仲裁者，它观察到的顺序就是全局顺序。它把这个顺序记录下来，我们得到日志；把日志传播给其他所有节点，叫做日志复制。

Raft把这三件工作清晰地拆分开，可理解性大大增加，成为事实上的工业标准实现。

#### 11.7.4 日志和日志复制
明白了共识的本质是确定唯一的构造顺序，就能彻底看懂为什么现代分布式组件的核心全都是一条一维的日志（Log）。

无论是数据库的WAL（Write Ahead Logging）、文件系统的Journal，还是Raft的Log、Kafka的分区日志（Partitioned Log），其底层都是将复杂的、无序的空间状态变化，降维投影为一条严格一维的时间序列。

```
[日志流]：[操作1：A=1] --> [操作2：B=2] --> [操作3：A=A+B] (唯一的全局因果链)
```

不用关心最终的状态，主要能重放日志，就可以直接得到一致的状态。日志这个简单的结构为分布式系统注入了四大逆天改命的能力：

| 能力       | 实现原理                                  |
| -------- | ------------------------------------- |
| **崩溃恢复** | 只要日志在，就可以通过重放历史（Replay）重构出任何时间点的完美状态  |
| **复制同步** | 节点间不再同步复杂的内存快照，而是源源不断地像传送带一样复制一维日志    |
| **一致性**  | 共识算法（如Raft）的本质，就是确保所有副本拥有完全一模一样序列号的日志 |
| **回滚能力** | 通过逆向解析日志，系统具备了逆转时间、恢复旧状态的“后悔药”能力      |

日志是接收方对顺序的本地裁定。当一个节点收到来自多个源头的并发消息时，它不需要物理时钟来决定谁先谁后。不依赖发送方的时间，而是由接收方给事件排序，谁先写入本地日志，谁就排在前面。这个自然顺序，就是该节点眼中的局部因果序。不同节点之间通过共识协议来对齐这些局部序，形成全局一致的逻辑时钟。

#### 11.7.5 分布式信任
一致性协议算法解决了节点可能崩溃但行为诚实的系统中达成共识的问题。所有节点在同一个信任域内。节点可能死，但不会撒谎。

当节点可能发出错误的消息，无论是故意还是无意识，这个假设就不存在了。  

这是拜占庭将军问题（Byzantine Generals Problem）的场景。几个将军围攻一座城市，只能通过信使通信。他们必须就进攻或撤退达成一致。但其中可能有叛徒，会发送虚假消息、谎报军情、甚至合谋破坏共识。

所以节点可能做出任意行为（恶意、伪造、合谋），而不仅仅是崩溃。这是一个根本性的跨越：

| 维度    | 崩溃-恢复模型（Paxos/Raft）  | 拜占庭模型               |
| ----- | -------------------- | ------------------- |
| 故障假设  | 节点诚实，但可能死机           | 节点可能主动作恶            |
| 信任基础  | 所有节点在同一信任域           | 节点互不信任              |
| 容错上限  | N/2（多数派）             | 2N/3（三分之二）          |
| 确定性   | 确定性共识                | 确定性的PBFT，或概率性的PoW   |
| 通信复杂度 | O(N) ~ O(N²)         | O(N²) 或更高           |
| 典型系统  | etcd、ZooKeeper、数据库集群 | 比特币、以太坊、Hyperledger |

在恢复-崩溃模型中，只要超过半数节点正常工作，系统就能推进。因为多数派之间必然有交集，可以保证一致性。但在拜占庭模型中，一个恶意节点可以向不同节点发送不同的投票（“两面派”）、声称自己收到了不存在的消息、与其它恶意节点合谋等。

为了抵御这种攻击，系统需要2/3多数。即使有1/3的节点是恶意的，剩下的1/3诚实节点加上1/3可被纠正的节点仍能保证正确性。这个2/3的阈值来源于拜占庭容错（BFT）理论的数学下界。当更多的节点不可信任，这个比例会更高。

实用拜占庭容错（PBFT）算法虽然存在，但其通信复杂度O(N²)无法扩展到大规模节点。比特币用完全不同的思路解决了这个问题：工作量证明（Proof of Work）。 不试图检测或隔离恶意节点，而是让作恶在经济上不划算：
1. 每个区块的产生需要消耗大量算力（电力、硬件成本）
2. 最长链被认为是“真相”
3. 攻击者要篡改历史，需要掌握超过50%的全网算力
4. 掌握50%算力的情况下，攻击的收益远小于诚实挖矿的收益

这是一个根本性的范式转移。不再用数学确定性保证共识，而是用经济学概率保证安全。 比特币的共识是概率性的，确认越多，被推翻的概率越低，但永远不是100%确定。

以下是关于分布式共识的简单谱系：

| 故障模型 | 容错上限 | 确定性 | 信任基础 | 代表系统 |
|----------|----------|--------|----------|----------|
| 崩溃-停止 | N/2 | 确定性 | 同一信任域 | 单机数据库 |
| 崩溃-恢复 | N/2 | 确定性 | 同一信任域 | Paxos/Raft、etcd |
| 拜占庭（PBFT） | 2N/3 | 确定性 | 节点可验证但可能恶意 | Hyperledger Fabric |
| 拜占庭（PoW） | 1/2（概率） | 概率性 | 无需信任 | 比特币、以太坊 |

#### 11.7.6 不可能性的工程边界
当共识算法要在多机世界大展身手时，另一道工程边界横亘在所有人面前，CAP定理。

我们已经知道网络分区（P, Partition）不可避免，它是广域网世界不可剥离的物理现实。因此CAP的含义是当网络分区不可避免地发生时，架构师必须在一致性（C）与可用性（A）之间做出权衡，鱼和熊掌不可兼得。

```
              [网络分区发生 (P)]
                /          \
               /            \
   [选择一致性 (CP)]        [选择可用性 (AP)]
   宁可拒绝服务或超时        明知数据可能过时
 也要保证绝不读到脏数据      也要立刻给用户响应
 (例: ZooKeeper/HBase)    (例: Cassandra/Dynamo)
```

> CAP中的可用性（A）不是系统正常工作，而是特指非故障节点对任何请求必须给出非错误的响应。AP系统在网络分区时接受的是，数据可能过时，但是也必须立刻给用户返回结果，确保有数可用。

错误的数据尽管比没有数据可用好，但是也好不了多少。数据的正确性至关重要。这就关系到一致性。一致性是一个谱系，不同的一致性有不同的属性，强一点的一致性会导致CP系统可能完全不可用，而弱一些的一致性则提供了高得多的可用性。

#### 11.7.7 一致性与CRDT
我们这里仅仅列出最常见的几种一致性语义，按照从强到弱排序。注意，他们之间并非严格偏序。
- 线性一致性（Linearizability）。按照绝对时间排序，地表最强一致性，也叫原子一致性，强一致性，即刻一致性等。
- 顺序一致性（Sequential Consistency）。放松会话间时间排序要求，会话内依然要求按照时间顺序排序。我们在共享存储多处理器已经了解了这个  
- 因果一致性（Causal Consistency）。仅仅以因果关系做排序
- 最终一致性（Eventually Consistency）。放弃因果关系，结果需要做收敛判定
- 弱一致性（Weak Consistency）。放弃一切躺平，无一致性要求。不能更弱

最终一致性看起来似乎意义不大，这就是CRDT（无冲突复制数据类型）引入的基础。CRDT的思想是通过设计数据结构，使得不需要因果或者时间判定就可以最终收敛。它不依赖任何共识，而是把合并（Merge）的冲突消除逻辑直接写进数据结构的基因里。

一个数学结构只要满足如下属性就可以是CRDT：
- 交换律。消息先到后到无所谓
- 结合律。网络怎么分组转发无所谓
- 幂等律。重传多少次无所谓

有了CRDT，即使这些节点全球分区、各自离线并发修改，一旦网络恢复，它们只需要简单地做一次数学上的并集或取最大值（Merge），整个世界就会在没有锁、没有共识算法的情况下，自动且必然地收敛到完全相同的正确状态。

这完成了分布式设计的最后一环。不用尝试维持昂贵的顺序，只要顺应物理现实，设计出天然能够容忍无序的语义结构就行。

从分布式一开始，我们就彻底放弃了时间，寻求不依赖时间的全局顺序构建办法。从简单的时间模拟（逻辑时间）到复杂的因果关系跟踪，再到更复杂的全局共识，甚至尝试完全可以容忍无需的语义结构。这一切不断说明，有可靠的时间是多么的珍贵！

现在我们回过头，再看看分布式下的时间是不是真的无可救药。

#### 11.7.8 时间，时间区间与TrueTime
每当说起时间，我们把时间要么看作是持续不断的流动，要么看作是一个绝对的点。你的火车10点20分开车，那是有明确所指。你晚到一分钟也会直接导致误车。

你也注意到，列车时刻表不会说10点20分零秒，也不会简单说10点。前者看起来很可笑，而后者显然也太宽泛了。然而说到公共假期，我们一般说从那一天到那一天，而不会具体到几点。因为这个默认的天作为最小的单位。

假设有一个客户端，一次发起两个操作A和B（A完成后发起B）。在客户端看来，A自然先于B。但是处理A和B的系统是分布式的，可能两个不同的节点N1和N2分别收到A和B。如果确保A和B的这种先后关系在这个分布式系统中也保持呢？

如果有绝对时间，A必然先于B收到，他们的接收时间戳就是顺序的可靠保证。如果N1和N2的时间完全不一致，B尽管晚，但是N2的本地时间可能比N1的大，这看起来B更早。但是如果我们可以肯定节点间的时间漂移不大于某个特定的值，我们就有办法。

假设我们肯定N1和N2的时间差不超过10分钟，那么N1收到A后，直接等10分钟（最大时间偏移），取本地时间做时间戳，然后提交并返回结果，那么N2按照类似的步骤完成B后，B的时间戳必然大于A。

这就是TrueTime的核心思想。TrueTime的假设是分布式系统的时间漂移有一个上限（通常是几毫秒，用高精度原子时钟+GPS保证）。TrueTime返回一个时间区间，\[本地时间-可能最大偏差，本地时间+可能最大偏差]，应用层可以根据这个时间区间来决定如何使用。对于写来说，等待到“本地时间+可能最大偏差”就可以保证后续事务时间戳更大（外部一致性），这是保守地“向后推”；而对于读，取“本地时间-可能最大偏差”则可以保证读到已提交的稳定数据，这是安全地“向前看”。因为时间漂移是有上界的，我们总是可以得到一个安全点来读或者写。

TrueTime将客户端可见的因果关系高效转化为全局时间戳顺序，极大简化了因果关系的位置。对于单个客户端的情况，一般叫做会话一致性。

更妙的是，即便对于多个客户端的情况，也能提供外部一致性。假设用户A给B汇款之后，然后打电话告诉B，TrueTime保证B可以查询到结果，这是侧信道因果（side channel causality），传统的因果跟踪工具完全无可奈何的场景！

事实上，TrueTime提供的是最强的线性一致性（需要commit wait等外部支持）。通过可控的时间偏移，TrueTime拥有了上帝视角，给每一个操作都赋予有意义的时间，完成了使用绝对时间才能完成的壮举。不愧为真正的时间（True Time）。

#### 11.7.9 一致性的选择
我们总结一下多机分布式系统系统的几个核心要点。   

首先，直接因果关系体现在会话中，这是从单个客户的角度看。这种显式的直接因果，在单会话内表现为“读己之所写”。多个会话收敛到唯一的任意结果，这就是顺序一致。 

其次，会话间（多个客户端）的侧信道因果是隐式的，无法直接管理。想跟踪这种因果，我们需要上帝视角，那就是外部时间保证。绝对时间不存在，但是使用漂移有上界的相对时间（TrueTime）也可以达到相同的效果，这是线性一致。

第三，如果可以涉及不依赖因果的数据结构（CRDT），我们就可以放弃对因果的执着，选择弱一致。

第四，一致性保证的是时间上的可靠性（因果），强调的是CAP中的C；一致性算法则考虑的是CAP中的A，如多副本复制，节点失效，甚至网络分区下的可用性。

#### 11.8 正确性验证
并发系统最棘手的特性在于，测试只能证明Bug存在，无法证明Bug不存在。

在单机程序中，一个难以复现的竞态条件已经够让人头疼。在分布式系统中，两个节点、三个网络延迟状态、四种故障组合的简单组合就可能产生数十种执行路径。绝大多数路径在测试环境中永远不会被执行，但在生产环境中可能在某一天突然触发。

这正是形式化验证（Formal Verification）试图解决的问题。它用数学证明系统在所有可能的执行路径下都满足某种性质。

Leslie Lamport 发明的 TLA+ 是目前最广泛使用的方法之一。它将系统建模为状态机，然后用模型检查器穷举所有可达状态。以 Raft 共识协议为例，TLA+ 可以证明，在任意网络延迟组合、任意节点故障组合下，协议永远不会选出两个不同的 Leader。

这并非纸上谈兵。Amazon 使用 TLA+ 验证了 S3 和 DynamoDB 的核心一致性协议，MongoDB 用它验证了复制协议，微软和 Intel 在芯片设计中使用形式化方法验证缓存一致性协议。

但形式化验证也不是银弹。它需要专门的数学建模技能，建模本身可能比实现更耗时。目前它的应用场景主要是错误代价极高的系统，如数据库内核、共识协议、加密协议、芯片设计等。对于普通业务系统，形式化验证的成本仍然超过收益。

一个值得注意的趋势是，随着系统越来越复杂、停机代价越来越高，形式化验证正在从学术界走向工业界。它未必是人人必备的技能，但对于构建关键基础设施的工程师来说，它正在成为工具箱的一个必备工具。

### 11.9 分布式系统的统一视角
当我们把视线重新拉回，将这幅跨越九个数量级的壮丽图景尽收眼底：

```
微观 [CPU流水线] ───> 核心问题：如何维持指令重排下的串行语义
       ↓
     [多核缓存]  ───> 核心问题：MESI协议如何修补私有Cache的共享裂缝
       ↓
     [内存模型]  ───> 核心问题：Happens-Before如何定义跨核可见性顺序
       ↓
     [NUMA架构]  ───> 核心问题：逻辑共享下的非均匀物理局部性处理
       ↓
     [操作系统]  ───> 核心问题：多进程隔离与显式消息传递边界的树立
       ↓
     [多机系统]  ───> 核心问题：绝对时间消失与局部部分失败的并发深渊
       ↓
宏观 [全球共识]  ───> 核心问题：在完全不确定性的异步网络中人为构造全局顺序
```

它们绝不是孤立分散的零碎技术，而是同一个命题在不同物理尺度上的连续展开。其目标始终如一，那就是当多个执行主体共享或传递部分状态时，我们究竟该付出何种代价，去维持可理解的语义顺序。

我们甚至可以注意到，小规模和大规模在处理某些问题上的相似性。如顺序一致性既可以表达平台指令执行，也可以表达分布式任务的顺序。这种分形特征几乎在所有的规模系统中不断涌现。

随着尺度和空间的扩大，维持物理顺序的代价越来越不可承受。系统也因而展现出一条清晰的演化光谱：从极其紧凑、自然强同步的结构，无奈却必然地走向弱一致、最终一致、概率一致，乃至无须信任完全分布式结构。

### 11.10 本章小结
分布式系统本质上是当规模扩大导致各种物理共享逐渐丧失时，人类为了维持语义正确性而发明的结构代偿史。每一次规模的扩大，都迫使系统放弃一种曾经理所当然的共享，并发明一种新的结构来代偿丧失的语义秩序。从硅片上的MESI到全球的Paxos，从硬件广播到CRDT的半格数学，从崩溃模型到拜占庭容错，结构的复杂度在增长，需要回答的问题从未改变：在失去统一的物理结构之后，如何在逻辑上重新建立可验证的语义秩序？

我们可以将这幅宏大的演进谱系完整浓缩为一张表：

| 丧失的共享 | 带来的语义危机 | 硬件/软件被迫发明的代偿结构 |
|-----------|---------------|---------------------------|
| **缓存透明性** | 核心读取过时数据，世界分裂 | MESI/目录协议（最早的片内副本复制） |
| **天然顺序性** | 编译器与CPU乱序优化导致语义崩塌 | 内存模型/内存屏障（Happens-Before偏序） |
| **内存均匀性** | 总线竞争爆炸，远端访问延迟激增 | NUMA架构与局部性调度（数据不动计算动） |
| **地址空间共享** | 隐式状态破坏，恶意越界破坏 | 进程隔离与IPC消息传递（状态走向消息驱动） |
| **统一时间轴** | 无法判定事件先后，失去绝对因果 | 逻辑时钟/向量时钟（用逻辑序替代物理序） |
| **无损网络现实** | 网络分区发生，物理割裂不可阻挡 | CAP定理边界（一致性vs可用性的痛苦取舍） |
| **节点诚实假设** | 恶意节点可能伪造消息、合谋破坏 | 拜占庭容错/工作量证明（用数学或经济换信任） |
| **直观推理性** | 状态空间指数爆炸，测试手段彻底失效 | TLA+/形式化验证（正确性从经验走向数学） |

每丧失一种共享，原有的假设就失效，新的挑战就会凸显，系统的复杂性就会陡然上升一个层级，全新的、更复杂的代偿结构就必须被发明出来。

然而这些结构并不是要完全彼此替代，选择合适的约束，依然可以得到强有力的工程实现：
- Redis依然在单机上用单线程笑傲江湖；
- 高性能数据库的内核依然在NUMA架构下疯狂压榨共享内存的每一枚并发锁；
- 操作系统依然在用进程隔离死死守护着安全的底线。

理解分布式系统最重要的，是当你在面对一个实际的架构时，能够冷酷且清醒地识别出，你的系统此时此刻，究竟站在哪个物理规模的临界点上。每跨越一个临界点，你就必须做好准备，去交出一种原以为理所当然的共享，并咬牙承担起一种伴随而来的全新复杂性。

从最细微的CPU核互联，到整个全球互联网的互联，这种结构都是人类有意设计的，转换也都是针对性优化的。当面对未知的结构时，特别是这种结构还极其庞大时，所有的显式转换设计都失效了。我们需要分析结构的表现，然后尝试拟合结构的特征，才能做有效的转换，这开始步入机器学习的领域。

## 附录
### 附录1 Paxos算法
Paxos（Lamport，1989）是第一个被证明正确的共识算法。

**角色划分**
- 提议者（Proposer）提出要达成共识的值
- 接受者（Acceptor）投票决定值
- 学习者（Learner）学习已选定的值（通常是所有节点）

**Paxos单轮（实例）流程**
```
阶段1（准备 Prepare）：
1. Proposer生成提案编号 n（全局唯一且递增）
2. 向所有Acceptor发送 Prepare(n)

阶段2（承诺 Promise）：
3. Acceptor收到 Prepare(n)：
   - 如果 n > 已承诺的编号，则承诺不再接受 <n 的提案
   - 返回当前已接受的提案（如有）

阶段3（接受 Accept）：
4. Proposer收集多数响应：
   - 如所有返回的提案为空，可自由选择值v
   - 否则选择返回中编号最大的提案值v
5. 向所有Acceptor发送 Accept(n, v)

阶段4（接受确认 Accepted）：
6. Acceptor收到 Accept(n, v)：
   - 如果未承诺过 ≥n 的Prepare，则接受
   - 通知Learner
```

Multi-Paxos通过固定Leader（避免重复准备阶段），优化到O(1)消息延迟。

Paxos有活锁风险，如两个Proposer交替提高编号，互相阻挡。工业界最常用的，是Raft算法。

### 附录2 Raft算法
Raft（Diego Ongaro，2013）的目标是“成为工业界和教学都可用的Paxos”，成功做到了。

**核心结构**
```
+----------------+     +----------------+     +----------------+
|    Leader       |     |   Follower     |     |   Follower     |
| (任期T，日志[])  |     | (任期T，日志[])  |     | (任期T，日志[])  |
+----------------+     +----------------+     +----------------+
        |                       |                       |
        |----AppendEntries----->|                       |
        |----AppendEntries------------------------------>|
```

**Raft的时间切割为任期（Term）**
```
Term 1          Term 2           Term 3
[ Leader A ] → [ Partition ] → [ Leader B ]
```

**Leader选举**
```
所有节点启动时是Follower
如果超时内未收到Leader心跳 → 变为Candidate
Candidate：
  - term++
  - 投票给自己
  - 发送RequestVote RPC给其他节点

获得多数票 → 成为Leader
收到更高term的消息 → 退回Follower
```

**日志复制**（一致性的核心）
```
Leader收到客户端请求：
  - 追加到自己的日志
  - 并行发送AppendEntries RPC给所有Follower
  - 等待多数确认 → 提交该日志项
  - 在下一个心跳中通知Follower已提交

所有节点维护：
  - 严格的日志匹配性质：如果两个节点的日志在某个索引有相同的term和命令，则之前的所有条目也一致
  - 发现不一致 → Leader覆盖Follower的日志
```

