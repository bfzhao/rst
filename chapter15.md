## 第十五章 软件工程：大规模转换的社会组织
在计算机科学的早期，编程更像是一种个人艺术。一位程序员，有一半的身份是数学家或者是计算机科学家，用机器码或汇编语言，可以完全掌控一台机器的所有资源。那时的软件是硬件的附属品，是为解决特定数学问题而编写的个人作品。然而，这种宁静的个人英雄主义时代，很快就被名叫规模的巨兽碾碎了。

当结构和转换达到一定规模，超出个人能力的边界，复杂性就自然派生，导致了软件危机。 

### 15.1 软件危机
软件危机（Software Crisis）指的软件规模的增长，突破了单个个体大脑的认知边界。当程序从几百行扩展到几万、几十万甚至上百万行时，人和人之间，团队和团队之间的协作越来越复杂，项目开始频繁地延期、预算超支，最终交付的产品充满了缺陷，甚至无法运行。软件危机真正暴露的，是人类第一次面对一种由纯信息构成、却具有工程规模的复杂系统。

弗雷德·布鲁克斯（Fred Brooks）在其传奇般的著作《没有银弹》中，将软件的复杂性分为了清晰的两层：
* 偶然复杂性（Accidental Complexity）。由工具链不完善、语法噪声、环境配置、API 历史包袱等技术细节导致的复杂性。
* 本质复杂性（Essential Complexity）。由业务逻辑本身、用户需求的非线性、现实世界的无序性以及分布式系统的物理限制（如光速带来的网络延迟）所决定的复杂性。它是由问题本身的结构决定的，无法被任何工具消除。

软件工程的诞生，正是为了对抗前者。它标志着软件从个人化的作品，转变为需要团队协作、系统化构建的系统。从这一刻起，工程化的思想被引入，目标是让软件的构建过程变得可预测、可控、可重复，即使面对的是远超个人能力的庞大系统。

软件工程是一门关于人与复杂性的学问，是社会学、管理学和计算机科学的交叉地带。它承认人类认知的局限性，并通过抽象建立层级，通过模块划分边界隔离复杂，通过架构进行复杂性的空间布局，通过协作汇聚力量，通过演化对抗时间，最终通过验证来交付信任。

### 15.2 抽象
抽象是对抗复杂性的利器。抽象的核心思想是：隐藏细节，只暴露必要的接口。这好比我们驾驶汽车，无需理解内燃机设计原理和运行情况，只需操作方向盘，加速踏板和刹车踏板即可。

抽象的核心是新概念的建立。通过隐藏大量无关的细节以及暴露必需的接口，抽象建立了全新的概念。这些概念仅仅依赖于那些暴露的接口的语义。

在软件的世界里，不同层级的抽象构成了我们的认知阶梯。
- 机器语言。直接与硬件对话，管理的是寄存器与内存地址的转换。
- 汇编语言。为机器指令赋予助记符，迈出了抽象的第一步。
- 高级语言。如FORTRAN、C、Java，它们彻底屏蔽了底层硬件细节，让程序员可以专注于问题域的逻辑。这背后，是编译器在默默地完成从高级语言到机器码的转换工作。
- 类型系统。为数据赋予意义，将内存中无意义的“0”和“1”抽象成整数、字符串、布尔值等，并规定了它们之间合法的转换操作。
- 函数/过程。将一段特定的转换逻辑打包，赋予其名称。调用者无需关心其内部实现，只需知道它能做什么。
- 对象。给数据绑定行为，数据有了可以维持内部状态的，成为行为可预测的实体。
- 模块。把相关的对象组织成模块，支持特定的功能实现。模块提供了代码复用的高级抽象。
- 服务。整个对象和模型，向外提供稳定的功能接口，可以不受干扰地独立存在。

这些抽象层级环环相扣，每一层都建立在前一层的基础之上。通过层层抽象，我们才得以在构建更复杂系统时，不会被海量的底层细节所淹没。

抽象重新分配复杂性。使用者获得简单接口，设计者承担内部设计和实现的复杂。后者即使有变化，良好的抽象也不会影响到外在语义接口。变化被隔离了。 

### 15.3 模块化
当单个抽象模块也变得过于庞大时，其内部必然是模块化的。如果说抽象解决的是简化复杂东西的外部可见行为，那么模块化解决的就是多个东西如何相处的问题。因为复杂性真正的来源，往往是组件之间错综复杂的关系。

模块是人为划定的认知边界。这个边界通过信息隐藏（Information Hiding）来强制实施。模块内部的工作细节对外部世界是不可见的，它只能通过定义良好的接口（Interface）与外界交互。这一实践的直接目标，便是追求高内聚（High Cohesion，模块内部功能紧密相关）和低耦合（Low Coupling，模块之间依赖尽可能少）。

模块化的发展也经历了和抽象类似的过程，和抽象的深度相辅相成。从过程式的模块（函数）到首先数据类型（ADT），再到面向对象编程（OOP）及其设计模式，再到组件化和服务化，这些概念都服务于同一个目的，更灵活地定义模块的边界和它们之间的协作关系。模块是人类为了限制自身认知范围而主动创造的边界。健康的边界，是系统稳定的基石。

模块之间的接口可以是简单的先通语言间的直接函数调用，也可以是跨平台间的复杂异步交互协议。模块通过接口暴露出来的行为叫做接口契约。一个模块的所有行为应该由它的接口契约决定，而不是其内部如何实现的细节。有些非直接功能化的契约，如性能要求可能体现在文档说明中，但是根本上，这些都是通过其接口契约表现出来的。

模块的抽象程度越高，维持其接口契约的成本也越高。兼容性的设计尽管增加了额外的复杂性，但是因为带来更好的演化支持能力，成为模块设计的基础要求。 

### 15.4 软件架构
当模块数量激增，系统演变成一个包含多个子系统、甚至部署在多台机器上的庞然大物时，我们需要一个更高层次的视角来组织它们，这便是软件架构（Software Architecture）。架构并不直接解决具体功能问题，而关注系统复杂性的时空分布。

架构决定了如何将系统这个大问题，在空间上切分并分配到不同的计算节点，逻辑层级，甚至时间顺序上。一个好的架构，是在充分理解业务需求的前提下，对系统未来的变化和规模增长做出的一种有意识的的技术布局。它决定了系统复杂性的基础。

架构选择的核心维度有
1. 依赖方向。谁依赖谁？是单向的（分层），还是双向的（客户端-服务端），还是无中心的（事件驱动）？
2. 状态管理。状态是集中的（服务端），还是分布在各节点（客户端/事件），还是无状态（云原生）？
3. 通信模式。是同步请求-响应，还是异步消息传递？
4. 部署边界。所有组件部署在一起（单体），还是独立部署（微服务/云原生）？

和设计模式类似，针对常见的场景，也有多种成熟的架构模式可用。这些可复用的模式，是软件架构师的基础工具。以下是常见的系统级架构模式。

#### 15.4.1 分层架构
分层架构是最常用的架构。把一个系统的复杂能力组织为从高到低的多个层级，每个层级只依赖其直接下层，然后为其直接上层通过确定的协议提供抽象能力。

分层架构的优势是极强的演化能力，每一层都可以单独发展而不影响整个系统。

#### 15.4.2 客户/服务架构（C/S）
C/S架构的核心思想是表现和服务的分离，适用于客户端计算能力不足或者地理隔离的情况下。客户端用于和用户交互，提供交互界面；服务端用于计算，数据访问和提供接口。二者之间通过二进制协议（RPC）或者文本协议（HTTP）交互。

#### 15.4.3 面向服务架构（SOA）
SOA是网络时代的服务组织架构，它强调的是服务的自主性。每个服务拥有独立的业务能力、独立的数据存储、独立的部署周期。服务之间通过标准协议（如SOAP、REST）通信，通常通过企业服务总线（ESB）进行中介和路由。

SOA的核心理念是服务级别的复用。企业已有的系统能力被封装为服务，新系统通过编排这些服务来构建。它第一次把模块的边界从代码级别提升到了组织级别。

#### 15.4.4 事件驱动架构
事件驱动架构把组件或服务之间的依赖关系抽象为消息传递。生产者发布事件，消费者订阅事件，两者通过消息队列或事件总线解耦。生产者不知道谁是消费者，消费者也一般不知道事件从何而来。

这种架构的依赖关系往往是异步的，组织极其灵活。新消费者可以随时加入，不影响已有生产者。系统从“请求-响应”的紧耦合模式，转变为“发布-订阅”的松耦合模式。

这非常适合于需要实时响应的系统（如IoT、实时风控）、需要松耦合的多团队协作场景、需要处理突发流量的系统（消息队列作为缓冲）。

#### 15.4.5 云原生架构
云原生架构不是一种单一的模式，而是一组设计原则的组合。容器化部署、自动化运维、弹性伸缩、服务网格、不可变基础设施等。它的核心目标是充分利用云计算的弹性，让系统能在负载变化时自动扩展，收缩或迁移，同时降低运维成本。

云原生架构将基础设施即代码（IAAS）的理念推向极致。服务器不再是需要人工维护的资产，而是可以随时创建和销毁的资源。部署单位从虚拟机变为轻量级的容器，配置从手工操作变为声明式描述，由协调系统（如K8S）负责维护声明和实际运行状态的一致性。

#### 15.4.6 架构演化谱系
这五种典型的架构模式展示了一个从紧耦合到松耦合的过程。在紧耦合的一端，我们有性能，确定性，易于管理等优点，在松耦合的一端，我们有灵活性好，弹性高，可用性强等优点。根据不同的软件要求，适配不同的架构风格，这是“结构即权衡”在架构层面的体现。

### 15.5 组织协作
软件工程的工程属性，在协作（Collaboration）中体现得最为淋漓尽致。当一个软件项目需要几十、几百甚至上千人共同完成时，它便第一次成为了社会化生产。如何组织这些人的智力劳动，其难度不亚于技术挑战。这是软件区别于纯算法问题的根本之处。

康威定律（Conway‘s Law）精准地描述了这种关联：“设计系统的组织，其产生的设计等同于组织之内、组织之间的沟通结构。” 也就是说，你最终得到的软件架构，是你团队沟通方式的镜像。如果组织之间存在稳定的信息隔离，那么系统中通常也会出现对应的信息边界。

为了支撑大规模协作，一系列工具和流程应运而生：
- 版本控制。这是协作的基石，它让多人能同时工作在同一个基线上，并跟踪每一次变更。
- 审查。含需求审查，代码审查等。这不仅仅是找当前输出中的缺陷，更是知识传递、统一风格和构建文化的过程。
- 问题跟踪系统。将工作项显式化、可追踪，是项目管理的核心。
- 文档。是跨越时空的沟通，将隐性知识转化为显性知识。
- 开发流程。开发流程是协作模式制度化的产品，为混乱的创造过程引入节奏和确定性。
- 产品管理。产品管理是需求管理流程化的直接结果。小的软件系统，需求理解错误不过是几十行代码的重写；而对于需要软件工程纪律支持的软件系统，需求的错误将是几百人天的巨大资源浪费。
- 项目管理。项目管理是控制成本，保障质量，确保交付的核心过程。它会尝试尽可能精确分配合适的资源到具体的阶段，确保项目目标的达成  

协作的成功，意味着将一群人的智力有效地转换成一个协调一致、目标明确的整体产出。这是软件工程的最高目标。

### 15.6 演化
软件的终极宿命是被持续修改，持续维护。一个成熟软件系统最大的特点，在于其漫长的生命周期。 软件工程管理的不是一个程序，而是这个程序长达数年乃至数十年的生命。软件不会像机械设备一样因为物理磨损而老化，它会因为持续变化而老化。每一次修改都会留下历史痕迹，如果没有结构整理，这些痕迹最终形成软件熵。与软件熵斗争，就软件生命周期中后期的主要工作内容。其内容有：
- 技术债（Technical Debt）清理。为了快速交付而采取的不完善的方案，如同金融债务，会随时间产生“利息”，增加未来修改的成本。
- 遗留系统（Legacy System）维护。任何活着的、有价值的系统，最终都会成为遗留系统。它不是贬义词，而是对现状的客观描述。
- 持续重构（Refactoring）。在不改变外部行为的前提下，持续改善内部结构，是偿还技术债务、延缓系统衰老的主要手段。
- 兼容性（Compatibility）支持。新旧版本、上下游系统之间必须能够协同工作，这极大地限制了演化的自由度。
- 版本管理与API稳定性。软件的变更通过不同的版本体现，而稳定API是软件可以长期存在的基础保证。一个不稳定的API会摧毁其所有调用者，而一个从不更新的API则会让系统停滞不前。

软件工程不仅仅要对软件的第一个发布版本负责，还需要对软件的长期演化做出结构性承诺。不支持演化的产品很难长久。

### 15.7 用验证保证可信
抽象、模块化、架构、协作、演化管理，这些最终都指向同一个终极目标，实现可信的转换。我们如何能确信，这个由无数人、历经无数修改构建起来的庞大系统，其行为是符合预期的？

验证（Verification）是回答这个问题的过程。它像一张精心编织的网，在不同层面捕获错误：
- 测试。最基础的保障。从单元测试、集成测试到系统测试，层层递进。
- 自动化测试。将测试变为可随时、重复执行的自动化流程，是持续集成的核心。
- 静态分析。在不运行代码的情况下，通过分析源码来发现潜在的错误、安全漏洞和代码坏味道。
- 类型系统。像Java、Rust这样的强类型语言，能在编译期就排除掉大量类型不匹配的错误，这是一种形式化但成本较低的验证手段。
- 形式化验证（Formal Verification）。最严格的验证方式。它用数学方法证明程序的正确性，即程序的行为完全符合其形式化规范。虽然成本极高，但在航空航天、医疗设备等安全攸关领域至关重要。

软件验证主要体现在以下方面。
1. 正确性验证。软件做对了吗？关注的是功能和逻辑。主要手段是强类型，形式验证以及全面的测试。
2. 安全性验证。软件会被破坏吗？关注的是攻击和风险。主要手段是3A设计（认证、授权，审计）静态分析，安全加固，攻击模拟等
3. 可靠性验证。软件能持续运行吗？关注的时间和故障。主要手段是数据和行为的一致性设计，压力测试，混沌工程，故障注入等
4. 性能与SLA验证。软件能达到承诺吗？关注的是性能指标，扩展性，以及可用性。主要手段是压力测试，容量规划，可观测性设计等      
5. 合规性验证。软件符合法规要求吗？关注的是数据保护，金融或者医疗等敏感领域的操作规程等。主要手段是操作留痕，审计支持，合规认证等   

验证活动贯穿软件生命周期的始终，它为用户、也为开发者自己，提供着至关重要的信心。

### 15.8 本章小结
软件工程人类为了应对大规模的软件开发主动引入其他工程领域的最佳实践，结合软件开发本身的特性而形成的工程实践活动。是对大规模转换的工程化，应对的是转换规模化后偶然复杂性的规模化。

软件工程的核心是人。单个人可能并不可靠，但是通过社会化的工程管理手段，我们可以相对确信地保证软件的可靠性。类似的工程管理哲学和规则在传统的机械工程，建筑工程，电力工程，化学工程等领域都有效地保证了大项目的成本可控和有效运作。

但是这些建立在一个基础之上。需求由人提出，规则由人设计，代码由人编写，正确性由人验证。机器最后负责翻译并执行指令，这个由物理法则保证。当人工智能开始参与代码生成本身时，这个基础正在被侵蚀。“Vibe Coding”的问世，宣告了人人可编程的时代的到来。我们不需要了解软件开发，甚至可以完全没有技术背景，只要一句话，就可以得到一个似乎还不错的东西。软件工程还需要么？如果需要，AI如何和现有的软件工程实践结合？这是我们下一章要讨论的内容。
